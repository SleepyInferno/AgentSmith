---
phase: 05-backup-confidence-dashboard
plan: 05
type: execute
wave: 5
depends_on:
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - apps/api/src/modules/backup/backup.fixtures.ts
  - apps/api/src/modules/backup/backup.repository.test.ts
  - apps/api/src/routes/backup.test.ts
  - apps/web/src/routes/backup/BackupOverviewPage.tsx
  - apps/web/src/routes/backup/BackupInventoryPage.tsx
  - apps/web/src/routes/backup/BackupDetailPage.tsx
  - apps/web/src/components/backup/BackupFindingsQueue.tsx
  - apps/web/src/components/backup/BackupInventoryTable.tsx
autonomous: true
requirements:
  - BACK-02
  - BACK-03
must_haves:
  truths:
    - "Phase 5 v1 stays read-only; no mutation routes or UI actions are added for backup jobs, restore tests, exceptions, or acknowledgements."
    - "Unknown telemetry, duplicate matches, excluded systems, and operator-attested proof each have explicit copy and test coverage."
    - "The module exposes provenance and matching confidence before asking the operator to trust a backup-health claim."
    - "Manual proof can be displayed as evidence, but not edited, approved, or acted on in this phase."
  artifacts:
    - path: "apps/api/src/modules/backup/backup.repository.test.ts"
      provides: "Repository coverage for duplicate-match, provider-outage, excluded, and operator-attested scenarios"
      contains: "Duplicate match needs review"
    - path: "apps/api/src/routes/backup.test.ts"
      provides: "HTTP contract coverage for read-only trust-boundary fields"
      contains: "isReadOnly"
    - path: "apps/web/src/routes/backup/BackupDetailPage.tsx"
      provides: "Explicit trust-boundary and provenance disclosures in the detail workflow"
      contains: "Read-only evidence view"
    - path: "apps/web/src/components/backup/BackupFindingsQueue.tsx"
      provides: "Queue rendering for duplicate-match, unknown telemetry, and excluded badges"
      contains: "Telemetry unknown"
  key_links:
    - from: "apps/api/src/modules/backup/backup.fixtures.ts"
      to: "apps/api/src/modules/backup/backup.repository.test.ts"
      via: "deterministic edge-case scenarios"
      pattern: "Duplicate match needs review"
    - from: "apps/api/src/routes/backup.test.ts"
      to: "apps/web/src/routes/backup/BackupDetailPage.tsx"
      via: "read-only trust-boundary contract"
      pattern: "isReadOnly"
    - from: "apps/web/src/components/backup/BackupFindingsQueue.tsx"
      to: "apps/web/src/routes/backup/BackupDetailPage.tsx"
      via: "state badges and provenance copy"
      pattern: "Operator-attested proof"
---

<objective>
Close the trust-boundary, verification, and edge-case gaps for backup confidence.

Purpose: Make sure Phase 5 stays reviewable and safe by proving the dangerous gray areas explicitly: duplicate identity matching, provider outages, excluded systems, operator-attested proof, and the absence of live actions.
Output: Edge-case fixtures, expanded API tests, and final UI copy that makes the read-only trust boundary unmistakable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md
@.planning/phases/05-backup-confidence-dashboard/05-02-PLAN.md
@.planning/phases/05-backup-confidence-dashboard/05-03-PLAN.md
@.planning/phases/05-backup-confidence-dashboard/05-04-PLAN.md
@apps/api/src/modules/backup/backup.repository.test.ts
@apps/api/src/routes/backup.test.ts
@apps/web/src/routes/backup/BackupOverviewPage.tsx
@apps/web/src/routes/backup/BackupDetailPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand backup fixtures and API tests for duplicate, excluded, unknown, and operator-attested evidence states</name>
  <files>apps/api/src/modules/backup/backup.fixtures.ts, apps/api/src/modules/backup/backup.repository.test.ts, apps/api/src/routes/backup.test.ts</files>
  <read_first>apps/api/src/modules/backup/backup.fixtures.ts, apps/api/src/modules/backup/backup.repository.test.ts, apps/api/src/routes/backup.test.ts, apps/api/src/modules/backup/backup.repository.ts, .planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md</read_first>
  <behavior>
    - Test 1: duplicate system matching produces `matchingConfidence: 'duplicate'`, `confidenceState: 'unknown'`, and the summary `Duplicate match needs review`
    - Test 2: excluded systems remain visible in inventory but do not appear as missing coverage in the review queue
    - Test 3: provider outage or stale connector freshness produces `sourceHealth.state: 'stale'` or `sourceHealth.state: 'error'` and preserves `isReadOnly: true`
    - Test 4: operator-attested restore proof is visible in detail payloads but does not enable any write action or approval state
  </behavior>
  <action>Extend `apps/api/src/modules/backup/backup.fixtures.ts` with deterministic cases for duplicate identity matching, excluded-by-policy systems, provider outage, stale connector freshness, and operator-attested restore proof. Update `apps/api/src/modules/backup/backup.repository.test.ts` to assert exact summaries and trust fields including `Duplicate match needs review`, `Telemetry unknown`, `Excluded by policy`, `Operator-attested proof`, and `matchingConfidence`. Update `apps/api/src/routes/backup.test.ts` so `/api/backup/overview`, `/api/backup/systems`, and `/api/backup/systems/:systemId` all prove the presence of exact fields `isReadOnly`, `sourceHealth`, `matchingConfidence`, and `evidenceSource`, and ensure none of those responses expose mutation affordances such as `actionUrl`, `mutation`, or `approve`.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/modules/backup/backup.repository.test.ts apps/api/src/routes/backup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `Duplicate match needs review`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `Telemetry unknown`
    - `apps/api/src/modules/backup/backup.repository.test.ts` contains `matchingConfidence`
    - `apps/api/src/modules/backup/backup.repository.test.ts` contains `Operator-attested proof`
    - `apps/api/src/routes/backup.test.ts` contains `isReadOnly`
    - `apps/api/src/routes/backup.test.ts` contains `sourceHealth`
    - `apps/api/src/routes/backup.test.ts` contains `matchingConfidence`
    - `apps/api/src/routes/backup.test.ts` does not contain `actionUrl`
  </acceptance_criteria>
  <done>The API layer now proves the edge cases most likely to undermine operator trust, without drifting into unplanned write flows.</done>
</task>

<task type="auto">
  <name>Task 2: Close trust-boundary copy and edge-case visibility across backup overview, inventory, and detail UI</name>
  <files>apps/web/src/routes/backup/BackupOverviewPage.tsx, apps/web/src/routes/backup/BackupInventoryPage.tsx, apps/web/src/routes/backup/BackupDetailPage.tsx, apps/web/src/components/backup/BackupFindingsQueue.tsx, apps/web/src/components/backup/BackupInventoryTable.tsx</files>
  <read_first>apps/web/src/routes/backup/BackupOverviewPage.tsx, apps/web/src/routes/backup/BackupInventoryPage.tsx, apps/web/src/routes/backup/BackupDetailPage.tsx, apps/web/src/components/backup/BackupFindingsQueue.tsx, apps/web/src/components/backup/BackupInventoryTable.tsx, .planning/phases/05-backup-confidence-dashboard/05-04-PLAN.md</read_first>
  <action>Update the backup overview, inventory, detail, queue, and table surfaces so any manual or operator-attested evidence renders the exact note `Read-only evidence view - no backup jobs, restores, or exceptions can be executed here.` Add exact visual or text badges `Excluded by policy`, `Telemetry unknown`, `Operator-attested proof`, and `Duplicate match needs review` wherever those states appear. Keep queue items and detail cards explanation-first by surfacing provenance, matching confidence, and next-step text, and ensure no component renders live-action verbs such as `Run restore`, `Acknowledge`, `Approve exception`, or `Retry backup`. Preserve the queue-to-detail navigation path while making the trust boundary more explicit than the base Phase 5 UI plans.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `Read-only evidence view - no backup jobs, restores, or exceptions can be executed here.`
    - `apps/web/src/routes/backup/BackupInventoryPage.tsx` contains `Excluded by policy`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Telemetry unknown`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Operator-attested proof`
    - `apps/web/src/components/backup/BackupFindingsQueue.tsx` contains `Duplicate match needs review`
    - `apps/web/src/components/backup/BackupInventoryTable.tsx` contains `Excluded by policy`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` does not contain `Run restore`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` does not contain `Approve exception`
  </acceptance_criteria>
  <done>The backup UI now makes the phase's read-only trust boundary explicit and surfaces the edge cases that most affect operator confidence.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/modules/backup/backup.repository.test.ts apps/api/src/routes/backup.test.ts` succeeds
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Duplicate matches, excluded systems, operator-attested proof, and unknown telemetry are all visible as distinct states
- [ ] No Phase 5 surface implies live backup, restore, acknowledgement, or exception writes
</verification>

<success_criteria>

- Phase 5 closes the highest-risk trust and verification gaps before execution starts
- Operators can see provenance and ambiguity before trusting a backup-confidence claim
- The final plan set keeps Backup Confidence firmly inside a read-only v1 boundary

</success_criteria>

<output>
After completion, create `.planning/phases/05-backup-confidence-dashboard/05-05-SUMMARY.md`
</output>
