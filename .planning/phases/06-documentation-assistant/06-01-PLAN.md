---
phase: 06-documentation-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - apps/api/src/modules/docs/docs.types.ts
  - apps/api/src/modules/docs/docs.fixtures.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "Documentation records persist searchable body text, review freshness, and operational kind so the module can rank more than document titles."
    - "System links, site tags, owner tags, category tags, and history are first-class canonical records instead of connector-specific blobs."
    - "Seeded example documents cover SOPs, vendor notes, contacts, infrastructure notes, and recovery procedures so Phase 06 stays usable before live connectors exist."
    - "Canonical TypeScript contracts exist for queue, search, detail, and explicit metadata review flows before repository or UI work begins."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Canonical documentation persistence for searchable content, typed metadata, system links, and revision history"
      contains: "model DocumentRevision"
    - path: "apps/api/src/modules/docs/docs.types.ts"
      provides: "Shared Phase 6 contracts for overview, search inventory, detail, and metadata review responses"
      contains: "export type DocumentationSearchResult"
    - path: "apps/api/src/modules/docs/docs.fixtures.ts"
      provides: "Deterministic seeded documentation corpus and search scenarios across all five v1 document kinds"
      contains: "documentationFixtureSearchCases"
  key_links:
    - from: "prisma/schema.prisma"
      to: "apps/api/src/modules/docs/docs.types.ts"
      via: "shared document kind, review state, and history semantics"
      pattern: "DocumentReviewState"
    - from: "apps/api/src/modules/docs/docs.fixtures.ts"
      to: "apps/api/src/modules/docs/docs.types.ts"
      via: "seeded queue, search, and detail DTO inputs"
      pattern: "DocumentationQueueItem"
    - from: "prisma/schema.prisma"
      to: "apps/api/src/modules/docs/docs.fixtures.ts"
      via: "fixture coverage for metadata assignments, system links, and revision history"
      pattern: "DocumentMetadataAssignment"
---

<objective>
Create the canonical documentation model, typed contracts, and seeded search corpus for Phase 06.

Purpose: Lock the searchable document shape, metadata-link shape, and history shape before repository, route, or UI work starts.
Output: Prisma schema additions, shared docs contracts, and deterministic fixtures that cover stale reviews, missing metadata, and operational search cases.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation-assistant/06-RESEARCH.md
@.planning/phases/06-documentation-assistant/06-VALIDATION.md
@AGENTS.md
@prisma/schema.prisma
@apps/api/src/modules/backup/backup.types.ts
@apps/api/src/modules/network/network.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical document, metadata, system-link, and history contracts</name>
  <files>prisma/schema.prisma, apps/api/src/modules/docs/docs.types.ts</files>
  <read_first>prisma/schema.prisma, .planning/phases/06-documentation-assistant/06-RESEARCH.md, .planning/phases/06-documentation-assistant/06-VALIDATION.md, AGENTS.md, apps/api/src/modules/backup/backup.types.ts, apps/api/src/modules/network/network.types.ts</read_first>
  <action>Extend `prisma/schema.prisma` by keeping the existing `Document` identity fields for migration safety while adding the exact enums `DocumentKind` with values `sop`, `vendor_note`, `contact`, `infrastructure_note`, and `recovery_procedure`; `DocumentReviewState` with values `current`, `due_soon`, `overdue`, and `unreviewed`; `DocumentMetadataDimension` with values `site`, `owner`, and `category`; and `DocumentRevisionType` with values `source_sync`, `metadata_review`, and `review_completed`. Extend `Document` with exact fields `kind`, `summary`, `contentText`, `searchText`, `reviewState`, `lastReviewedAt`, `sourceUpdatedAt`, and `contentUpdatedAt`; keep `reviewDueAt`; and add relations `metadataAssignments`, `systemLinks`, and `revisions`. Add `DocumentMetadataAssignment` with fields `documentId`, `dimension`, `valueKey`, `valueLabel`, `createdAt`, and `updatedAt`; `DocumentSystemLink` with fields `documentId`, `systemId`, `relationshipLabel`, `createdAt`, and `updatedAt`; and `DocumentRevision` with fields `documentId`, `revisionType`, `summary`, `changedFields`, `actorLabel`, `reviewState`, `reviewDueAt`, and `createdAt`. Add the back relation `documentationLinks` on `System`. Create `apps/api/src/modules/docs/docs.types.ts` exporting exact arrays and types named `documentationDataModes`, `documentationWriteBoundaries`, `documentKinds`, `documentReviewStates`, `documentMetadataDimensions`, `documentRevisionTypes`, `DocumentMetadataTag`, `DocumentLinkedSystem`, `DocumentationSearchReason`, `DocumentationOverviewCard`, `DocumentationQueueItem`, `DocumentationOverview`, `DocumentationFacetOption`, `DocumentationSearchFacets`, `DocumentationSearchFilters`, `DocumentationSearchResult`, `DocumentationSearchResponse`, `DocumentHistoryEntry`, `DocumentationDetail`, `DocumentationMetadataCatalog`, `DocumentationMetadataReviewInput`, and `DocumentationMetadataReviewResult`. Use the exact write-boundary string `metadata_review_only` and include exact DTO fields `matchedExcerpt`, `relevanceScore`, `suggestedNextStep`, `history`, `metadataCatalog`, and `changedFields` so later plans do not invent contracts.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const schema=fs.readFileSync('prisma/schema.prisma','utf8');const types=fs.readFileSync('apps/api/src/modules/docs/docs.types.ts','utf8');for (const token of ['enum DocumentKind','enum DocumentReviewState','enum DocumentMetadataDimension','enum DocumentRevisionType','model DocumentMetadataAssignment','model DocumentSystemLink','model DocumentRevision','searchText String','documentationLinks DocumentSystemLink[]']) { if (!schema.includes(token)) process.exit(1); } for (const token of ['documentationWriteBoundaries','metadata_review_only','DocumentationQueueItem','DocumentationSearchResponse','DocumentationMetadataReviewInput','DocumentationMetadataReviewResult']) { if (!types.includes(token)) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `enum DocumentKind`
    - `prisma/schema.prisma` contains `enum DocumentReviewState`
    - `prisma/schema.prisma` contains `enum DocumentMetadataDimension`
    - `prisma/schema.prisma` contains `enum DocumentRevisionType`
    - `prisma/schema.prisma` contains `model DocumentMetadataAssignment`
    - `prisma/schema.prisma` contains `model DocumentSystemLink`
    - `prisma/schema.prisma` contains `model DocumentRevision`
    - `prisma/schema.prisma` contains `searchText String`
    - `prisma/schema.prisma` contains `documentationLinks DocumentSystemLink[]`
    - `apps/api/src/modules/docs/docs.types.ts` contains `metadata_review_only`
    - `apps/api/src/modules/docs/docs.types.ts` contains `export type DocumentationSearchResult`
    - `apps/api/src/modules/docs/docs.types.ts` contains `export type DocumentationMetadataReviewInput`
  </acceptance_criteria>
  <done>The repo has one canonical documentation domain model and one shared TypeScript contract set for every later Phase 06 slice.</done>
</task>

<task type="auto">
  <name>Task 2: Seed a deterministic documentation corpus with overdue, incomplete, and recovery-critical examples</name>
  <files>apps/api/src/modules/docs/docs.fixtures.ts</files>
  <read_first>apps/api/src/modules/docs/docs.types.ts, prisma/schema.prisma, .planning/phases/06-documentation-assistant/06-RESEARCH.md, .planning/phases/06-documentation-assistant/06-VALIDATION.md</read_first>
  <action>Create `apps/api/src/modules/docs/docs.fixtures.ts` exporting exact arrays `documentationFixtures`, `documentMetadataAssignmentFixtures`, `documentSystemLinkFixtures`, `documentRevisionFixtures`, `documentationFixtureSystems`, and `documentationFixtureSearchCases`. Include at least ten deterministic documents covering the five v1 kinds with exact IDs `doc-password-reset-sop`, `doc-branch-firewall-recovery`, `doc-contoso-isp-contacts`, `doc-m365-break-glass`, `doc-hyperv-cluster-notes`, `doc-veeam-renewal-notes`, `doc-new-hire-workstation-setup`, `doc-domain-controller-restore`, `doc-print-vendor-contact-list`, and `doc-site-oncall-handoff`. Ensure the fixture corpus includes exact queue summary strings `Review due date has passed`, `Document metadata is incomplete for operational search`, and `Content changed after the last review`; at least one overdue review case, one due-soon case, one unreviewed recent-change case, one missing-owner case, one missing-site case, and one multi-system recovery document. Add exact search cases where query `sharepoint restore` returns `doc-m365-break-glass`, query `fiber noc circuit` returns `doc-contoso-isp-contacts`, query `hyper-v san` returns `doc-hyperv-cluster-notes`, and query `veeam renewal` returns `doc-veeam-renewal-notes` as top matches. Keep seeded metadata and linked-system labels operator-readable so later overview, search, and detail UI copy can ship without inventing dummy text.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const fixtures=fs.readFileSync('apps/api/src/modules/docs/docs.fixtures.ts','utf8');for (const token of ['documentationFixtures','documentMetadataAssignmentFixtures','documentSystemLinkFixtures','documentRevisionFixtures','documentationFixtureSystems','documentationFixtureSearchCases','doc-m365-break-glass','doc-contoso-isp-contacts','Review due date has passed','Document metadata is incomplete for operational search','Content changed after the last review','sharepoint restore','fiber noc circuit','veeam renewal']) { if (!fixtures.includes(token)) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `documentationFixtures`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `documentMetadataAssignmentFixtures`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `documentSystemLinkFixtures`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `documentRevisionFixtures`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `documentationFixtureSystems`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `documentationFixtureSearchCases`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `doc-m365-break-glass`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `Review due date has passed`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `Document metadata is incomplete for operational search`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `Content changed after the last review`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `sharepoint restore`
    - `apps/api/src/modules/docs/docs.fixtures.ts` contains `fiber noc circuit`
  </acceptance_criteria>
  <done>Phase 06 now has a seeded document corpus and search cases that prove the operator-facing requirements before any API or UI work begins.</done>
</task>

</tasks>

<verification>
- [ ] `npx prisma validate --schema prisma/schema.prisma` succeeds
- [ ] The documentation model stores searchable content, typed metadata, linked systems, and revision history without widening into a full editor
- [ ] Seeded fixtures cover all five v1 document kinds plus overdue and incomplete-document queue scenarios
</verification>

<success_criteria>

- Phase 06 has a canonical documentation data model that can satisfy search, tagging, and history requirements
- Seeded example data makes the module demoable before a live documentation connector exists
- Later repository, route, and UI plans can work from stable shared contracts instead of rediscovering data shape

</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-assistant/06-01-SUMMARY.md`
</output>
