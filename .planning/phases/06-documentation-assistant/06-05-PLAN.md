---
phase: 06-documentation-assistant
plan: 05
type: execute
wave: 5
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - prisma/migrations/20260328_0605_documentation_search_hardening/migration.sql
  - apps/api/src/modules/docs/docs.repository.ts
  - apps/api/src/modules/docs/docs.repository.test.ts
  - apps/api/src/routes/docs.ts
  - apps/api/src/routes/docs.test.ts
  - apps/web/src/lib/docs.ts
  - apps/web/src/routes/docs/DocumentationDetailPage.tsx
  - apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "Metadata changes are explicit, reviewable, and logged; there is no silent save path for document tags or review dates."
    - "A metadata review updates document tags, linked systems, review timing, document history, and audit trail from one server-owned mutation."
    - "Search remains useful for short vendor, contact, and acronym-heavy queries instead of only long body-text phrases."
    - "The detail flow makes the write boundary unmistakable by showing before-and-after metadata plus audit language before submit."
  artifacts:
    - path: "prisma/migrations/20260328_0605_documentation_search_hardening/migration.sql"
      provides: "Search hardening support for short-label matching"
      contains: "pg_trgm"
    - path: "apps/api/src/modules/docs/docs.repository.ts"
      provides: "Metadata review mutation plus audit and history writes"
      contains: "submitMetadataReview"
    - path: "apps/api/src/routes/docs.ts"
      provides: "HTTP mutation contract for explicit metadata review"
      contains: "/api/docs/:documentId/metadata-review"
    - path: "apps/api/src/routes/docs.test.ts"
      provides: "Route coverage for explicit review writes, validation, and audit-safe responses"
      contains: "docs.metadata.reviewed"
    - path: "apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx"
      provides: "Before-and-after review UI for docs metadata changes"
      contains: "Save metadata review (audit log entry)"
    - path: "apps/web/src/routes/docs/DocumentationDetailPage.tsx"
      provides: "Explicit review launch point from the explanation-first detail route"
      contains: "Review metadata"
  key_links:
    - from: "apps/api/src/routes/docs.ts"
      to: "apps/api/src/modules/docs/docs.repository.ts"
      via: "explicit metadata review mutation"
      pattern: "submitMetadataReview"
    - from: "apps/api/src/modules/docs/docs.repository.ts"
      to: "prisma/schema.prisma"
      via: "DocumentRevision and AuditEvent writes"
      pattern: "docs.metadata.reviewed"
    - from: "apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx"
      to: "apps/web/src/routes/docs/DocumentationDetailPage.tsx"
      via: "before-and-after review flow"
      pattern: "Save metadata review"
---

<objective>
Add the explicit metadata review flow, audit logging, and final search-relevance hardening for the Documentation Assistant.

Purpose: Finish Phase 06 with a trustworthy write boundary so operators can improve document relevance without turning the module into a silent editor or weak search index.
Output: Metadata-review API mutation with audit/history writes, short-label search hardening, and a detail-page review panel with before-and-after confirmation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation-assistant/06-RESEARCH.md
@.planning/phases/06-documentation-assistant/06-02-PLAN.md
@.planning/phases/06-documentation-assistant/06-03-PLAN.md
@.planning/phases/06-documentation-assistant/06-04-PLAN.md
@AGENTS.md
@apps/api/src/modules/lifecycle/lifecycle.repository.ts
@apps/api/src/routes/docs.ts
@apps/web/src/routes/docs/DocumentationDetailPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the explicit metadata-review mutation, audit trail write, and short-label search hardening</name>
  <files>prisma/migrations/20260328_0605_documentation_search_hardening/migration.sql, apps/api/src/modules/docs/docs.repository.ts, apps/api/src/modules/docs/docs.repository.test.ts, apps/api/src/routes/docs.ts, apps/api/src/routes/docs.test.ts</files>
  <read_first>apps/api/src/modules/docs/docs.repository.ts, apps/api/src/modules/docs/docs.repository.test.ts, apps/api/src/routes/docs.ts, apps/api/src/routes/docs.test.ts, apps/api/src/modules/lifecycle/lifecycle.repository.ts, .planning/phases/06-documentation-assistant/06-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `POST /api/docs/:documentId/metadata-review` persists category, site, owner, linked-system, and `reviewDueAt` changes and returns `changedFields`, `historyEntryId`, and `auditAction: 'docs.metadata.reviewed'`
    - Test 2: metadata review writes append one `DocumentRevision` event and one `AuditEvent` with before-and-after metadata snapshots
    - Test 3: after metadata review, search and overview calls return the updated tags and remove resolved overdue or incomplete queue reasons
    - Test 4: short queries like `veeam`, `noc`, and `mx` still return the expected vendor/contact/infrastructure records through trigram or title/metadata fallback
    - Test 5: invalid bodies missing `reviewSummary` or `actorLabel` return `400` and do not write audit or history rows
  </behavior>
  <action>Create `prisma/migrations/20260328_0605_documentation_search_hardening/migration.sql` with `CREATE EXTENSION IF NOT EXISTS pg_trgm;` plus exact indexes `document_title_trgm_idx` and `document_search_text_trgm_idx` so short vendor, contact, and acronym-heavy searches stay useful. Extend `apps/api/src/modules/docs/docs.repository.ts` with exact method `submitMetadataReview(documentId, input)` where `input` uses the exact fields `categoryLabels`, `siteLabels`, `ownerLabels`, `systemIds`, `reviewDueAt`, `reviewSummary`, and `actorLabel`. The method must update `Document.searchText`, `Document.reviewState`, `Document.lastReviewedAt`, metadata assignments, system links, and create both `DocumentRevision` with `revisionType: 'metadata_review'` and `AuditEvent` with exact action `docs.metadata.reviewed`, target type `Document`, and before/after metadata snapshots inside `metadata`. Extend the search query so when full-text results are empty or the query is shorter than 4 characters it uses trigram or exact-title/metadata fallback without moving relevance logic to React. Add `POST /api/docs/:documentId/metadata-review` to `apps/api/src/routes/docs.ts`, validate required fields `reviewSummary` and `actorLabel`, and return exact response fields `documentId`, `changedFields`, `historyEntryId`, `auditAction`, `reviewDueAt`, and `lastReviewedAt`. Lock all of this in repository and route tests, including queue removal after a successful review mutation.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/modules/docs/docs.repository.test.ts apps/api/src/routes/docs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/migrations/20260328_0605_documentation_search_hardening/migration.sql` contains `pg_trgm`
    - `prisma/migrations/20260328_0605_documentation_search_hardening/migration.sql` contains `document_title_trgm_idx`
    - `apps/api/src/modules/docs/docs.repository.ts` contains `submitMetadataReview`
    - `apps/api/src/modules/docs/docs.repository.ts` contains `docs.metadata.reviewed`
    - `apps/api/src/modules/docs/docs.repository.ts` contains `reviewSummary`
    - `apps/api/src/routes/docs.ts` contains `/api/docs/:documentId/metadata-review`
    - `apps/api/src/routes/docs.ts` contains `actorLabel`
    - `apps/api/src/routes/docs.test.ts` contains `docs.metadata.reviewed`
    - `apps/api/src/routes/docs.test.ts` contains `historyEntryId`
    - `apps/api/src/modules/docs/docs.repository.test.ts` contains `veeam`
    - `apps/api/src/modules/docs/docs.repository.test.ts` contains `noc`
  </acceptance_criteria>
  <done>The backend now supports explicit, audited metadata review plus final search hardening for short operational queries.</done>
</task>

<task type="auto">
  <name>Task 2: Build the before-and-after metadata review panel on the document detail route</name>
  <files>apps/web/src/lib/docs.ts, apps/web/src/routes/docs/DocumentationDetailPage.tsx, apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx</files>
  <read_first>apps/web/src/lib/docs.ts, apps/web/src/routes/docs/DocumentationDetailPage.tsx, apps/api/src/routes/docs.ts, .planning/phases/06-documentation-assistant/06-04-PLAN.md, AGENTS.md</read_first>
  <action>Extend `apps/web/src/lib/docs.ts` with exact mutation helper `reviewDocumentMetadata(input)` targeting `/api/docs/:documentId/metadata-review`, plus the exact types `DocumentationMetadataReviewRequest` and `DocumentationMetadataReviewResponse`. Reuse the shared query-key helper from Plan 03 by importing or exporting `docsQueryKeys` in the same file, and require the detail route to invalidate `docsQueryKeys.overview`, `docsQueryKeys.searchRoot`, and `docsQueryKeys.detail(documentId)` instead of hard-coded string literals or unavailable filter state. Create `DocumentMetadataReviewPanel.tsx` with explicit before-and-after sections for `categoryLabels`, `siteLabels`, `ownerLabels`, `systemIds`, and `reviewDueAt`; a required summary field labeled `Review summary`; a required actor field labeled `Operator name`; the exact checkbox label `I understand this metadata review creates an audit log entry`; and the exact submit button copy `Save metadata review (audit log entry)`. Update `DocumentationDetailPage.tsx` to add a `Review metadata` launch action, render the review panel inline or in a side panel, and invalidate the shared overview, search, and detail keys after a successful mutation so the queue, search inventory, and detail route all reflect updated tags immediately. Keep the exact trust copy `Document content stays read-only in this phase. Metadata changes go through explicit review and audit logging.` visible while the review panel is open, and do not add freeform body editing, attachment upload, or AI-generated content actions.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/docs.ts` contains `reviewDocumentMetadata`
    - `apps/web/src/lib/docs.ts` contains `docsQueryKeys`
    - `apps/web/src/lib/docs.ts` contains `/api/docs/`
    - `apps/web/src/lib/docs.ts` contains `metadata-review`
    - `apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx` contains `Review summary`
    - `apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx` contains `Operator name`
    - `apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx` contains `I understand this metadata review creates an audit log entry`
    - `apps/web/src/components/docs/DocumentMetadataReviewPanel.tsx` contains `Save metadata review (audit log entry)`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `Review metadata`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `docsQueryKeys.overview`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `docsQueryKeys.searchRoot`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `docsQueryKeys.detail`
  </acceptance_criteria>
  <done>The operator can improve document relevance through one explicit review flow that is visible, auditable, and still clearly outside full document authoring scope.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/modules/docs/docs.repository.test.ts apps/api/src/routes/docs.test.ts` succeeds
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Metadata reviews create history plus audit artifacts and refresh the queue/search/detail surfaces
- [ ] No Phase 06 screen implies rich-text authoring, attachment management, or silent metadata saves
</verification>

<success_criteria>

- Documentation metadata updates are explicit, reviewable, and logged
- Search remains useful for short operational queries and vendor/contact names
- Phase 06 closes inside a metadata-review boundary instead of drifting into a wiki or editor platform

</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-assistant/06-05-SUMMARY.md`
</output>
