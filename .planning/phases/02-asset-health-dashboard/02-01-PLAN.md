---
phase: 02-asset-health-dashboard
plan: 01
type: execute
wave: 1
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - prisma/schema.prisma
  - apps/api/src/modules/assets/asset-health.types.ts
  - apps/api/src/modules/assets/asset-health.repository.ts
  - apps/api/src/modules/assets/asset-health.service.ts
  - apps/api/src/modules/assets/asset-health.service.test.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-04
user_setup: []
must_haves:
  truths:
    - Canonical device identity stays separate from derived dashboard risk output.
    - Asset health statuses support unknown and incomplete states instead of treating missing telemetry as healthy.
    - Risk scoring is deterministic, explainable, and reusable by the queue, inventory, and detail views.
  artifacts:
    - prisma/schema.prisma
    - apps/api/src/modules/assets/asset-health.types.ts
    - apps/api/src/modules/assets/asset-health.repository.ts
    - apps/api/src/modules/assets/asset-health.service.ts
  key_links:
    - Phase 2 reads normalized device and user data created by the Phase 1 data backbone.
    - The asset health service emits one shared DTO contract that later API routes reuse directly.
    - Risk reasons are stored or returned as structured codes and explanations, not freeform UI-only text.
---

<objective>
Create the asset-health domain backbone for Phase 2.

Purpose: Extend the canonical data model and backend service layer so the app can compute trustworthy device risk without leaking connector-specific payloads into the UI.
Output: Device health fields or snapshots, typed asset DTOs, a deterministic scoring service, and backend tests for the core ranking logic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-asset-health-dashboard/02-RESEARCH.md
@.planning/phases/02-asset-health-dashboard/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the canonical asset-health data model</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma, .planning/REQUIREMENTS.md, .planning/phases/02-asset-health-dashboard/02-RESEARCH.md, .planning/phases/01-foundations-and-secure-data-flow/01-01-PLAN.md</read_first>
  <action>Update `prisma/schema.prisma` so Phase 2 can represent asset-health signals explicitly. Add enums `AssetSignalStatus` with values `healthy`, `warning`, `missing`, `unsupported`, `unknown` and `AssetRiskLevel` with values `low`, `watch`, `high`, `critical`. Extend `Device` with the exact nullable fields `departmentSnapshot String?`, `siteSnapshot String?`, `encryptionStatus AssetSignalStatus?`, `antivirusStatus AssetSignalStatus?`, `patchStatus AssetSignalStatus?`, `diskFreePercent Int?`, `deviceAgeDays Int?`, `supportStatus AssetSignalStatus?`, and `lastCheckInAt DateTime?`. Add a new model `DeviceRiskAssessment` with fields `id String @id @default(cuid())`, `deviceId String @unique`, `device Device @relation(fields: [deviceId], references: [id])`, `riskScore Int`, `riskLevel AssetRiskLevel`, `queueRank Int`, `sourceFreshnessState FreshnessState`, `summary String`, `signals Json`, `calculatedAt DateTime @default(now())`, `createdAt DateTime @default(now())`, and `updatedAt DateTime @updatedAt`.</action>
  <verify>pnpm db:generate</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `enum AssetSignalStatus`
    - prisma/schema.prisma contains `healthy`, `warning`, `missing`, `unsupported`, and `unknown` under `AssetSignalStatus`
    - prisma/schema.prisma contains `enum AssetRiskLevel`
    - prisma/schema.prisma contains `model DeviceRiskAssessment`
    - prisma/schema.prisma contains `departmentSnapshot`, `siteSnapshot`, `encryptionStatus`, `antivirusStatus`, `patchStatus`, `diskFreePercent`, `deviceAgeDays`, `supportStatus`, and `lastCheckInAt` on `Device`
  </acceptance_criteria>
  <done>The schema can represent inventory fields, explicit signal states, and derived device risk output for the dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Define typed asset DTOs and repository queries</name>
  <files>apps/api/src/modules/assets/asset-health.types.ts, apps/api/src/modules/assets/asset-health.repository.ts, packages/shared/src/index.ts</files>
  <read_first>apps/api/src/server.ts, prisma/schema.prisma, packages/shared/src/index.ts, .planning/phases/02-asset-health-dashboard/02-RESEARCH.md</read_first>
  <action>Create `apps/api/src/modules/assets/asset-health.types.ts` with the exact exported types `AssetRiskSignalCode`, `AssetRiskSignal`, `AssetInventoryRow`, `AssetQueueItem`, `AssetDetail`, and `AssetInventoryFilters`. Define `AssetRiskSignalCode` values as `stale_check_in`, `low_disk`, `missing_encryption`, `missing_antivirus`, `missing_patch`, `unsupported_os`, `old_device`, and `data_incomplete`. Create `apps/api/src/modules/assets/asset-health.repository.ts` with repository methods `listInventory(filters)`, `listQueue(limit)`, and `getDeviceDetail(deviceId)` that query `Device`, join owner information from `User`, and include related `DeviceRiskAssessment` records. Export the shared asset DTO types from `packages/shared/src/index.ts` so web and api packages can consume the same contract names.</action>
  <verify>pnpm --filter @agentsmith/api build</verify>
  <acceptance_criteria>
    - apps/api/src/modules/assets/asset-health.types.ts contains `export type AssetRiskSignalCode`
    - apps/api/src/modules/assets/asset-health.types.ts contains `stale_check_in`, `low_disk`, `missing_encryption`, `missing_antivirus`, `missing_patch`, `unsupported_os`, `old_device`, and `data_incomplete`
    - apps/api/src/modules/assets/asset-health.repository.ts contains `listInventory`, `listQueue`, and `getDeviceDetail`
    - packages/shared/src/index.ts contains `AssetInventoryRow` or a re-export from `asset-health.types`
  </acceptance_criteria>
  <done>The repo has a typed asset-health contract that hides raw provider payloads and can be reused by later API routes and UI pages.</done>
</task>

<task type="auto">
  <name>Task 3: Implement deterministic risk scoring and backend tests</name>
  <files>apps/api/src/modules/assets/asset-health.service.ts, apps/api/src/modules/assets/asset-health.service.test.ts</files>
  <read_first>apps/api/src/modules/assets/asset-health.types.ts, apps/api/src/modules/assets/asset-health.repository.ts, .planning/phases/02-asset-health-dashboard/02-RESEARCH.md, .planning/phases/02-asset-health-dashboard/02-VALIDATION.md</read_first>
  <action>Create `apps/api/src/modules/assets/asset-health.service.ts` with functions `scoreDeviceRisk(device)`, `buildQueueItems(devices)`, and `buildDeviceDetail(device)` that compute a deterministic integer `riskScore`, assign `riskLevel` values `low`, `watch`, `high`, or `critical`, and emit signal objects containing `code`, `severity`, `label`, and `explanation`. Weight the exact rules as follows: `missing_encryption` +35, `missing_antivirus` +30, `missing_patch` +25, `unsupported_os` +25, `stale_check_in` +20, `low_disk` +15, `old_device` +10, `data_incomplete` +10. Create `apps/api/src/modules/assets/asset-health.service.test.ts` covering at least one high-risk device with missing encryption plus stale check-in, one incomplete-data device that stays non-healthy, and one low-risk device that produces an empty or low-severity signal list.</action>
  <verify>pnpm --filter @agentsmith/api test</verify>
  <acceptance_criteria>
    - apps/api/src/modules/assets/asset-health.service.ts contains `scoreDeviceRisk`
    - apps/api/src/modules/assets/asset-health.service.ts contains `missing_encryption`, `missing_antivirus`, `missing_patch`, `unsupported_os`, `stale_check_in`, `low_disk`, `old_device`, and `data_incomplete`
    - apps/api/src/modules/assets/asset-health.service.ts contains `riskScore` and `riskLevel`
    - apps/api/src/modules/assets/asset-health.service.test.ts contains `missing encryption` or `missing_encryption`
    - apps/api/src/modules/assets/asset-health.service.test.ts exits 0 through `pnpm --filter @agentsmith/api test`
  </acceptance_criteria>
  <done>The backend can calculate and explain device risk consistently enough for queue, inventory, and detail surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm db:generate` succeeds
- [ ] `pnpm --filter @agentsmith/api build` succeeds
- [ ] `pnpm --filter @agentsmith/api test` succeeds
- [ ] Risk-scoring tests cover high-risk, low-risk, and incomplete-data examples
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Asset-health scoring is backend-owned and deterministic
- Missing telemetry never defaults to healthy

</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-health-dashboard/02-01-SUMMARY.md`
</output>
