# Deferred Items

## 05-01

- `apps/api/src/modules/lifecycle/lifecycle.repository.ts`: pre-existing Prisma input typing errors surfaced during `npx pnpm --filter @agentsmith/api typecheck`; outside the Phase 05 backup scope.
- `apps/api/src/routes/lifecycle.ts`: pre-existing route typing errors surfaced during `npx pnpm --filter @agentsmith/api typecheck`; outside the Phase 05 backup scope.
