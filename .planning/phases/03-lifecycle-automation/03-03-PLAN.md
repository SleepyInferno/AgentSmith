---
phase: 03-lifecycle-automation
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
  - "02-03"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/lib/lifecycle.ts
  - apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx
  - apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx
  - apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
must_haves:
  truths:
    - "The operator can open a lifecycle page, see onboarding and offboarding templates, and launch a run from the UI."
    - "The lifecycle landing page shows active runs with grouped progress and unresolved work visibility."
    - "The UI explains that Phase 3 records work; it does not trigger live administrative actions."
  artifacts:
    - path: "apps/web/src/lib/lifecycle.ts"
      provides: "Lifecycle fetchers and mutation helpers"
      exports: ["getLifecycleTemplates", "getLifecycleRuns", "startLifecycleRun"]
    - path: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      provides: "Lifecycle landing page"
      contains: "Lifecycle workflows"
    - path: "apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx"
      provides: "Template launch cards"
      contains: "employee-onboarding"
    - path: "apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx"
      provides: "Active-run progress cards"
      contains: "unresolved"
  key_links:
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      via: "route registration"
      pattern: "path: \"lifecycle\""
    - from: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      to: "apps/web/src/lib/lifecycle.ts"
      via: "React Query load and launch mutation"
      pattern: "startLifecycleRun"
    - from: "apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx"
      to: "/api/lifecycle/runs"
      via: "active-run list rendering"
      pattern: "unresolved"
---

<objective>
Deliver the operator-facing lifecycle landing page that makes onboarding and offboarding usable immediately.

Purpose: Give the solo operator a queue-first lifecycle workspace where reusable templates, active runs, and unresolved work are visible within minutes of opening the app.
Output: Lifecycle fetch/mutation helpers, router wiring, template launch cards, and active-run overview UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lifecycle-automation/03-CONTEXT.md
@.planning/phases/03-lifecycle-automation/03-02-PLAN.md
@apps/web/src/router.tsx
@apps/web/src/lib/queryClient.ts
@apps/web/src/lib/assets.ts

<interfaces>
From apps/web/src/lib/assets.ts:
```ts
async function apiRequest<T>(input: string): Promise<T> { ... }
export function getDeviceInventory(params: DeviceInventoryParams = {}) { ... }
export function getDeviceDetail(deviceId: string) { ... }
```

From apps/web/src/router.tsx:
```tsx
export const router = createBrowserRouter([
  {
    path: "/",
    element: <AppShell />,
    children: [...]
  },
]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle web contracts and route entry points</name>
  <files>apps/web/src/router.tsx, apps/web/src/lib/lifecycle.ts</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/assets.ts, apps/web/src/lib/queryClient.ts, apps/api/src/routes/lifecycle.ts, .planning/phases/03-lifecycle-automation/03-CONTEXT.md</read_first>
  <action>Create `apps/web/src/lib/lifecycle.ts` using the same `credentials: "include"` fetch pattern as `assets.ts`. Export lifecycle DTO types and exact helpers `getLifecycleTemplates()`, `getLifecycleRuns()`, `getLifecycleRun(runId)`, `getLifecycleRunSummary(runId)`, `startLifecycleRun(input)`, `updateLifecycleStep(input)`, and `closeLifecycleRun(runId)`. Update `apps/web/src/router.tsx` to add `path: "lifecycle"` for `LifecycleQueuePage` and `path: "lifecycle/runs/:runId"` for the later detail page while preserving the existing shell. Adjust the primary nav copy so lifecycle sits beside the asset queue rather than replacing it, and add tracking-only copy per D-04 and D-06 such as `Records work only — no live admin actions are triggered here.`</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/lifecycle.ts` contains `getLifecycleTemplates`
    - `apps/web/src/lib/lifecycle.ts` contains `startLifecycleRun`
    - `apps/web/src/lib/lifecycle.ts` contains `updateLifecycleStep`
    - `apps/web/src/lib/lifecycle.ts` contains `closeLifecycleRun`
    - `apps/web/src/router.tsx` contains `path: "lifecycle"`
    - `apps/web/src/router.tsx` contains `path: "lifecycle/runs/:runId"`
    - `apps/web/src/router.tsx` contains `Records work only`
  </acceptance_criteria>
  <done>The web app knows how to call the lifecycle API and route operators into the new workflow area without introducing a second data-loading pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Build the lifecycle landing page with template launch and active-run cards</name>
  <files>apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx, apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx, apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx</files>
  <read_first>apps/web/src/lib/lifecycle.ts, apps/web/src/router.tsx, apps/web/src/routes/dashboard/AssetDashboardPage.tsx, .planning/phases/03-lifecycle-automation/03-RESEARCH.md</read_first>
  <action>Create `LifecycleQueuePage.tsx` with the exact heading `Lifecycle workflows`, a short tracking-only explainer, and two top sections: `Start a run` and `Active runs`. Build `LifecycleTemplateCards.tsx` so the operator can launch `employee-onboarding` or `employee-offboarding` by entering at minimum `subjectDisplayName` and `subjectEmail`, then calling `startLifecycleRun`. Build `ActiveLifecycleRuns.tsx` to show `subjectDisplayName`, `kind`, grouped progress counts, latest updated time, and unresolved follow-up counts so the operator can decide what matters next per D-12. Use React Query mutations and invalidate `["lifecycle-templates"]`, `["lifecycle-runs"]`, and the new run detail query after a successful launch.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `Lifecycle workflows`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `Start a run`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `Active runs`
    - `apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx` contains `employee-onboarding`
    - `apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx` contains `employee-offboarding`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `unresolved`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `/lifecycle/runs/`
  </acceptance_criteria>
  <done>The operator can launch onboarding or offboarding from reusable templates and immediately see active grouped runs and unresolved work from the landing page.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Lifecycle navigation is available from the existing app shell
- [ ] Launching a run invalidates and refreshes the active-run overview
- [ ] The landing page stays queue-first and clearly states the tracking-only boundary
</verification>

<success_criteria>

- The lifecycle page is a usable operator slice with template launch and active-run visibility
- Onboarding and offboarding are presented as reusable grouped workflows instead of ad hoc lists
- The operator can understand current lifecycle workload and unresolved follow-up items without opening every run

</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-automation/03-03-SUMMARY.md`
</output>
