---
phase: 04-network-visibility-lite
plan: 04
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/routes/network/NetworkMapPage.tsx
  - apps/web/src/routes/network/NetworkDetailPage.tsx
  - apps/web/src/components/network/NetworkMapCanvas.tsx
  - apps/web/src/components/network/NetworkRelationshipLegend.tsx
  - apps/web/src/routes/network/NetworkOverviewPage.tsx
  - apps/web/src/components/network/NetworkInventoryTable.tsx
  - apps/web/src/components/network/NetworkFindingsQueue.tsx
autonomous: true
requirements:
  - NET-01
  - NET-02
  - NET-03
must_haves:
  truths:
    - "The mapper makes confirmed versus inferred relationships obvious without pretending to be a real-time topology engine."
    - "Network detail views explain scope, freshness, confidence, and suggested next step in readable operator language."
    - "Queue and inventory screens hand off directly into map and detail context so the operator can move from triage to explanation without hunting."
    - "Map and detail views continue the seeded-example disclosure so sample topology is never mistaken for live network telemetry."
  artifacts:
    - path: "apps/web/src/routes/network/NetworkMapPage.tsx"
      provides: "Dedicated network mapper route"
      contains: "Site topology"
    - path: "apps/web/src/components/network/NetworkMapCanvas.tsx"
      provides: "Lightweight topology rendering with confirmed versus inferred visuals"
      contains: "Confirmed"
    - path: "apps/web/src/components/network/NetworkRelationshipLegend.tsx"
      provides: "Operator-facing explanation of map confidence styles"
      contains: "Inferred"
    - path: "apps/web/src/routes/network/NetworkDetailPage.tsx"
      provides: "Explanation-first network resource detail route"
      contains: "Suggested next step"
  key_links:
    - from: "apps/web/src/lib/network.ts"
      to: "apps/web/src/routes/network/NetworkMapPage.tsx"
      via: "map query"
      pattern: "getNetworkMap"
    - from: "apps/web/src/lib/network.ts"
      to: "apps/web/src/routes/network/NetworkDetailPage.tsx"
      via: "detail query"
      pattern: "getNetworkResourceDetail"
    - from: "apps/web/src/components/network/NetworkFindingsQueue.tsx"
      to: "apps/web/src/routes/network/NetworkDetailPage.tsx"
      via: "triage to detail handoff"
      pattern: "/network/resources/"
---

<objective>
Deliver the topology map and explanation-first detail workflow for Phase 4.

Purpose: Give the operator a lightweight mapper they can trust and a readable detail view that explains what is known, what is inferred, and what to do next.
Output: Dedicated map route, confidence legend, network detail page, and queue or inventory links into those views.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-network-visibility-lite/04-CONTEXT.md
@.planning/phases/04-network-visibility-lite/04-RESEARCH.md
@.planning/phases/04-network-visibility-lite/04-03-PLAN.md
@apps/web/src/lib/network.ts
@apps/web/src/routes/assets/DeviceDetailPage.tsx
@apps/web/src/routes/network/NetworkOverviewPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the dedicated network map route with a clear confidence legend</name>
  <files>apps/web/src/router.tsx, apps/web/src/routes/network/NetworkMapPage.tsx, apps/web/src/components/network/NetworkMapCanvas.tsx, apps/web/src/components/network/NetworkRelationshipLegend.tsx, apps/web/src/routes/network/NetworkOverviewPage.tsx</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/network.ts, apps/web/src/routes/network/NetworkOverviewPage.tsx, apps/web/src/routes/dashboard/AssetDashboardPage.tsx, .planning/phases/04-network-visibility-lite/04-RESEARCH.md</read_first>
  <action>Create `NetworkMapPage.tsx` and register route `/network/map`. Render exact sections `Site topology`, `Relationship legend`, and `Freshness watch`. When the map payload reports `dataMode === 'seeded_example'`, render the exact disclosure copy `Example network data is shown until a live source is connected`. Create `NetworkMapCanvas.tsx` using plain React plus SVG or CSS layout to group nodes into columns labeled `Sites`, `WAN links`, `LAN segments`, and `Core infrastructure`. Render `confirmed` relationships with solid visual treatment and `inferred` relationships with dashed or outlined treatment, and show per-resource badges for `operationalStatus` and `freshnessState`. Create `NetworkRelationshipLegend.tsx` that explicitly explains `Confirmed relationship` and `Inferred relationship`. Update `NetworkOverviewPage.tsx` to include a clear call to action labeled `Open network mapper` linking to `/network/map`.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/router.tsx` contains `/network/map`
    - `apps/web/src/routes/network/NetworkMapPage.tsx` contains `Site topology`
    - `apps/web/src/routes/network/NetworkMapPage.tsx` contains `Relationship legend`
    - `apps/web/src/routes/network/NetworkMapPage.tsx` contains `Freshness watch`
    - `apps/web/src/routes/network/NetworkMapPage.tsx` contains `Example network data is shown until a live source is connected`
    - `apps/web/src/components/network/NetworkMapCanvas.tsx` contains `Sites`
    - `apps/web/src/components/network/NetworkMapCanvas.tsx` contains `WAN links`
    - `apps/web/src/components/network/NetworkMapCanvas.tsx` contains `LAN segments`
    - `apps/web/src/components/network/NetworkMapCanvas.tsx` contains `Core infrastructure`
    - `apps/web/src/components/network/NetworkRelationshipLegend.tsx` contains `Confirmed relationship`
    - `apps/web/src/components/network/NetworkRelationshipLegend.tsx` contains `Inferred relationship`
    - `apps/web/src/routes/network/NetworkOverviewPage.tsx` contains `Open network mapper`
  </acceptance_criteria>
  <done>The operator can now open a dedicated network mapper that makes trust boundaries visible instead of implied.</done>
</task>

<task type="auto">
  <name>Task 2: Build the explanation-first network detail page and triage handoff links</name>
  <files>apps/web/src/routes/network/NetworkDetailPage.tsx, apps/web/src/components/network/NetworkInventoryTable.tsx, apps/web/src/components/network/NetworkFindingsQueue.tsx, apps/web/src/router.tsx</files>
  <read_first>apps/web/src/lib/network.ts, apps/web/src/routes/assets/DeviceDetailPage.tsx, apps/web/src/components/network/NetworkInventoryTable.tsx, apps/web/src/components/network/NetworkFindingsQueue.tsx, .planning/phases/04-network-visibility-lite/04-CONTEXT.md</read_first>
  <action>Create `NetworkDetailPage.tsx` and register route `/network/resources/:resourceId`. Query `getNetworkResourceDetail(resourceId)` and render exact sections `Resource scope`, `Freshness and confidence`, `Related infrastructure`, and `Open findings`. When the detail payload reports `dataMode === 'seeded_example'`, render the exact disclosure copy `Example network data is shown until a live source is connected`. Display exact fields `resourceName`, `resourceKind`, `siteName`, `operationalStatus`, `lastSeenAt`, `freshnessState`, `summary`, and `suggestedNextStep`, and list related relationships with `confirmed` or `inferred` labels. Update `NetworkFindingsQueue.tsx` and `NetworkInventoryTable.tsx` so rows link to `/network/resources/:resourceId`. Render exact error or empty copy `Unable to load the selected network resource.` and `No related infrastructure returned by the network API.` when those states occur.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/router.tsx` contains `/network/resources/:resourceId`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `Resource scope`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `Freshness and confidence`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `Related infrastructure`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `Open findings`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `Example network data is shown until a live source is connected`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `Suggested next step`
    - `apps/web/src/components/network/NetworkFindingsQueue.tsx` contains `/network/resources/`
    - `apps/web/src/components/network/NetworkInventoryTable.tsx` contains `/network/resources/`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `Unable to load the selected network resource.`
    - `apps/web/src/routes/network/NetworkDetailPage.tsx` contains `No related infrastructure returned by the network API.`
  </acceptance_criteria>
  <done>The operator can move from triage into a readable network detail view that explains scope, trust, and next action.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] The mapper distinguishes confirmed versus inferred relationships clearly
- [ ] Detail pages explain site or segment scope, freshness, confidence, and suggested next step
- [ ] Queue and inventory views link directly into the detail route
</verification>

<success_criteria>

- The operator can open a lightweight but trustworthy network mapper
- Network detail pages explain what is affected and what to review next
- Phase 4 now covers queue, inventory, map, and detail workflows end to end

</success_criteria>

<output>
After completion, create `.planning/phases/04-network-visibility-lite/04-04-SUMMARY.md`
</output>
