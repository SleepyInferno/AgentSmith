---
phase: 13-intune-device-sync
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/web/e2e/intune-sync.spec.ts
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03

must_haves:
  truths:
    - "Playwright E2E tests verify the sync button, freshness bar, compliance badge, and compliance table render correctly"
    - "Human verifies the full visual experience across inventory, detail, and connector pages"
  artifacts:
    - path: "apps/web/e2e/intune-sync.spec.ts"
      provides: "E2E test coverage for Intune sync UI surfaces"
      min_lines: 40
  key_links:
    - from: "apps/web/e2e/intune-sync.spec.ts"
      to: "ConnectorStatusPage + DeviceInventoryPage + DeviceDetailPage"
      via: "Playwright page navigation and assertions"
      pattern: "expect.*toBeVisible|getByText|getByRole"
---

<objective>
Add Playwright E2E tests for the Intune sync UI surfaces and verify the visual experience with a human checkpoint.

Purpose: Provides automated regression coverage for the sync button, freshness bar, compliance badge, and compliance table, plus human verification that the full flow looks correct.

Output: Passing Playwright tests + human sign-off on the Intune sync experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-intune-device-sync/13-CONTEXT.md
@.planning/phases/13-intune-device-sync/13-01-SUMMARY.md
@.planning/phases/13-intune-device-sync/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E tests for Intune sync UI</name>
  <files>
    apps/web/e2e/intune-sync.spec.ts
  </files>
  <read_first>
    apps/web/e2e/shell-navigation.spec.ts,
    apps/web/e2e/inventory-detail.spec.ts,
    apps/web/src/test/mockApi.ts,
    apps/web/src/routes/connectors/ConnectorStatusPage.tsx,
    apps/web/src/routes/assets/DeviceInventoryPage.tsx,
    apps/web/src/routes/assets/DeviceDetailPage.tsx,
    apps/web/src/components/assets/DeviceInventoryTable.tsx
  </read_first>
  <action>
    Create `apps/web/e2e/intune-sync.spec.ts` following the existing E2E test patterns from `shell-navigation.spec.ts` and `inventory-detail.spec.ts`.

    The mockApi already handles all needed routes (from Plan 02 updates).

    **Test cases:**

    1. **Connector status page shows Sync now button for Intune**
       - Navigate to `/connectors`
       - Expect `getByText("Sync now")` to be visible
       - Expect `getByText("Microsoft Intune")` to be visible

    2. **Sync now button triggers sync and shows loading state**
       - Navigate to `/connectors`
       - Click "Sync now" button
       - After click, the button text should change (may show "Syncing..." briefly or return to "Sync now")
       - Verify no error message appears on the card

    3. **Device inventory page shows freshness bar**
       - Navigate to `/devices`
       - Expect text matching "Last Intune sync" to be visible
       - Expect device count text (e.g., matching pattern `\d+ devices`) to be visible

    4. **Device inventory table shows Compliance column**
       - Navigate to `/devices`
       - Expect table header "Compliance" to be visible
       - Expect at least one compliance badge pill to be visible (text matching `compliant` or `noncompliant` or `unknown`)

    5. **Device detail page shows Compliance Policies section**
       - Navigate to a device detail page (e.g., `/devices/{first-device-id}` — use the mock data device ID)
       - Expect `getByText("Compliance Policies")` to be visible
       - Expect at least one table row with policy name text (e.g., "Windows 10 Baseline" from mock data)

    6. **Device detail page shows empty compliance state gracefully**
       - If a mock device has no compliance assignments, expect "No compliance policies assigned" text
       - (This may need a specific mock device — check mockApi for a device without assignments, or create a navigation to the second mock device if it has empty assignments)

    Follow existing patterns: use `test.describe`, proper `await page.goto()`, `await expect(page.getByText(...)).toBeVisible()`.
  </action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web test:e2e -- --grep "intune"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/e2e/intune-sync.spec.ts exists and has at least 4 test cases
    - apps/web/e2e/intune-sync.spec.ts contains `Sync now`
    - apps/web/e2e/intune-sync.spec.ts contains `Last Intune sync`
    - apps/web/e2e/intune-sync.spec.ts contains `Compliance Policies`
    - apps/web/e2e/intune-sync.spec.ts contains `Compliance` (table header)
    - `npx pnpm --filter @agentsmith/web test:e2e` exits 0
    - `npx pnpm test` exits 0
  </acceptance_criteria>
  <done>
    Playwright E2E tests verify all four Intune sync UI surfaces: sync button on connectors page, freshness bar on inventory page, compliance badge column in inventory table, and compliance policies table on device detail page. All tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Intune sync visual experience</name>
  <files>none</files>
  <action>
    Present the completed Phase 13 work for human review. All automated work is complete at this point — this checkpoint only confirms the visual experience.
  </action>
  <what-built>
    Complete Intune device sync integration:
    - Backend: Real Graph API provider with pagination (graphPageAll), 429 retry (withRetry), device upsert, compliance policy sync, stale row cleanup, and manual sync trigger endpoint
    - UI: Compliance badge column on device inventory table, freshness bar on inventory page showing last sync time and device count, compliance policies table on device detail page, and "Sync now" button on connector status page
    - Tests: Unit tests for Graph helpers, Intune provider, sync route, compliance API; Playwright E2E for all UI surfaces
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npx pnpm --filter @agentsmith/web dev`
    2. Navigate to `/connectors` — verify the Intune card shows a "Sync now" button
    3. Navigate to `/devices` — verify a "Last Intune sync" freshness bar appears below the page title
    4. Verify the device inventory table has a "Compliance" column with colored pill badges
    5. Click on any device to open its detail page — verify a "Compliance Policies" section appears with a table (or "No compliance policies assigned" if using mock data)
    6. Run `npx pnpm test` to confirm all tests pass
  </how-to-verify>
  <verify>Human confirms visual experience is correct</verify>
  <done>Human approved the Intune sync visual experience across connectors, inventory, and detail pages</done>
  <resume-signal>Type "approved" to mark Phase 13 complete, or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `npx pnpm --filter @agentsmith/web test:e2e` passes
- `npx pnpm test` passes (full suite)
- Human approves visual experience
</verification>

<success_criteria>
Playwright E2E tests cover all Intune sync UI surfaces and pass. Human confirms the visual experience across all three pages (connectors, inventory, device detail) meets expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/13-intune-device-sync/13-03-SUMMARY.md`
</output>
