---
phase: 07-operator-shell-refresh
task: design-review
total_tasks: 5
status: in_progress
last_updated: 2026-03-30T04:54:37.878Z
---

<current_state>
Phase 07 structural execution is complete (plans 07-01 through 07-05 done per prior session).
This session was UI design review and iterative visual polish — not a plan execution session.
The user is reviewing topbar/banner design options via HTML mockups before committing to the
actual app implementation. A decision has been reached on the banner approach (Option B), and
the new AgentSmithBanner.png asset has been added.
</current_state>

<completed_work>

- Phase 07 execution (plans 01-05): Done in prior session — shell, sidebar, PageTitle, dashboard risk cards, responsive CSS
- Hero image dark background wrapper added: `agent-dashboard__hero-image-wrap` in AssetDashboardPage.tsx + styles.css
- Navigable element visibility fix: risk card and dashboard row backgrounds made near-opaque (0.97)
- Sidebar subtitle renamed: "Agent-OS" → "S-OS" in router.tsx
- Dashboard hero label renamed: "Agent-OS" → "S-OS" in AssetDashboardPage.tsx
- New banner asset added: AgentSmithBanner.png copied to apps/web/public/mockups/
- Dashboard hero image swapped: now references AgentSmithBanner.png instead of hero-agent-smith.png
- HTML mockups created: topbar-mockup.html, topbar-mockup-b.html, topbar-mockup-c.html
- topbar-mockup-b.html updated: banner fills full topbar width with object-fit: fill, sign-out floats top-right
</completed_work>

<remaining_work>

- User has approved Option B banner concept (banner image as topbar) — apply it to the actual app:
  1. Update `ProtectedLayout.tsx` — replace existing header content with `<img src="/mockups/AgentSmithBanner.png">` + floating sign-out
  2. Update `--topbar-height` in `styles.css` from `60px` to match the banner's natural aspect ratio at full width (likely 120–160px)
  3. Update `agent-console` min-height calc to use the new topbar height
  4. Run `npx pnpm test` and verify tests still pass after topbar height change
- Review whether shell-navigation.spec.ts assertions need updating for new topbar height
- Clean up mockup HTML files from repo root (topbar-mockup*.html) once design is applied
- Confirm with user: should the banner also appear on non-home routes, or only the dashboard?
</remaining_work>

<decisions_made>

- Option B topbar selected: banner image fills the full topbar width, sign-out button floats over top-right
- Banner uses object-fit: fill to stretch across full width (user preference — no letterboxing)
- AgentSmithBanner.png is the canonical banner asset going forward (replaces hero-agent-smith.png)
- S-OS is the correct sidebar subtitle (reverted from Agent-OS which was a prior-session rename)
- Dark background wrapper added to hero image container to contain any white PNG background
- Risk card and dashboard row backgrounds bumped to near-opaque (0.97) to fix green-on-light visibility
</decisions_made>

<blockers>

- None technical. One human decision pending: whether the banner topbar applies to all routes or just the dashboard home route.
</blockers>

<context>
The session was entirely design review and incremental polish — no test-gated plan execution.
The core Phase 07 shell work was done in the prior session. This session focused on:
1. Fixing the hero image white background bleed
2. Fixing navigable green-on-white visibility issues
3. Iterating on a new topbar banner concept using the new AgentSmithBanner.png
The user reviewed HTML mockups and selected Option B (banner as topbar). The mockup now
looks correct (stretched across full width). Next step is applying Option B to the real app.
</context>

<next_action>
Apply the Option B banner topbar to ProtectedLayout.tsx and update --topbar-height in styles.css.
Start by reading ProtectedLayout.tsx and styles.css (topbar section) to plan the exact changes,
then ask user to confirm the target topbar height before writing.
</next_action>
