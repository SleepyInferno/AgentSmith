---
phase: 03-lifecycle-automation
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx
  - apps/web/src/components/lifecycle/LifecycleRunGroupList.tsx
  - apps/web/src/components/lifecycle/LifecycleStepEditor.tsx
  - apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx
autonomous: true
requirements:
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "The operator can open a lifecycle run and record per-step status, notes, and structured evidence."
    - "Skipped and blocked statuses require a visible reason field before the update is sent."
    - "Closing a run produces a readable final summary with unresolved follow-up work called out separately."
  artifacts:
    - path: "apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx"
      provides: "Grouped lifecycle run detail screen"
      contains: "Close run and review summary"
    - path: "apps/web/src/components/lifecycle/LifecycleRunGroupList.tsx"
      provides: "Grouped phase rendering"
      contains: "identity"
    - path: "apps/web/src/components/lifecycle/LifecycleStepEditor.tsx"
      provides: "Step status and evidence form"
      contains: "statusReason"
    - path: "apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx"
      provides: "Final summary and unresolved follow-up display"
      contains: "Unresolved follow-up"
  key_links:
    - from: "apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx"
      to: "apps/web/src/lib/lifecycle.ts"
      via: "detail, step update, and close-run mutations"
      pattern: "updateLifecycleStep"
    - from: "apps/web/src/components/lifecycle/LifecycleStepEditor.tsx"
      to: "/api/lifecycle/runs/:runId/steps/:stepId"
      via: "status and evidence mutation"
      pattern: "statusReason"
    - from: "apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx"
      to: "/api/lifecycle/runs/:runId/summary"
      via: "server-derived unresolved summary rendering"
      pattern: "unresolvedFollowUps"
---

<objective>
Finish the operator workflow by delivering grouped run detail, evidence capture, and close-out summary UX.

Purpose: Make lifecycle runs reviewable and auditable from start to finish, with explicit exception handling and a clear answer to what still needs manual work.
Output: Lifecycle run detail page, grouped step editor, evidence fields, and final summary panel.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lifecycle-automation/03-CONTEXT.md
@.planning/phases/03-lifecycle-automation/03-03-PLAN.md
@apps/web/src/lib/lifecycle.ts
@apps/web/src/router.tsx

<interfaces>
From apps/web/src/lib/queryClient.ts:
```ts
export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      retry: 1,
      refetchOnWindowFocus: false,
    },
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build grouped run detail and per-step evidence capture</name>
  <files>apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx, apps/web/src/components/lifecycle/LifecycleRunGroupList.tsx, apps/web/src/components/lifecycle/LifecycleStepEditor.tsx</files>
  <read_first>apps/web/src/lib/lifecycle.ts, apps/web/src/router.tsx, apps/web/src/routes/assets/DeviceDetailPage.tsx, .planning/phases/03-lifecycle-automation/03-CONTEXT.md</read_first>
  <action>Create `LifecycleRunDetailPage.tsx` to load one run and render grouped phases in order per D-01 and D-02. Create `LifecycleRunGroupList.tsx` to render groups like `identity`, `licensing`, `access`, `device`, `handoff`, and `follow-up` as distinct sections with per-group progress chips. Create `LifecycleStepEditor.tsx` so each step can be marked `automated`, `manual`, `skipped`, or `blocked` per D-07, and capture `note`, `ticketId`, `assetId`, `mailboxRef`, and `handoffRef` per D-09. Require the `statusReason` field in the UI whenever `skipped` or `blocked` is chosen per D-08, and do not add any buttons that imply live provisioning or deprovisioning per D-04 and D-06. After successful step updates, invalidate `["lifecycle-run", runId]`, `["lifecycle-runs"]`, and `["lifecycle-run-summary", runId]`.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx` contains `lifecycle-run`
    - `apps/web/src/components/lifecycle/LifecycleRunGroupList.tsx` contains `identity`
    - `apps/web/src/components/lifecycle/LifecycleRunGroupList.tsx` contains `follow-up`
    - `apps/web/src/components/lifecycle/LifecycleStepEditor.tsx` contains `automated`
    - `apps/web/src/components/lifecycle/LifecycleStepEditor.tsx` contains `manual`
    - `apps/web/src/components/lifecycle/LifecycleStepEditor.tsx` contains `skipped`
    - `apps/web/src/components/lifecycle/LifecycleStepEditor.tsx` contains `blocked`
    - `apps/web/src/components/lifecycle/LifecycleStepEditor.tsx` contains `statusReason`
    - `apps/web/src/components/lifecycle/LifecycleStepEditor.tsx` contains `ticketId`
    - `apps/web/src/components/lifecycle/LifecycleStepEditor.tsx` contains `handoffRef`
  </acceptance_criteria>
  <done>The operator can update grouped lifecycle steps with explicit evidence and exception reasons while staying inside the tracking-only workflow boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add close-run summary and unresolved follow-up UX</name>
  <files>apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx, apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx</files>
  <read_first>apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx, apps/web/src/lib/lifecycle.ts, .planning/phases/03-lifecycle-automation/03-RESEARCH.md, .planning/phases/03-lifecycle-automation/03-CONTEXT.md</read_first>
  <action>Create `LifecycleSummaryPanel.tsx` to render server-derived `completedCount`, `manualCount`, `skippedCount`, `blockedCount`, and `unresolvedFollowUps` with a separate section titled `Unresolved follow-up` per D-10, D-11, and D-12. Update `LifecycleRunDetailPage.tsx` to show a `Close run and review summary` action that calls `closeLifecycleRun(runId)` only after the operator has reviewed the grouped step state, then loads `getLifecycleRunSummary(runId)` and keeps the summary visible after closure. Ensure the unresolved list shows the exact remaining manual work context rather than burying it in full history, and preserve audit-safe copy that the operator reviewed and recorded the outcome rather than the app executing the task.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx` contains `Unresolved follow-up`
    - `apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx` contains `completedCount`
    - `apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx` contains `blockedCount`
    - `apps/web/src/components/lifecycle/LifecycleSummaryPanel.tsx` contains `unresolvedFollowUps`
    - `apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx` contains `Close run and review summary`
    - `apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx` contains `closeLifecycleRun`
    - `apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx` contains `getLifecycleRunSummary`
  </acceptance_criteria>
  <done>Closing a lifecycle run produces a readable final summary with unresolved follow-up called out as the next-action queue for the solo operator.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] The run detail page renders grouped phases and step status controls
- [ ] Skipped and blocked actions prompt for an explicit reason before save
- [ ] Closing a run loads the server-derived summary and surfaces unresolved follow-up separately
</verification>

<success_criteria>

- The operator can record step status, notes, and structured evidence for every lifecycle step
- Exception handling is explicit and reviewable in the UI
- Finished runs present a summary that makes remaining manual work obvious

</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-automation/03-04-SUMMARY.md`
</output>
