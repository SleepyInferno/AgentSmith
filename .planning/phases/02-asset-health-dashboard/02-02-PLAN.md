---
phase: 02-asset-health-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "01-02"
  - "01-03"
  - "02-01"
files_modified:
  - apps/api/src/routes/assets.ts
  - apps/api/src/server.ts
  - apps/api/src/modules/assets/asset-health.repository.ts
  - apps/api/src/modules/assets/asset-health.service.ts
  - apps/api/src/routes/assets.test.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
user_setup: []
must_haves:
  truths:
    - Asset endpoints expose stable app-owned DTOs for queue, inventory, and detail workflows.
    - Filter and sort behavior is implemented server-side so queue and inventory semantics stay consistent.
    - API payloads preserve freshness and incomplete-data visibility instead of implying false confidence.
  artifacts:
    - apps/api/src/routes/assets.ts
    - apps/api/src/server.ts
    - apps/api/src/routes/assets.test.ts
  key_links:
    - Asset routes reuse the scoring and repository contracts from Plan 01.
    - The dashboard queue endpoint and the inventory endpoint both derive from the same assessment logic.
    - Device detail responses return the exact signal explanations later rendered in the web detail page.
---

<objective>
Expose Phase 2 asset-health APIs for queue, inventory, and device detail.

Purpose: Turn the domain model from Plan 01 into frontend-ready endpoints that make ranking, filtering, and signal explanations consistent and testable.
Output: Protected asset routes, typed filter parsing, queue and inventory responses, device detail payloads, and route-level tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-asset-health-dashboard/02-RESEARCH.md
@.planning/phases/02-asset-health-dashboard/02-VALIDATION.md
@.planning/phases/02-asset-health-dashboard/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add asset API routes to the Fastify server</name>
  <files>apps/api/src/routes/assets.ts, apps/api/src/server.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/routes/health.ts, apps/api/src/modules/assets/asset-health.service.ts, apps/api/src/modules/assets/asset-health.types.ts, .planning/phases/01-foundations-and-secure-data-flow/01-02-PLAN.md</read_first>
  <action>Create `apps/api/src/routes/assets.ts` and register it from `apps/api/src/server.ts`. Add the exact routes `GET /api/assets/queue`, `GET /api/assets/devices`, and `GET /api/assets/devices/:deviceId`. If a Phase 1 auth/session pre-handler exists by execution time, apply that same guard to all three routes; do not add a second auth system. `GET /api/assets/queue` must return queue items with `deviceId`, `deviceName`, `riskScore`, `riskLevel`, `queueRank`, `summary`, `signals`, and `sourceFreshnessState`. `GET /api/assets/devices` must return inventory rows with `ownerDisplayName`, `department`, `site`, `operatingSystem`, `encryptionStatus`, `antivirusStatus`, `patchStatus`, `lastCheckInAt`, `riskScore`, and `riskLevel`. `GET /api/assets/devices/:deviceId` must return a detail payload with `signals`, `summary`, `sourceFreshnessState`, and the normalized health fields used to derive risk.</action>
  <verify>pnpm --filter @agentsmith/api build</verify>
  <acceptance_criteria>
    - apps/api/src/routes/assets.ts contains `/api/assets/queue`
    - apps/api/src/routes/assets.ts contains `/api/assets/devices`
    - apps/api/src/routes/assets.ts contains `/api/assets/devices/:deviceId`
    - apps/api/src/routes/assets.ts returns `riskScore`, `riskLevel`, `summary`, `signals`, and `sourceFreshnessState`
    - apps/api/src/server.ts contains `registerAssetRoutes` or an equivalent assets route registration
  </acceptance_criteria>
  <done>The backend exposes the exact Phase 2 routes the dashboard UI needs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement server-side filter and sort parsing for the inventory endpoint</name>
  <files>apps/api/src/routes/assets.ts, apps/api/src/modules/assets/asset-health.repository.ts, apps/api/src/modules/assets/asset-health.service.ts</files>
  <read_first>apps/api/src/routes/assets.ts, apps/api/src/modules/assets/asset-health.repository.ts, apps/api/src/modules/assets/asset-health.types.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Implement inventory query support for the exact filters `search`, `ownerId`, `department`, `site`, `riskLevel`, `riskSignal`, `encryptionStatus`, `antivirusStatus`, `patchStatus`, and `staleOnly`. Implement sorting for the exact fields `riskScore`, `lastCheckInAt`, `deviceName`, and `operatingSystem`, with `sortDirection` values `asc` and `desc`. Keep all filtering and sorting in the API layer by parsing query params, mapping them to repository filters, and returning one stable response shape instead of asking the frontend to re-rank raw records.</action>
  <verify>pnpm --filter @agentsmith/api build</verify>
  <acceptance_criteria>
    - apps/api/src/routes/assets.ts contains `search`, `ownerId`, `department`, `site`, `riskLevel`, `riskSignal`, `encryptionStatus`, `antivirusStatus`, `patchStatus`, and `staleOnly`
    - apps/api/src/routes/assets.ts contains `riskScore`, `lastCheckInAt`, `deviceName`, and `operatingSystem` as supported sort fields
    - apps/api/src/modules/assets/asset-health.repository.ts contains filter handling for `riskLevel` and `riskSignal`
    - apps/api/src/modules/assets/asset-health.service.ts or repository code preserves `sourceFreshnessState` for stale-data warnings
  </acceptance_criteria>
  <done>The inventory API can drive a real triage table without frontend-only filtering hacks.</done>
</task>

<task type="auto">
  <name>Task 3: Add route-level tests for queue ordering, filters, and device detail</name>
  <files>apps/api/src/routes/assets.test.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/routes/assets.ts, apps/api/src/modules/assets/asset-health.service.test.ts, .planning/phases/02-asset-health-dashboard/02-VALIDATION.md</read_first>
  <action>Create `apps/api/src/routes/assets.test.ts` using Fastify injection tests. Add one test that hits `GET /api/assets/queue` and verifies descending `queueRank` or descending `riskScore`, one test that hits `GET /api/assets/devices?staleOnly=true` and verifies only stale rows are returned, and one test that hits `GET /api/assets/devices/:deviceId` and verifies the payload contains `signals`, `summary`, and `sourceFreshnessState`. Seed or stub repository responses directly inside the test setup rather than calling external connectors.</action>
  <verify>pnpm --filter @agentsmith/api test</verify>
  <acceptance_criteria>
    - apps/api/src/routes/assets.test.ts contains `/api/assets/queue`
    - apps/api/src/routes/assets.test.ts contains `staleOnly=true`
    - apps/api/src/routes/assets.test.ts contains `/api/assets/devices/`
    - apps/api/src/routes/assets.test.ts contains `signals`, `summary`, and `sourceFreshnessState`
    - apps/api/src/routes/assets.test.ts exits 0 through `pnpm --filter @agentsmith/api test`
  </acceptance_criteria>
  <done>The asset APIs are verified as stable contracts before the dashboard UI is built on them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @agentsmith/api build` succeeds
- [ ] `pnpm --filter @agentsmith/api test` succeeds
- [ ] Queue ordering is covered by route tests
- [ ] Detail responses include explicit signal explanations and freshness state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Queue, inventory, and detail APIs share one scoring contract
- The API keeps stale or incomplete data visible

</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-health-dashboard/02-02-SUMMARY.md`
</output>
