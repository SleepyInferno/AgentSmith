---
phase: 04-network-visibility-lite
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/api/src/routes/network.ts
  - apps/api/src/routes/network.test.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - NET-01
  - NET-02
  - NET-03
must_haves:
  truths:
    - "The network HTTP contract exposes explicit freshness, confidence, and scope fields instead of asking the frontend to infer them."
    - "Queue, inventory, map, and detail endpoints all read from the same repository so ranking and explanation stay consistent."
    - "Network routes register through the same injected Fastify server composition used by assets and lifecycle."
    - "Every route includes an explicit `dataMode` so seeded example data can stay usable without being mistaken for live connector output."
  artifacts:
    - path: "apps/api/src/routes/network.ts"
      provides: "Route-layer HTTP contract for network findings, inventory, map, and detail views"
      contains: "/api/network/findings"
    - path: "apps/api/src/server.ts"
      provides: "Server registration and dependency injection for network routes"
      contains: "networkRoutes"
    - path: "apps/api/src/routes/network.test.ts"
      provides: "Injected route coverage for the new network API surface"
      contains: "/api/network/map"
  key_links:
    - from: "apps/api/src/modules/network/network.repository.ts"
      to: "apps/api/src/routes/network.ts"
      via: "read-only network contract mapping"
      pattern: "listFindings"
    - from: "apps/api/src/routes/network.ts"
      to: "apps/api/src/routes/network.test.ts"
      via: "HTTP contract verification"
      pattern: "/api/network/resources"
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/routes/network.ts"
      via: "injected route registration"
      pattern: "registerNetworkRoutes"
---

<objective>
Expose the Phase 4 backend read model through stable API contracts.

Purpose: Turn the network repository into queue, inventory, map, and detail endpoints that the web app can consume without learning backend internals.
Output: Fastify network routes, injected server registration, and API tests that lock the contract before UI work begins.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-network-visibility-lite/04-CONTEXT.md
@.planning/phases/04-network-visibility-lite/04-RESEARCH.md
@.planning/phases/04-network-visibility-lite/04-01-PLAN.md
@apps/api/src/server.ts
@apps/api/src/routes/assets.ts
@apps/api/src/routes/lifecycle.ts
@apps/api/src/modules/network/network.repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add network findings, inventory, map, and detail routes with explicit query parsing</name>
  <files>apps/api/src/routes/network.ts</files>
  <read_first>apps/api/src/routes/assets.ts, apps/api/src/routes/lifecycle.ts, apps/api/src/modules/network/network.repository.ts, apps/api/src/modules/network/network.types.ts, .planning/phases/04-network-visibility-lite/04-RESEARCH.md</read_first>
  <action>Create `apps/api/src/routes/network.ts` exporting `registerNetworkRoutes`. Define dependency type `NetworkRoutesDependencies` with repository methods `listFindings`, `listInventory`, `getMap`, and `getResourceDetail`, plus optional `preHandler`. Add exact endpoints `GET /api/network/findings`, `GET /api/network/resources`, `GET /api/network/map`, and `GET /api/network/resources/:resourceId`. Parse `limit` for findings, and parse inventory query params `search`, `kind`, `site`, `operationalStatus`, and `freshnessState`. Map repository results to response fields named `dataMode`, `resourceId`, `resourceName`, `resourceKind`, `siteName`, `operationalStatus`, `freshnessState`, `lastSeenAt`, `summary`, `suggestedNextStep`, `confidence`, and `relatedResources` where applicable. Return `404` with message `Network resource not found` when detail lookup misses.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const route=fs.readFileSync('apps/api/src/routes/network.ts','utf8');for (const token of ['/api/network/findings','/api/network/resources','/api/network/map','/api/network/resources/:resourceId','Network resource not found']) { if (!route.includes(token)) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/network.ts` contains `/api/network/findings`
    - `apps/api/src/routes/network.ts` contains `/api/network/resources`
    - `apps/api/src/routes/network.ts` contains `/api/network/map`
    - `apps/api/src/routes/network.ts` contains `/api/network/resources/:resourceId`
    - `apps/api/src/routes/network.ts` contains `dataMode`
    - `apps/api/src/routes/network.ts` contains `resourceId`, `resourceName`, and `resourceKind`
    - `apps/api/src/routes/network.ts` contains `freshnessState`
    - `apps/api/src/routes/network.ts` contains `confidence`
    - `apps/api/src/routes/network.ts` contains `Network resource not found`
  </acceptance_criteria>
  <done>The API now exposes stable network read endpoints with explicit trust and scope fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register network routes in the server and lock the HTTP contract with route tests</name>
  <files>apps/api/src/server.ts, apps/api/src/routes/network.test.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/routes/network.ts, apps/api/src/routes/assets.test.ts, apps/api/src/routes/lifecycle.test.ts, apps/api/src/modules/network/network.fixtures.ts</read_first>
  <behavior>
    - Test 1: `GET /api/network/findings` returns ranked findings with `resourceId`, `summary`, and `suggestedNextStep`
    - Test 2: `GET /api/network/resources` returns typed inventory rows with `resourceKind`, `siteName`, and `freshnessState`
    - Test 3: `GET /api/network/map` returns relationships with both `confirmed` and `inferred` confidence values
    - Test 4: `GET /api/network/resources/:resourceId` returns detail scope and `404` on missing resource
    - Test 5: route responses surface `dataMode` as `live` or `seeded_example`
  </behavior>
  <action>Update `apps/api/src/server.ts` so `BuildServerOptions` includes `networkRoutes?: Partial<NetworkRoutesDependencies>`, instantiate `NetworkRepository`, and register `registerNetworkRoutes` with the same injected `preHandler` pattern used by assets and lifecycle. Create `apps/api/src/routes/network.test.ts` using `buildServer` with injected repository doubles that return deterministic fixtures for findings, inventory, map, and detail responses. Assert exact response properties for `dataMode`, `resourceId`, `resourceName`, `resourceKind`, `siteName`, `freshnessState`, `summary`, `suggestedNextStep`, and `confidence`, and assert that missing detail responses return status `404` with message `Network resource not found`.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/routes/network.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/server.ts` contains `networkRoutes`
    - `apps/api/src/server.ts` contains `registerNetworkRoutes`
    - `apps/api/src/routes/network.test.ts` contains `/api/network/findings`
    - `apps/api/src/routes/network.test.ts` contains `/api/network/resources`
    - `apps/api/src/routes/network.test.ts` contains `/api/network/map`
    - `apps/api/src/routes/network.test.ts` contains `dataMode`
    - `apps/api/src/routes/network.test.ts` contains `Network resource not found`
    - `apps/api/src/routes/network.test.ts` contains `confirmed`
    - `apps/api/src/routes/network.test.ts` contains `inferred`
  </acceptance_criteria>
  <done>The server contract is locked, tested, and ready for the web module to consume.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/routes/network.test.ts` succeeds
- [ ] Server registration exposes the network routes through the same injected composition as other modules
- [ ] Every route includes explicit freshness, confidence, and scope fields where the operator needs them
</verification>

<success_criteria>

- Network queue, inventory, map, and detail APIs exist
- Route contracts are tested before UI work begins
- The frontend can consume one consistent API surface for Phase 4

</success_criteria>

<output>
After completion, create `.planning/phases/04-network-visibility-lite/04-02-SUMMARY.md`
</output>
