---
phase: 05-backup-confidence-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - apps/api/src/modules/backup/backup.types.ts
  - apps/api/src/modules/backup/backup.fixtures.ts
  - apps/api/src/modules/backup/backup.v1-scope.ts
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - BACK-03
must_haves:
  truths:
    - "Phase 5 uses the canonical `System` entity plus an explicit backup-policy baseline so missing coverage is measured against expected protection, not only against provider data."
    - "The backend distinguishes `missing`, `stale`, `excluded`, and `unknown` states so missing telemetry never reads as healthy protection."
    - "Backup confidence is server-owned and depends on both last successful backup freshness and restore-proof recency."
    - "Provider-specific source keys stay isolated in a scope file instead of leaking into UI conditionals."
    - "Seeded-example fallback remains explicit so the dashboard can be demoed before live backup connectors exist."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Canonical backup policy, provider evidence, and restore-proof persistence"
      contains: "model BackupCoveragePolicy"
    - path: "apps/api/src/modules/backup/backup.types.ts"
      provides: "Canonical backup read-model types for overview, findings, inventory, and detail flows"
      contains: "export type BackupInventoryRow"
    - path: "apps/api/src/modules/backup/backup.fixtures.ts"
      provides: "Deterministic protected-system cases for healthy, missing, stale, excluded, and unknown states"
      contains: "Coverage missing for an expected protected system"
    - path: "apps/api/src/modules/backup/backup.v1-scope.ts"
      provides: "Exact v1 provider and workload boundary for Phase 5"
      contains: "azure_backup"
  key_links:
    - from: "prisma/schema.prisma"
      to: "apps/api/src/modules/backup/backup.types.ts"
      via: "canonical backup enums and read-model contracts"
      pattern: "BackupConfidenceState"
    - from: "apps/api/src/modules/backup/backup.v1-scope.ts"
      to: "apps/api/src/modules/backup/backup.fixtures.ts"
      via: "provider keys and workload labels"
      pattern: "providerKey"
    - from: "apps/api/src/modules/backup/backup.fixtures.ts"
      to: "apps/api/src/modules/backup/backup.types.ts"
      via: "deterministic edge-case data"
      pattern: "confidenceState"
---

<objective>
Define the exact Phase 5 source boundary, coverage baseline, and canonical backup model before routes or UI depend on it.

Purpose: Lock what counts as an expected protected system, which providers are in v1, and how backup plus restore-proof evidence map to honest operator-facing states.
Output: Prisma schema additions, canonical backup types, exact v1 source scope, and deterministic fixture data that proves missing-versus-healthy semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md
@AGENTS.md
@prisma/schema.prisma
@apps/api/src/modules/assets/asset-health.types.ts
@apps/api/src/modules/network/network.types.ts
@apps/api/src/modules/network/network.fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical backup-policy, provider-evidence, and source-scope contracts</name>
  <files>prisma/schema.prisma, apps/api/src/modules/backup/backup.types.ts, apps/api/src/modules/backup/backup.v1-scope.ts</files>
  <read_first>prisma/schema.prisma, .planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md, AGENTS.md, apps/api/src/modules/assets/asset-health.types.ts, apps/api/src/modules/network/network.types.ts</read_first>
  <action>Extend `prisma/schema.prisma` with exact enums `BackupCoverageMode` values `required` and `excluded`; `BackupCoverageState` values `protected`, `missing`, `partial`, `excluded`, and `unknown`; `BackupRestoreState` values `current`, `stale`, `missing`, and `unknown`; `BackupConfidenceState` values `healthy`, `watch`, `high_risk`, and `unknown`; `BackupEvidenceSource` values `provider_sync` and `operator_attested`; and `BackupRestoreOutcome` values `success`, `partial`, and `failure`. Add model `BackupCoveragePolicy` with fields `systemId`, `coverageMode`, `backupFreshnessHours`, `restoreTestMaxAgeDays`, `gracePeriodHours`, `providerScope`, `exclusionReason`, `notes`, `createdAt`, and `updatedAt`. Add model `BackupEvidence` with fields `systemId`, `providerKey`, `workloadKind`, `sourceSystem`, `sourceId`, `coverageState`, `lastSuccessfulBackupAt`, `lastFailedBackupAt`, `backupFreshnessState`, `connectorFreshnessState`, `lastObservedAt`, and `metadata`. Add model `BackupRestoreTest` with fields `systemId`, `backupEvidenceId`, `evidenceSource`, `outcome`, `testedAt`, `recoveryPointAt`, `ticketRef`, `notes`, and `metadata`. Create `apps/api/src/modules/backup/backup.v1-scope.ts` exporting exact provider descriptors `azure_backup`, `m365_backup`, and `veeam` with workload keys `virtual_machine`, `server`, `m365_exchange`, `m365_sharepoint`, and `endpoint_image`, plus a constant note that any other provider is treated as `unsupported_provider` until a later phase expands scope. Create `apps/api/src/modules/backup/backup.types.ts` exporting `BackupDataMode`, `BackupCoverageMode`, `BackupCoverageState`, `BackupRestoreState`, `BackupConfidenceState`, `BackupOverview`, `BackupOverviewCard`, `BackupFindingItem`, `BackupInventoryRow`, `BackupInventoryFilters`, `BackupSourceHealth`, and `BackupSystemDetail` so later repository, route, and web layers all share one canonical contract.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const schema=fs.readFileSync('prisma/schema.prisma','utf8');const types=fs.readFileSync('apps/api/src/modules/backup/backup.types.ts','utf8');const scope=fs.readFileSync('apps/api/src/modules/backup/backup.v1-scope.ts','utf8');for (const token of ['enum BackupCoverageMode','enum BackupCoverageState','enum BackupRestoreState','enum BackupConfidenceState','model BackupCoveragePolicy','model BackupEvidence','model BackupRestoreTest']) { if (!schema.includes(token)) process.exit(1); } for (const token of ['BackupOverview','BackupFindingItem','BackupInventoryRow','BackupSystemDetail']) { if (!types.includes(token)) process.exit(1); } for (const token of ['azure_backup','m365_backup','veeam','unsupported_provider']) { if (!scope.includes(token)) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `enum BackupCoverageMode`
    - `prisma/schema.prisma` contains `enum BackupCoverageState`
    - `prisma/schema.prisma` contains `enum BackupRestoreState`
    - `prisma/schema.prisma` contains `enum BackupConfidenceState`
    - `prisma/schema.prisma` contains `model BackupCoveragePolicy`
    - `prisma/schema.prisma` contains `model BackupEvidence`
    - `prisma/schema.prisma` contains `model BackupRestoreTest`
    - `apps/api/src/modules/backup/backup.types.ts` contains `export type BackupInventoryRow`
    - `apps/api/src/modules/backup/backup.types.ts` contains `export type BackupSystemDetail`
    - `apps/api/src/modules/backup/backup.v1-scope.ts` contains `azure_backup`
    - `apps/api/src/modules/backup/backup.v1-scope.ts` contains `m365_backup`
    - `apps/api/src/modules/backup/backup.v1-scope.ts` contains `veeam`
  </acceptance_criteria>
  <done>The project has a canonical Phase 5 domain model, explicit provider boundary, and typed backup contracts for later repository, route, and UI work.</done>
</task>

<task type="auto">
  <name>Task 2: Create deterministic fixture data that proves coverage, restore-proof, and telemetry edge cases</name>
  <files>apps/api/src/modules/backup/backup.fixtures.ts</files>
  <read_first>apps/api/src/modules/backup/backup.types.ts, apps/api/src/modules/backup/backup.v1-scope.ts, apps/api/src/modules/network/network.fixtures.ts, .planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md</read_first>
  <action>Create `apps/api/src/modules/backup/backup.fixtures.ts` exporting `backupFixtureSystems`, `backupFixturePolicies`, `backupFixtureEvidence`, and `backupFixtureRestoreTests` with at least six deterministic systems: `sys-finance-sql`, `sys-domain-controller`, `sys-sharepoint-tenant`, `sys-helpdesk-files`, `sys-branch-nas`, and `sys-lab-jumpbox`. Include one `healthy` case, one `missing` coverage case, one `partial` coverage case, one `high_risk` case because backup freshness is stale, one `high_risk` case because restore proof is stale, one `unknown` case because provider telemetry is unavailable, and one `excluded` policy case. Use the exact summary strings `Coverage missing for an expected protected system`, `Backup is recent but restore proof is stale`, and `Backup telemetry is unavailable from the configured provider` so repository and route tests can assert them later. Keep fixture providers within the exact v1 keys `azure_backup`, `m365_backup`, and `veeam`, and include at least one `operator_attested` restore-proof record so later plans can render that evidence as read-only instead of inventing a write flow.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const fixtures=fs.readFileSync('apps/api/src/modules/backup/backup.fixtures.ts','utf8');for (const token of ['backupFixtureSystems','backupFixturePolicies','backupFixtureEvidence','backupFixtureRestoreTests','sys-finance-sql','sys-domain-controller','Coverage missing for an expected protected system','Backup is recent but restore proof is stale','Backup telemetry is unavailable from the configured provider','operator_attested']) { if (!fixtures.includes(token)) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `backupFixtureSystems`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `backupFixturePolicies`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `backupFixtureEvidence`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `backupFixtureRestoreTests`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `sys-finance-sql`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `sys-domain-controller`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `Coverage missing for an expected protected system`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `Backup is recent but restore proof is stale`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `Backup telemetry is unavailable from the configured provider`
    - `apps/api/src/modules/backup/backup.fixtures.ts` contains `operator_attested`
  </acceptance_criteria>
  <done>The phase now has deterministic protected-system and provider evidence fixtures that prove the trust distinctions Phase 5 needs before any repository or UI logic is added.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm db:generate` succeeds
- [ ] Phase 5 has an explicit v1 provider boundary instead of vague backup-source assumptions
- [ ] Missing, excluded, stale, and unknown states are represented in canonical types and fixtures before routes or UI depend on them
</verification>

<success_criteria>

- The backup module has a canonical persistence and type contract
- Expected protection baseline is explicit and measurable against `System`
- Deterministic fixtures prove the backup-confidence edge cases the rest of the phase must respect

</success_criteria>

<output>
After completion, create `.planning/phases/05-backup-confidence-dashboard/05-01-SUMMARY.md`
</output>
