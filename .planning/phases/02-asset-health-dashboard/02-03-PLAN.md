---
phase: 02-asset-health-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - apps/web/package.json
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/lib/asset-types.ts
  - apps/web/src/components/assets/AssetDashboardPage.tsx
  - apps/web/src/components/assets/AssetQueuePanel.tsx
  - apps/web/src/components/assets/AssetInventoryTable.tsx
  - apps/web/src/components/assets/AssetDetailDrawer.tsx
  - apps/web/src/components/assets/StaleDataBanner.tsx
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
must_haves:
  truths:
    - The operator can open the app and immediately see a prioritized endpoint queue plus a filterable device inventory.
    - The operator can drill into a device detail view and understand the signals driving its risk score.
    - The UI distinguishes healthy emptiness from stale or incomplete data using explicit banners and badges.
  artifacts:
    - path: apps/web/src/components/assets/AssetDashboardPage.tsx
      provides: Dashboard composition for queue, filters, and inventory
    - path: apps/web/src/components/assets/AssetQueuePanel.tsx
      provides: Needs-attention queue rendering
    - path: apps/web/src/components/assets/AssetDetailDrawer.tsx
      provides: Signal explanation view for ASST-03
    - path: apps/web/src/components/assets/StaleDataBanner.tsx
      provides: Shared stale-data warning treatment across screens
  key_links:
    - from: apps/web/src/components/assets/AssetDashboardPage.tsx
      to: /api/assets and /api/assets/queue
      via: Initial dashboard data load and filter changes
      pattern: "/api/assets|/api/assets/queue"
    - from: apps/web/src/components/assets/AssetInventoryTable.tsx
      to: apps/web/src/components/assets/AssetDetailDrawer.tsx
      via: Row selection opens device detail
      pattern: "selectedAssetId|onSelectAsset"
    - from: apps/web/src/components/assets/AssetDetailDrawer.tsx
      to: /api/assets/:assetId
      via: Detail fetch and signal rendering
      pattern: "/api/assets/"
---

<objective>
Build the Phase 2 asset-health dashboard UX on top of the new read-only asset APIs.

Purpose: Deliver the first operator-usable morning triage surface with a ranked queue, filterable inventory, understandable device detail, and obvious stale-data warnings.
Output: Web routing/data-fetch plumbing plus dashboard, queue, inventory, detail, and stale-data UI components.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/AI/AgentSmith/AGENTS.md
@F:/AI/AgentSmith/.planning/ROADMAP.md
@F:/AI/AgentSmith/.planning/REQUIREMENTS.md
@F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-VALIDATION.md
@F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-02-PLAN.md
@F:/AI/AgentSmith/apps/web/src/App.tsx

<interfaces>
Expected API payloads from 02-02:
```ts
type AssetListResponse = {
  items: AssetInventoryItem[];
  total: number;
  filtersApplied: Record<string, string | boolean | null>;
  staleDataBanner: string | null;
};

type AssetQueueResponse = {
  items: AssetQueueItem[];
  generatedAt: string;
  staleDataBanner: string | null;
};

type AssetDetailResponse = {
  asset: AssetDetail;
  signals: Array<{ key: string; status: string; severity: string; summary: string }>;
  staleDataBanner: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the Phase 1 placeholder shell with a real asset dashboard data flow</name>
  <files>apps/web/package.json, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/lib/api.ts, apps/web/src/lib/asset-types.ts</files>
  <read_first>apps/web/package.json, apps/web/src/main.tsx, apps/web/src/App.tsx, F:/AI/AgentSmith/.planning/research/STACK.md</read_first>
  <action>Add `react-router-dom` and `@tanstack/react-query` to `apps/web/package.json`. Replace the single placeholder `App.tsx` shell with a routed Phase 2 app where `/` renders `AssetDashboardPage`. Create `apps/web/src/lib/api.ts` with credentialed JSON fetch helpers for `GET /api/assets`, `GET /api/assets/queue`, and `GET /api/assets/:assetId`. Add `apps/web/src/lib/asset-types.ts` that mirrors the Phase 2 API contracts exactly so the UI never imports provider-specific fields. Keep the app read-heavy: there should be no mutation hooks, no remediation buttons, and no write actions in this plan.</action>
  <acceptance_criteria>
    - apps/web/package.json contains `react-router-dom`
    - apps/web/package.json contains `@tanstack/react-query`
    - apps/web/src/App.tsx contains `AssetDashboardPage`
    - apps/web/src/lib/api.ts contains `/api/assets`
    - apps/web/src/lib/asset-types.ts contains `AssetInventoryItem`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agentsmith/web build</automated>
  </verify>
  <done>The web app has a concrete Phase 2 data-fetch foundation and route entrypoint instead of a static placeholder screen.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dashboard queue and filterable inventory surfaces</name>
  <files>apps/web/src/components/assets/AssetDashboardPage.tsx, apps/web/src/components/assets/AssetQueuePanel.tsx, apps/web/src/components/assets/AssetInventoryTable.tsx, apps/web/src/components/assets/StaleDataBanner.tsx</files>
  <read_first>apps/web/src/App.tsx, apps/web/src/lib/api.ts, apps/web/src/lib/asset-types.ts, F:/AI/AgentSmith/AGENTS.md</read_first>
  <action>Create `AssetDashboardPage.tsx` that loads both `/api/assets/queue` and `/api/assets` on first render and lays them out as a morning-start triage screen, not a chart-heavy dashboard. Create `AssetQueuePanel.tsx` to render the top needs-attention items with `queueRank`, `name`, `riskLevel`, `riskScore`, and the first two `riskReasons`. Create `AssetInventoryTable.tsx` with filter controls for `search`, `riskLevel`, `site`, `department`, and `staleOnly`, plus sortable columns for `riskScore`, `lastCheckInAt`, `diskFreeGb`, `assetAgeDays`, and `name`. Create `StaleDataBanner.tsx` and render it whenever the API returns `staleDataBanner`, using different copy from an empty-state message so stale data cannot be mistaken for a clean environment.</action>
  <acceptance_criteria>
    - apps/web/src/components/assets/AssetDashboardPage.tsx contains `/api/assets/queue`
    - apps/web/src/components/assets/AssetDashboardPage.tsx contains `/api/assets`
    - apps/web/src/components/assets/AssetInventoryTable.tsx contains `staleOnly`
    - apps/web/src/components/assets/AssetInventoryTable.tsx contains `riskScore`, `lastCheckInAt`, and `diskFreeGb`
    - apps/web/src/components/assets/StaleDataBanner.tsx contains `staleDataBanner`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agentsmith/web build</automated>
  </verify>
  <done>The operator can immediately scan a ranked queue and browse the full inventory with practical filters that answer what matters next.</done>
</task>

<task type="auto">
  <name>Task 3: Add device detail UX that explains risk signals without hidden logic</name>
  <files>apps/web/src/components/assets/AssetDashboardPage.tsx, apps/web/src/components/assets/AssetInventoryTable.tsx, apps/web/src/components/assets/AssetDetailDrawer.tsx, apps/web/src/components/assets/StaleDataBanner.tsx</files>
  <read_first>apps/web/src/components/assets/AssetDashboardPage.tsx, apps/web/src/components/assets/AssetInventoryTable.tsx, apps/web/src/lib/api.ts, apps/web/src/lib/asset-types.ts</read_first>
  <action>Create `AssetDetailDrawer.tsx` that opens from either the queue or inventory and fetches `GET /api/assets/:assetId`. The drawer must show the device header fields `ownerName`, `department`, `site`, `operatingSystem`, `osVersion`, `lastCheckInAt`, `diskFreeGb`, and `assetAgeDays`, followed by a visible list of signal cards for `stale-check-in`, `missing-encryption`, `missing-antivirus`, `low-disk`, `unsupported-os`, `outdated-patches`, and `stale-source-data`. Reuse `StaleDataBanner.tsx` inside the detail drawer when the detail payload includes `staleDataBanner`. Wire row clicks and queue item clicks through a shared `selectedAssetId` state in `AssetDashboardPage.tsx`; do not add editing, acknowledge buttons, or remediation actions in this phase.</action>
  <acceptance_criteria>
    - apps/web/src/components/assets/AssetDetailDrawer.tsx contains `/api/assets/`
    - apps/web/src/components/assets/AssetDetailDrawer.tsx contains `stale-check-in` and `stale-source-data`
    - apps/web/src/components/assets/AssetDashboardPage.tsx contains `selectedAssetId`
    - apps/web/src/components/assets/AssetInventoryTable.tsx contains `onSelectAsset`
    - apps/web/src/components/assets/AssetDetailDrawer.tsx contains `ownerName`, `department`, and `site`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agentsmith/web build</automated>
  </verify>
  <done>The Phase 2 UI explains why a device is risky and clearly surfaces stale-data caveats without requiring hidden tooltips or guesswork.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @agentsmith/web build` passes with the dashboard, inventory, and detail UI
- The UI contains explicit stale-data banners distinct from empty states
- The queue and detail flows stay read-only with no remediation scope added
</verification>

<success_criteria>
Phase 2 delivers a usable asset-health dashboard that helps a solo IT operator prioritize endpoints quickly, inspect the reasons behind risk, and recognize stale data immediately.
</success_criteria>

<output>
After completion, create `F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-03-SUMMARY.md`
</output>
