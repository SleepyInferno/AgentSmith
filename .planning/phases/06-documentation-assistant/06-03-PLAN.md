---
phase: 06-documentation-assistant
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/lib/docs.ts
  - apps/web/src/routes/docs/DocumentationOverviewPage.tsx
  - apps/web/src/routes/docs/DocumentationSearchPage.tsx
  - apps/web/src/components/docs/DocumentationReviewQueue.tsx
  - apps/web/src/components/docs/DocumentationSearchResultsTable.tsx
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "The first documentation screen is queue-first and surfaces stale or weakly classified knowledge before generic search."
    - "The search inventory is bookmarkable, server-driven, and explains why each result matched instead of acting like a client-side table filter."
    - "Documentation remains in the existing app shell as a utility module, preserving the established console layout from earlier phases."
    - "The UI keeps content editing out of scope by repeating the exact boundary that only explicit metadata review changes are allowed later."
  artifacts:
    - path: "apps/web/src/router.tsx"
      provides: "Utility navigation and route wiring for `/docs` and `/docs/search`"
      contains: "/docs/search"
    - path: "apps/web/src/lib/docs.ts"
      provides: "Typed web fetchers plus shared React Query key helpers for docs overview, search, and detail contracts"
      contains: "docsQueryKeys"
    - path: "apps/web/src/routes/docs/DocumentationOverviewPage.tsx"
      provides: "Queue-first documentation landing page"
      contains: "Needs review"
    - path: "apps/web/src/routes/docs/DocumentationSearchPage.tsx"
      provides: "Bookmarkable documentation search inventory"
      contains: "Search inventory"
    - path: "apps/web/src/components/docs/DocumentationReviewQueue.tsx"
      provides: "Operator-facing documentation queue rows with reasons and next steps"
      contains: "suggestedNextStep"
    - path: "apps/web/src/components/docs/DocumentationSearchResultsTable.tsx"
      provides: "Dense search results table with review and relevance columns"
      contains: "matchedExcerpt"
  key_links:
    - from: "apps/web/src/lib/docs.ts"
      to: "apps/web/src/routes/docs/DocumentationOverviewPage.tsx"
      via: "overview query wiring through shared query keys"
      pattern: "docsQueryKeys.overview"
    - from: "apps/web/src/lib/docs.ts"
      to: "apps/web/src/routes/docs/DocumentationSearchPage.tsx"
      via: "URL-driven docs search query wiring through shared query keys"
      pattern: "docsQueryKeys.search"
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/routes/docs/DocumentationOverviewPage.tsx"
      via: "utility navigation registration"
      pattern: "/docs"
    - from: "apps/web/src/components/docs/DocumentationReviewQueue.tsx"
      to: "apps/web/src/routes/docs/DocumentationDetailPage.tsx"
      via: "queue-row navigation into explanation-first detail"
      pattern: "/docs/"
---

<objective>
Deliver the queue-first documentation overview and bookmarkable search inventory.

Purpose: Give the solo operator a first screen that answers what knowledge needs attention now, then a server-driven search inventory that works across operational document types.
Output: Router wiring, typed docs web fetchers, a queue-first overview route, and a filterable documentation search page.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation-assistant/06-RESEARCH.md
@.planning/phases/06-documentation-assistant/06-02-PLAN.md
@AGENTS.md
@apps/web/src/router.tsx
@apps/web/src/lib/backup.ts
@apps/web/src/routes/backup/BackupOverviewPage.tsx
@apps/web/src/routes/network/NetworkOverviewPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docs navigation, route entry points, and typed web contracts</name>
  <files>apps/web/src/router.tsx, apps/web/src/lib/docs.ts</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/backup.ts, apps/web/src/lib/network.ts, apps/web/src/routes/backup/BackupOverviewPage.tsx, apps/api/src/routes/docs.ts, .planning/phases/06-documentation-assistant/06-02-PLAN.md</read_first>
  <action>Create `apps/web/src/lib/docs.ts` with exact fetchers `getDocumentationOverview()`, `searchDocumentation(params)`, and `getDocumentationDetail(documentId)` for `/api/docs/overview`, `/api/docs/search`, and `/api/docs/:documentId`. In the same file export one shared React Query helper named `docsQueryKeys` with exact members `overview: ['docs-overview']`, `searchRoot: ['docs-search']`, `search: (params) => ['docs-search', params]`, and `detail: (documentId) => ['docs-detail', documentId]` so later plans can invalidate all documentation search queries through one explicit prefix contract instead of relying on route-state filters or string literals. Export exact types `DocumentationOverviewResponse`, `DocumentationSearchParams`, `DocumentationSearchResponse`, `DocumentationSearchRow`, and `DocumentationDetailResponse`, including fields `writeBoundary`, `reasons`, `matchedExcerpt`, `relevanceScore`, `linkedSystems`, and `history` so later routes and components share one web contract. Update `apps/web/src/router.tsx` so the existing ghost utility nav item `Documentation` becomes a real utility route at `/docs`, add `/docs/search`, keep the module in the utility navigation group instead of promoting it to primary, and anchor the exact boundary sentence `Document content stays read-only in this phase. Metadata changes go through explicit review and audit logging.` in router-owned docs copy that can be passed into the new routes.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/docs.ts` contains `/api/docs/overview`
    - `apps/web/src/lib/docs.ts` contains `/api/docs/search`
    - `apps/web/src/lib/docs.ts` contains `getDocumentationOverview`
    - `apps/web/src/lib/docs.ts` contains `searchDocumentation`
    - `apps/web/src/lib/docs.ts` contains `getDocumentationDetail`
    - `apps/web/src/lib/docs.ts` contains `docsQueryKeys`
    - `apps/web/src/lib/docs.ts` contains `docs-overview`
    - `apps/web/src/lib/docs.ts` contains `searchRoot`
    - `apps/web/src/lib/docs.ts` contains `docs-search`
    - `apps/web/src/lib/docs.ts` contains `docs-detail`
    - `apps/web/src/router.tsx` contains `Documentation`
    - `apps/web/src/router.tsx` contains `/docs`
    - `apps/web/src/router.tsx` contains `/docs/search`
    - `apps/web/src/router.tsx` contains `Document content stays read-only in this phase. Metadata changes go through explicit review and audit logging.`
  </acceptance_criteria>
  <done>The shared shell exposes Documentation as a real utility route and the web layer has typed contracts for the docs overview, search, and detail APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the queue-first docs overview and bookmarkable search inventory</name>
  <files>apps/web/src/routes/docs/DocumentationOverviewPage.tsx, apps/web/src/routes/docs/DocumentationSearchPage.tsx, apps/web/src/components/docs/DocumentationReviewQueue.tsx, apps/web/src/components/docs/DocumentationSearchResultsTable.tsx</files>
  <read_first>apps/web/src/lib/docs.ts, apps/web/src/routes/backup/BackupOverviewPage.tsx, apps/web/src/routes/network/NetworkOverviewPage.tsx, apps/web/src/routes/network/NetworkInventoryPage.tsx, .planning/phases/06-documentation-assistant/06-RESEARCH.md</read_first>
  <action>Create `DocumentationOverviewPage.tsx` with exact sections `Needs review`, `Search coverage`, `Review aging`, and `Search inventory`. Render the docs queue with `DocumentationReviewQueue.tsx`, showing exact fields `title`, `kind`, `reviewState`, `reviewDueAt`, `summary`, `reasons`, and `suggestedNextStep`. Make every queue row link forward to `/docs/:documentId` and pass route state with exact fields `from: 'docs-overview'` and `focusReason` derived from the leading queue reason so the later detail page can preserve queue-to-detail context explicitly. When any docs response returns `dataMode === 'seeded_example'`, render the exact disclosure copy `Example documentation records are shown until a live source is connected`. Create `DocumentationSearchPage.tsx` using `DocumentationSearchResultsTable.tsx` with exact query-param filters `q`, `kind`, `category`, `site`, `owner`, `systemId`, `reviewState`, and `staleOnly`, and keep them server-driven by wiring URL params directly into `searchDocumentation` rather than filtering in memory. Show exact result columns `title`, `kind`, `reviewState`, `reviewDueAt`, `owner`, `site`, `category`, `matchedExcerpt`, and `relevanceScore`, and make every search row link forward to `/docs/:documentId` with route state including exact fields `from: 'docs-search'`, `focusReason`, and `searchQuery` for later explanation-first detail flow. Render exact empty-state copy `No documentation results match the current filters` and `Try search terms from SOPs, vendors, contacts, infrastructure notes, or recovery procedures` when needed.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/docs/DocumentationOverviewPage.tsx` contains `Needs review`
    - `apps/web/src/routes/docs/DocumentationOverviewPage.tsx` contains `Search coverage`
    - `apps/web/src/routes/docs/DocumentationOverviewPage.tsx` contains `Review aging`
    - `apps/web/src/routes/docs/DocumentationOverviewPage.tsx` contains `Search inventory`
    - `apps/web/src/routes/docs/DocumentationOverviewPage.tsx` contains `Example documentation records are shown until a live source is connected`
    - `apps/web/src/components/docs/DocumentationReviewQueue.tsx` contains `suggestedNextStep`
    - `apps/web/src/components/docs/DocumentationReviewQueue.tsx` contains `/docs/`
    - `apps/web/src/components/docs/DocumentationReviewQueue.tsx` contains `docs-overview`
    - `apps/web/src/components/docs/DocumentationReviewQueue.tsx` contains `focusReason`
    - `apps/web/src/routes/docs/DocumentationSearchPage.tsx` contains `Search inventory`
    - `apps/web/src/routes/docs/DocumentationSearchPage.tsx` contains `reviewState`
    - `apps/web/src/routes/docs/DocumentationSearchPage.tsx` contains `systemId`
    - `apps/web/src/routes/docs/DocumentationSearchPage.tsx` contains `No documentation results match the current filters`
    - `apps/web/src/routes/docs/DocumentationSearchPage.tsx` contains `Try search terms from SOPs, vendors, contacts, infrastructure notes, or recovery procedures`
    - `apps/web/src/components/docs/DocumentationSearchResultsTable.tsx` contains `matchedExcerpt`
    - `apps/web/src/components/docs/DocumentationSearchResultsTable.tsx` contains `relevanceScore`
    - `apps/web/src/components/docs/DocumentationSearchResultsTable.tsx` contains `/docs/`
    - `apps/web/src/components/docs/DocumentationSearchResultsTable.tsx` contains `docs-search`
    - `apps/web/src/components/docs/DocumentationSearchResultsTable.tsx` contains `searchQuery`
  </acceptance_criteria>
  <done>The operator can start in a queue-first documentation view and move into a URL-driven search inventory without losing result explanations or trust boundaries.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Documentation remains a utility module inside the existing shell
- [ ] Search filters stay bookmarkable and server-driven
- [ ] Seeded-example disclosure and metadata-review boundary copy are visible before content looks authoritative
</verification>

<success_criteria>

- Documentation is reachable from the shared shell without changing the established module layout
- The first docs screen helps the operator decide what stale or weakly tagged knowledge matters next
- Search inventory reflects backend-owned relevance and filter semantics instead of a client-side table filter

</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-assistant/06-03-SUMMARY.md`
</output>
