---
phase: 03-lifecycle-automation
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/modules/lifecycle/lifecycle.repository.ts
  - apps/api/src/routes/lifecycle.ts
  - apps/api/src/routes/lifecycle.test.ts
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "The API can list lifecycle templates and start onboarding or offboarding runs from them."
    - "The API can return active runs and grouped run detail for the web workflow."
    - "Step updates require exception reasons and persist structured evidence fields."
    - "Closing a run produces a stable final summary and writes an audit-safe event trail."
  artifacts:
    - path: "apps/api/src/modules/lifecycle/lifecycle.repository.ts"
      provides: "Prisma-backed lifecycle persistence and audit logging"
      exports: ["LifecycleRepository"]
    - path: "apps/api/src/routes/lifecycle.ts"
      provides: "Lifecycle HTTP contract"
      exports: ["registerLifecycleRoutes"]
    - path: "apps/api/src/routes/lifecycle.test.ts"
      provides: "Fastify injection coverage for lifecycle launch, update, and summary routes"
      contains: "/api/lifecycle/runs"
    - path: "apps/api/src/server.ts"
      provides: "Lifecycle route registration with injected repository support"
      contains: "registerLifecycleRoutes"
  key_links:
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/routes/lifecycle.ts"
      via: "Fastify route registration"
      pattern: "registerLifecycleRoutes"
    - from: "apps/api/src/routes/lifecycle.ts"
      to: "apps/api/src/modules/lifecycle/lifecycle.repository.ts"
      via: "route-layer DTO mapping"
      pattern: "lifecycleRepository"
    - from: "apps/api/src/modules/lifecycle/lifecycle.repository.ts"
      to: "prisma/schema.prisma"
      via: "lifecycle run, step, and audit writes"
      pattern: "prisma\\.lifecycle"
---

<objective>
Expose the lifecycle engine through Fastify routes using the repo’s injected route pattern.

Purpose: Turn the grouped template engine into a usable API slice for web launch, active-run tracking, per-step evidence updates, and final summary retrieval.
Output: Prisma-backed lifecycle repository, Fastify lifecycle routes, server registration, and injection-tested HTTP contracts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lifecycle-automation/03-CONTEXT.md
@.planning/phases/03-lifecycle-automation/03-RESEARCH.md
@.planning/phases/03-lifecycle-automation/03-01-PLAN.md
@apps/api/src/server.ts
@apps/api/src/routes/assets.ts

<interfaces>
From apps/api/src/routes/assets.ts:
```ts
export type AssetRoutesDependencies = {
  assetHealthRepository: Pick<AssetHealthRepository, "listQueue" | "listInventory" | "getDeviceDetail">;
  preHandler?: preHandlerHookHandler | preHandlerHookHandler[];
};

export async function registerAssetRoutes(app: FastifyInstance, options: AssetRoutesOptions) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Prisma lifecycle persistence and server registration</name>
  <files>apps/api/src/modules/lifecycle/lifecycle.repository.ts, apps/api/src/server.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/modules/assets/asset-health.repository.ts, apps/api/src/modules/lifecycle/lifecycle.types.ts, apps/api/src/modules/lifecycle/lifecycle.templates.ts, apps/api/src/modules/lifecycle/lifecycle.service.ts</read_first>
  <action>Create `apps/api/src/modules/lifecycle/lifecycle.repository.ts` using one shared lifecycle engine per the phase note and D-03. Implement `listTemplates()`, `listActiveRuns()`, `startRun(input)`, `getRun(runId)`, `updateRunStep(runId, stepId, input)`, `getRunSummary(runId)`, and `closeRun(runId)`. Use `buildLifecycleRunSnapshot`, `validateLifecycleStepUpdate`, and `buildLifecycleRunSummary` from Plan 01. Persist run snapshots through Prisma, and record audit entries in `AuditEvent` with exact `action` values `lifecycle.run.started`, `lifecycle.step.updated`, and `lifecycle.run.closed` so D-06 stays reviewable even before full Phase 1 auth lands. Update `apps/api/src/server.ts` to inject `lifecycleRoutes` dependencies alongside the existing asset route injection pattern and register `registerLifecycleRoutes` without auto-start side effects.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const repo=fs.readFileSync('apps/api/src/modules/lifecycle/lifecycle.repository.ts','utf8');const server=fs.readFileSync('apps/api/src/server.ts','utf8');for (const token of ['listTemplates','listActiveRuns','startRun','updateRunStep','getRunSummary','lifecycle.run.started','lifecycle.step.updated','lifecycle.run.closed']) { if (!repo.includes(token)) process.exit(1); } if (!server.includes('registerLifecycleRoutes') || !server.includes('lifecycleRoutes')) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/lifecycle/lifecycle.repository.ts` contains `listTemplates`
    - `apps/api/src/modules/lifecycle/lifecycle.repository.ts` contains `listActiveRuns`
    - `apps/api/src/modules/lifecycle/lifecycle.repository.ts` contains `startRun`
    - `apps/api/src/modules/lifecycle/lifecycle.repository.ts` contains `updateRunStep`
    - `apps/api/src/modules/lifecycle/lifecycle.repository.ts` contains `getRunSummary`
    - `apps/api/src/modules/lifecycle/lifecycle.repository.ts` contains `lifecycle.run.started`
    - `apps/api/src/modules/lifecycle/lifecycle.repository.ts` contains `lifecycle.run.closed`
    - `apps/api/src/server.ts` contains `registerLifecycleRoutes`
  </acceptance_criteria>
  <done>The API server owns lifecycle persistence, immutable run snapshots, and audit-safe write behavior through injected repository wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish lifecycle HTTP routes for launch, step updates, and summaries</name>
  <files>apps/api/src/routes/lifecycle.ts, apps/api/src/routes/lifecycle.test.ts</files>
  <read_first>apps/api/src/routes/assets.ts, apps/api/src/routes/assets.test.ts, apps/api/src/modules/lifecycle/lifecycle.repository.ts, apps/api/src/modules/lifecycle/lifecycle.types.ts, .planning/phases/03-lifecycle-automation/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `GET /api/lifecycle/templates` returns onboarding and offboarding templates with grouped phase metadata for LIFE-01 and LIFE-02
    - Test 2: `POST /api/lifecycle/runs` launches a run from `employee-onboarding` or `employee-offboarding` and returns grouped run detail
    - Test 3: `PATCH /api/lifecycle/runs/:runId/steps/:stepId` rejects `skipped` and `blocked` without `statusReason`, but accepts note and structured evidence fields for LIFE-03
    - Test 4: `POST /api/lifecycle/runs/:runId/close` and `GET /api/lifecycle/runs/:runId/summary` return deterministic counts and unresolved follow-up items for LIFE-04
  </behavior>
  <action>Create `apps/api/src/routes/lifecycle.test.ts` first, then add `apps/api/src/routes/lifecycle.ts`. Register exact endpoints `GET /api/lifecycle/templates`, `GET /api/lifecycle/runs`, `POST /api/lifecycle/runs`, `GET /api/lifecycle/runs/:runId`, `PATCH /api/lifecycle/runs/:runId/steps/:stepId`, `GET /api/lifecycle/runs/:runId/summary`, and `POST /api/lifecycle/runs/:runId/close`. Keep Prisma and domain shapes internal per the existing route-layer DTO rule; map HTTP fields like `templateKey`, `subjectDisplayName`, `groups`, `steps`, `status`, `statusReason`, `note`, `ticketId`, `assetId`, `mailboxRef`, `handoffRef`, `unresolvedFollowUps`, and `completedCount` inside the route file. Use injected repositories in tests through `buildServer({... lifecycleRoutes: { lifecycleRepository } })` exactly like Phase 2 asset route tests.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/routes/lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/lifecycle.ts` contains `/api/lifecycle/templates`
    - `apps/api/src/routes/lifecycle.ts` contains `/api/lifecycle/runs`
    - `apps/api/src/routes/lifecycle.ts` contains `/api/lifecycle/runs/:runId/steps/:stepId`
    - `apps/api/src/routes/lifecycle.ts` contains `/api/lifecycle/runs/:runId/summary`
    - `apps/api/src/routes/lifecycle.ts` contains `/api/lifecycle/runs/:runId/close`
    - `apps/api/src/routes/lifecycle.test.ts` contains `statusReason`
    - `apps/api/src/routes/lifecycle.test.ts` contains `unresolvedFollowUps`
  </acceptance_criteria>
  <done>The lifecycle API now exposes a usable, test-backed contract for launch, active-run tracking, per-step evidence capture, and final summaries.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/routes/lifecycle.test.ts` succeeds
- [ ] Lifecycle routes follow the repo’s injected Fastify registration pattern
- [ ] Write endpoints remain tracking-only and never imply live administrative execution
- [ ] Audit events are recorded for run launch, step updates, and run closure
</verification>

<success_criteria>

- The API can start onboarding and offboarding runs from reusable templates
- The API can return grouped run detail and active-run lists for the operator UI
- Step updates persist required exception reasons plus structured evidence fields
- Final summaries and unresolved follow-up items are server-derived and reachable by HTTP

</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-automation/03-02-SUMMARY.md`
</output>
