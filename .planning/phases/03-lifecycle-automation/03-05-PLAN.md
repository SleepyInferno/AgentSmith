---
phase: 03-lifecycle-automation
plan: 05
type: execute
wave: 5
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx
  - apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx
autonomous: true
gap_closure: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "Launching an onboarding or offboarding run from the lifecycle queue immediately opens that run's detail page."
    - "Every active lifecycle run card exposes a visible queue-side CTA into `/lifecycle/runs/:runId`."
    - "The queue-first lifecycle flow reaches the existing step editor and summary route without manual URL entry."
  artifacts:
    - path: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      provides: "Queue-side launch handoff into the run detail route"
      contains: "navigate(`/lifecycle/runs/${run.runId}`)"
    - path: "apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx"
      provides: "Visible run-detail CTA on active lifecycle cards"
      contains: "Open run details"
  key_links:
    - from: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      to: "apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx"
      via: "post-launch programmatic navigation"
      pattern: "navigate\\(`/lifecycle/runs/\\$\\{run.runId\\}`\\)"
    - from: "apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx"
      to: "apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx"
      via: "visible CTA link on each active-run card"
      pattern: "Open run details"
---

<objective>
Close the remaining queue-to-detail gap in Phase 03 by wiring lifecycle launch and active-run cards into the existing run detail route.

Purpose: Complete the guided lifecycle workflow so operators move from queue triage into recorded step execution and close-out without manual page bridging.
Output: A focused queue-side navigation fix in the lifecycle launch flow and active-run cards.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lifecycle-automation/03-CONTEXT.md
@.planning/phases/03-lifecycle-automation/03-VERIFICATION.md
@.planning/phases/03-lifecycle-automation/03-03-SUMMARY.md
@.planning/phases/03-lifecycle-automation/03-04-SUMMARY.md
@apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx
@apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx
@apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx
@apps/web/src/router.tsx

<interfaces>
From apps/web/src/router.tsx:
```tsx
{ path: "lifecycle", element: <LifecycleQueuePage /> },
{ path: "lifecycle/runs/:runId", element: <LifecycleRunDetailPage /> },
```

From apps/web/src/lib/lifecycle.ts:
```ts
export type LifecycleRunListItem = {
  runId: string;
  kind: string;
  subjectDisplayName: string;
  subjectEmail: string | null;
  updatedAt: string;
};

export function startLifecycleRun(input: StartLifecycleRunInput) {
  return apiRequest<LifecycleRunDetail>("/api/lifecycle/runs", {
    method: "POST",
    body: JSON.stringify(input),
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigate directly into run detail after lifecycle launch</name>
  <files>apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx</files>
  <read_first>apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx, apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx, apps/web/src/router.tsx, apps/web/src/lib/lifecycle.ts, .planning/phases/03-lifecycle-automation/03-CONTEXT.md, .planning/phases/03-lifecycle-automation/03-VERIFICATION.md</read_first>
  <action>Update `LifecycleQueuePage.tsx` so the queue uses React Router navigation after a successful launch instead of stopping at query invalidation. Import `useNavigate`, create `const navigate = useNavigate()`, and inside the existing launch success flow keep the current invalidations and then call the exact handoff `navigate(`/lifecycle/runs/${run.runId}`)` so the operator lands in the existing detail route immediately. Replace the stale active-runs helper copy `before opening detail pages in the next wave` with wording that reflects the shipped route, and preserve the tracking-only boundary per D-04 and D-06 by keeping the exact sentence `Records work only - no live admin actions are triggered here.`</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `useNavigate`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `const navigate = useNavigate()`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `navigate(`/lifecycle/runs/${run.runId}`)`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` does not contain `before opening detail pages in the next wave`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `Records work only - no live admin actions are triggered here.`
  </acceptance_criteria>
  <done>The queue launch flow now hands operators straight into the guided run record, closing the missing path that blocked LIFE-02, LIFE-03, and LIFE-04 from the shipped UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add a visible queue-side CTA from active-run cards into run detail</name>
  <files>apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx</files>
  <read_first>apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx, apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx, apps/web/src/router.tsx, apps/web/src/lib/lifecycle.ts, .planning/phases/03-lifecycle-automation/03-CONTEXT.md, .planning/phases/03-lifecycle-automation/03-VERIFICATION.md</read_first>
  <action>Update `ActiveLifecycleRuns.tsx` to render a visible CTA on every run card that routes into the existing detail page. Import `Link` from `react-router-dom` and render a footer action with the exact text `Open run details` and the exact destination `to={`/lifecycle/runs/${run.runId}`}`. Keep the card review-first per D-06 by placing the CTA beside the unresolved-follow-up section or progress summary rather than styling it as a destructive action, and do not change the existing server-derived summary queries or unresolved-count presentation.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `import { Link } from "react-router-dom"`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `Open run details`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `to={`/lifecycle/runs/${run.runId}`}`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `unresolvedFollowUps`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `updatedAt`
  </acceptance_criteria>
  <done>Operators can reopen any active onboarding or offboarding run from the queue without manual URL edits, completing the queue-first handoff into the existing detail, evidence, and summary screens.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Launching a lifecycle run from the queue navigates to `/lifecycle/runs/:runId`
- [ ] Every active lifecycle card renders a visible `Open run details` CTA
- [ ] The lifecycle queue no longer contains stale copy that implies detail routing is unfinished
</verification>

<success_criteria>
The lifecycle queue can launch and reopen runs through the existing detail route, so the guided queue flow now reaches step editing and final summary review without manual navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-automation/03-05-SUMMARY.md`
</output>
