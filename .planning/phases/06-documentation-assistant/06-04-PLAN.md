---
phase: 06-documentation-assistant
plan: 04
type: execute
wave: 4
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/lib/docs.ts
  - apps/web/src/routes/docs/DocumentationDetailPage.tsx
  - apps/web/src/components/docs/DocumentHistoryTimeline.tsx
  - apps/web/src/components/docs/DocumentMetadataSummaryCard.tsx
  - apps/web/src/components/docs/DocumentLinkedSystemsCard.tsx
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
must_haves:
  truths:
    - "Document detail is explanation-first: the operator sees why a document surfaced, how old the review is, and what systems or tags are connected before any edit affordance appears."
    - "History and review dates are visible as a readable timeline instead of hidden in raw timestamps."
    - "Queue and search pages hand off directly into document detail with route state that preserves the surfaced reason and origin."
    - "Content remains read-only while detail pages prepare the operator for the later explicit metadata review flow."
  artifacts:
    - path: "apps/web/src/router.tsx"
      provides: "Detail route registration for `/docs/:documentId`"
      contains: "/docs/:documentId"
    - path: "apps/web/src/lib/docs.ts"
      provides: "Client-side detail adapter plus shared query-key usage for review age, history highlights, and metadata catalog parsing"
      contains: "docsQueryKeys.detail"
    - path: "apps/web/src/routes/docs/DocumentationDetailPage.tsx"
      provides: "Explanation-first documentation detail workflow"
      contains: "Why this surfaced"
    - path: "apps/web/src/components/docs/DocumentHistoryTimeline.tsx"
      provides: "Readable document history timeline"
      contains: "Review history"
    - path: "apps/web/src/components/docs/DocumentMetadataSummaryCard.tsx"
      provides: "Compact metadata and review-age summary"
      contains: "Next review window"
    - path: "apps/web/src/components/docs/DocumentLinkedSystemsCard.tsx"
      provides: "Linked-system context card for docs detail"
      contains: "Linked systems"
  key_links:
    - from: "apps/web/src/lib/docs.ts"
      to: "apps/web/src/routes/docs/DocumentationDetailPage.tsx"
      via: "detail query and client-side review-age adapter through shared query keys"
      pattern: "docsQueryKeys.detail"
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/routes/docs/DocumentationDetailPage.tsx"
      via: "detail route registration"
      pattern: "/docs/:documentId"
    - from: "apps/web/src/routes/docs/DocumentationDetailPage.tsx"
      to: "apps/web/src/components/docs/DocumentHistoryTimeline.tsx"
      via: "history rendering"
      pattern: "history"
---

<objective>
Deliver the explanation-first document detail and review-history workflow.

Purpose: Let the operator move from queue or search into one document and immediately understand why it matched, which systems it affects, and how stale its knowledge is.
Output: Detail route wiring, a detail adapter, and reusable components for history, metadata, and linked-system context.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation-assistant/06-02-PLAN.md
@.planning/phases/06-documentation-assistant/06-03-PLAN.md
@AGENTS.md
@apps/web/src/router.tsx
@apps/web/src/lib/docs.ts
@apps/web/src/routes/backup/BackupDetailPage.tsx
@apps/web/src/routes/docs/DocumentationSearchPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the docs detail route and client-side detail adapter</name>
  <files>apps/web/src/router.tsx, apps/web/src/lib/docs.ts</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/docs.ts, apps/web/src/lib/backup.ts, apps/web/src/routes/backup/BackupDetailPage.tsx, apps/api/src/routes/docs.ts</read_first>
  <action>Update `apps/web/src/lib/docs.ts` so `getDocumentationDetail(documentId)` adapts the raw detail payload into exact derived fields `reviewAgeLabel`, `nextReviewStatus`, `historyHighlights`, and `linkedSystemSummary` without widening the backend contract, and wire the detail query through the shared helper `docsQueryKeys.detail(documentId)` from Plan 03 instead of a fresh string literal. Register the exact route `/docs/:documentId` in `apps/web/src/router.tsx`, keeping the docs module inside the established app shell and preserving direct links from both the overview queue and search inventory. Keep the exact boundary copy `Document content stays read-only in this phase. Metadata changes go through explicit review and audit logging.` available on the detail route, and do not add any content-editing or freeform authoring actions.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/router.tsx` contains `/docs/:documentId`
    - `apps/web/src/lib/docs.ts` contains `getDocumentationDetail`
    - `apps/web/src/lib/docs.ts` contains `docsQueryKeys.detail`
    - `apps/web/src/lib/docs.ts` contains `reviewAgeLabel`
    - `apps/web/src/lib/docs.ts` contains `nextReviewStatus`
    - `apps/web/src/lib/docs.ts` contains `historyHighlights`
    - `apps/web/src/router.tsx` contains `Document content stays read-only in this phase. Metadata changes go through explicit review and audit logging.`
  </acceptance_criteria>
  <done>The router and web detail contract are ready for a real document detail page without widening the Phase 06 scope into content editing.</done>
</task>

<task type="auto">
  <name>Task 2: Build the explanation-first document detail page with history and linked-system context</name>
  <files>apps/web/src/routes/docs/DocumentationDetailPage.tsx, apps/web/src/components/docs/DocumentHistoryTimeline.tsx, apps/web/src/components/docs/DocumentMetadataSummaryCard.tsx, apps/web/src/components/docs/DocumentLinkedSystemsCard.tsx</files>
  <read_first>apps/web/src/lib/docs.ts, apps/web/src/routes/backup/BackupDetailPage.tsx, apps/web/src/routes/docs/DocumentationOverviewPage.tsx, apps/web/src/routes/docs/DocumentationSearchPage.tsx, .planning/phases/06-documentation-assistant/06-RESEARCH.md</read_first>
  <action>Create `DocumentationDetailPage.tsx` with exact sections `Why this surfaced`, `Operational context`, `Linked systems`, `Review history`, and `Next review window`. Show exact fields `title`, `kind`, `summary`, `contentText`, `reasons`, `reviewState`, `reviewDueAt`, `lastReviewedAt`, `sourceUpdatedAt`, `metadataTags`, `linkedSystems`, `history`, and `suggestedNextStep`. When location state includes `from`, `focusReason`, or `searchQuery`, render a visible handoff note so operators can see whether they arrived from the queue or search and which reason surfaced the document. Use `DocumentMetadataSummaryCard.tsx` to render owner, site, category, and next-review context; `DocumentLinkedSystemsCard.tsx` to render linked systems with their relationship labels; and `DocumentHistoryTimeline.tsx` to render a chronological timeline of `source_sync`, `metadata_review`, and `review_completed` events. Add the exact explanatory sentence `This page explains document relevance and staleness before any metadata review is started.` and keep top-level navigation actions limited to `Back to docs overview` and `Open search inventory` so content editing stays out of scope. Preserve seeded-example disclosure with the exact copy `Example documentation records are shown until a live source is connected` when detail data is seeded.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `Why this surfaced`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `Operational context`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `Linked systems`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `Review history`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `Next review window`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `focusReason`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `docs-overview`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `docs-search`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `This page explains document relevance and staleness before any metadata review is started.`
    - `apps/web/src/routes/docs/DocumentationDetailPage.tsx` contains `Example documentation records are shown until a live source is connected`
    - `apps/web/src/components/docs/DocumentHistoryTimeline.tsx` contains `Review history`
    - `apps/web/src/components/docs/DocumentMetadataSummaryCard.tsx` contains `Next review window`
    - `apps/web/src/components/docs/DocumentLinkedSystemsCard.tsx` contains `Linked systems`
  </acceptance_criteria>
  <done>The docs module now has an explanation-first detail workflow that makes review age and history visible before the operator decides to change metadata.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Queue and search results hand off directly into a usable detail route
- [ ] History and review dates are visible enough for the operator to spot stale documentation quickly
- [ ] No detail page control implies full content authoring or silent metadata writes
</verification>

<success_criteria>

- Documentation detail explains why a record matters, not just what it contains
- Review history and next-review context are visible on one route
- The module preserves explanation-first UX while staying inside the metadata-review-only boundary

</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-assistant/06-04-SUMMARY.md`
</output>
