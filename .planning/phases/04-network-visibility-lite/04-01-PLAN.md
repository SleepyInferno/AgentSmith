---
phase: 04-network-visibility-lite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - apps/api/src/modules/network/network.types.ts
  - apps/api/src/modules/network/network.fixtures.ts
  - apps/api/src/modules/network/network.findings.ts
  - apps/api/src/modules/network/network.repository.ts
  - apps/api/src/modules/network/network.repository.test.ts
  - apps/api/src/modules/network/network.findings.test.ts
autonomous: true
requirements:
  - NET-01
  - NET-02
  - NET-03
must_haves:
  truths:
    - "Phase 4 persists read-only network resources, relationships, and findings as canonical internal models instead of leaking connector-specific payloads."
    - "Confirmed versus inferred relationships are explicit in the backend model and available for later API and UI contracts."
    - "Finding severity, queue rank, scope, and suggested next step are derived on the server rather than invented by the web layer."
    - "Freshness and last-seen context are stored with network records so the operator can tell whether topology data is trustworthy."
    - "The module can return an explicit `seeded_example` data mode when live network rows are not available yet, so Phase 4 stays usable without overclaiming source authority."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Network resource, relationship, and finding persistence with explicit confidence and freshness fields"
      contains: "model NetworkResource"
    - path: "apps/api/src/modules/network/network.types.ts"
      provides: "Canonical network read-model types for inventory, map, finding, and detail flows, including data-mode honesty flags"
      contains: "export type NetworkInventoryRow"
    - path: "apps/api/src/modules/network/network.repository.ts"
      provides: "Read-only inventory, map, finding, and detail repository methods"
      exports: ["NetworkRepository"]
    - path: "apps/api/src/modules/network/network.findings.ts"
      provides: "Server-derived queue ranking and suggestion helpers"
      exports: ["buildNetworkFindingQueue", "buildNetworkScopeSummary"]
    - path: "apps/api/src/modules/network/network.repository.test.ts"
      provides: "Repository coverage for inventory and map behavior"
      contains: "confirmed"
    - path: "apps/api/src/modules/network/network.findings.test.ts"
      provides: "Finding coverage for topology gaps and next-step messaging"
      contains: "Review site mapping"
  key_links:
    - from: "prisma/schema.prisma"
      to: "apps/api/src/modules/network/network.repository.ts"
      via: "canonical network tables"
      pattern: "NetworkResource"
    - from: "apps/api/src/modules/network/network.fixtures.ts"
      to: "apps/api/src/modules/network/network.repository.test.ts"
      via: "deterministic test data"
      pattern: "networkFixtureResources"
    - from: "apps/api/src/modules/network/network.findings.ts"
      to: "apps/api/src/modules/network/network.findings.test.ts"
      via: "server-derived queue and suggestion logic"
      pattern: "buildNetworkFindingQueue"
---

<objective>
Create the canonical Phase 4 backend read model before routes or UI depend on it.

Purpose: Lock the typed network inventory, relationship confidence semantics, and finding logic that the rest of the phase will expose.
Output: Prisma schema additions, canonical network types, deterministic fixture data, and tested repository plus finding helpers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-network-visibility-lite/04-CONTEXT.md
@.planning/phases/04-network-visibility-lite/04-RESEARCH.md
@prisma/schema.prisma
@apps/api/src/modules/assets/asset-health.repository.ts
@apps/api/src/modules/assets/asset-health.types.ts
@apps/api/src/modules/lifecycle/lifecycle.repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add network persistence models, canonical types, and deterministic fixture data</name>
  <files>prisma/schema.prisma, apps/api/src/modules/network/network.types.ts, apps/api/src/modules/network/network.fixtures.ts</files>
  <read_first>prisma/schema.prisma, .planning/phases/04-network-visibility-lite/04-CONTEXT.md, .planning/phases/04-network-visibility-lite/04-RESEARCH.md, apps/api/src/modules/assets/asset-health.types.ts, apps/api/src/modules/lifecycle/lifecycle.types.ts</read_first>
  <action>Extend `prisma/schema.prisma` with exact enums `NetworkResourceKind` values `site`, `wan_link`, `lan_segment`, `firewall`, `switch`, `access_point`, `dhcp_service`, and `vpn_service`; `NetworkRelationConfidence` values `confirmed` and `inferred`; and `NetworkFindingKind` values `offline_infrastructure`, `stale_telemetry`, `topology_gap`, and `unclear_ownership`. Add model `NetworkResource` with fields `sourceSystem`, `sourceId`, `kind`, `name`, `siteName`, `operationalStatus`, `freshnessState`, `lastSeenAt`, `managementIp`, `cidr`, `ownerLabel`, and `metadata`. Add model `NetworkRelationship` with fields `fromResourceId`, `toResourceId`, `relationship`, `confidence`, `sourceSystem`, `lastSeenAt`, and `metadata`. Add model `NetworkFinding` with fields `resourceId`, `kind`, `severity`, `summary`, `suggestedNextStep`, `queueRank`, `siteName`, `scopeLabel`, `freshnessState`, and `lastSeenAt`. Create `apps/api/src/modules/network/network.types.ts` exporting `NetworkInventoryRow`, `NetworkFindingItem`, `NetworkMapResponse`, `NetworkResourceDetail`, and `NetworkInventoryFilters`, and include a root or row-level `dataMode` union with exact values `live` and `seeded_example` wherever later routes need to tell the UI whether example data is being shown. Create `apps/api/src/modules/network/network.fixtures.ts` exporting `networkFixtureResources`, `networkFixtureRelationships`, and `networkFixtureFindings` with at least two sites, one WAN link, two LAN segments, one firewall, one switch, one access point, one DHCP service, and one VPN service, including both `confirmed` and `inferred` relationships and at least one `offline_infrastructure`, one `stale_telemetry`, and one `topology_gap` finding.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const schema=fs.readFileSync('prisma/schema.prisma','utf8');const types=fs.readFileSync('apps/api/src/modules/network/network.types.ts','utf8');const fixtures=fs.readFileSync('apps/api/src/modules/network/network.fixtures.ts','utf8');for (const token of ['enum NetworkResourceKind','enum NetworkRelationConfidence','enum NetworkFindingKind','model NetworkResource','model NetworkRelationship','model NetworkFinding']) { if (!schema.includes(token)) process.exit(1); } for (const token of ['NetworkInventoryRow','NetworkFindingItem','NetworkMapResponse','NetworkResourceDetail']) { if (!types.includes(token)) process.exit(1); } for (const token of ['networkFixtureResources','networkFixtureRelationships','networkFixtureFindings','confirmed','inferred','topology_gap']) { if (!fixtures.includes(token)) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `enum NetworkResourceKind`
    - `prisma/schema.prisma` contains `enum NetworkRelationConfidence`
    - `prisma/schema.prisma` contains `enum NetworkFindingKind`
    - `prisma/schema.prisma` contains `model NetworkResource`
    - `prisma/schema.prisma` contains `model NetworkRelationship`
    - `prisma/schema.prisma` contains `model NetworkFinding`
    - `apps/api/src/modules/network/network.types.ts` contains `export type NetworkInventoryRow`
    - `apps/api/src/modules/network/network.types.ts` contains `export type NetworkMapResponse`
    - `apps/api/src/modules/network/network.types.ts` contains `dataMode`
    - `apps/api/src/modules/network/network.fixtures.ts` contains `networkFixtureResources`
    - `apps/api/src/modules/network/network.fixtures.ts` contains `confirmed`
    - `apps/api/src/modules/network/network.fixtures.ts` contains `inferred`
    - `apps/api/src/modules/network/network.fixtures.ts` contains `topology_gap`
  </acceptance_criteria>
  <done>The phase has a canonical network read model with explicit freshness, confidence, and finding concepts for later routes and UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement repository and finding helpers for inventory, map, queue, and detail flows</name>
  <files>apps/api/src/modules/network/network.findings.ts, apps/api/src/modules/network/network.repository.ts, apps/api/src/modules/network/network.repository.test.ts, apps/api/src/modules/network/network.findings.test.ts</files>
  <read_first>apps/api/src/modules/network/network.types.ts, apps/api/src/modules/network/network.fixtures.ts, apps/api/src/modules/assets/asset-health.repository.ts, apps/api/src/modules/lifecycle/lifecycle.repository.ts, .planning/phases/04-network-visibility-lite/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `listInventory` returns typed resources filtered by `kind`, `siteName`, and `freshnessState`
    - Test 2: `getMap` returns both `confirmed` and `inferred` relationships plus grouped site scope
    - Test 3: `buildNetworkFindingQueue` ranks offline or stale infrastructure ahead of topology gaps with preserved `queueRank`
    - Test 4: `getResourceDetail` returns related resources, freshness, confidence context, and suggested next-step text
    - Test 5: repository responses fall back to `seeded_example` data mode when no live network rows exist yet
  </behavior>
  <action>Create `apps/api/src/modules/network/network.findings.ts` exporting `buildNetworkFindingQueue(resources, relationships, findings)` and `buildNetworkScopeSummary(resource, relatedResources)`. Create `apps/api/src/modules/network/network.repository.ts` exporting class `NetworkRepository` with methods `listFindings(limit)`, `listInventory(filters)`, `getMap()`, and `getResourceDetail(resourceId)`. Read from Prisma tables `NetworkResource`, `NetworkRelationship`, and `NetworkFinding`, but when the network tables are empty, return the deterministic fixture dataset from `network.fixtures.ts` and tag the returned payloads with `dataMode: 'seeded_example'`; otherwise tag payloads with `dataMode: 'live'`. Order findings by `queueRank` then severity, and ensure returned rows include exact fields `resourceId`, `resourceName`, `resourceKind`, `siteName`, `operationalStatus`, `freshnessState`, `lastSeenAt`, `summary`, `suggestedNextStep`, and `dataMode`. Create `apps/api/src/modules/network/network.repository.test.ts` covering inventory filtering, live-data reads, and fixture fallback responses with both `confirmed` and `inferred` relationships. Create `apps/api/src/modules/network/network.findings.test.ts` covering queue ranking, topology-gap explanations, and suggested-next-step text such as `Review site mapping` and `Confirm uplink relationship`.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/modules/network/network.repository.test.ts apps/api/src/modules/network/network.findings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/network/network.findings.ts` contains `buildNetworkFindingQueue`
    - `apps/api/src/modules/network/network.findings.ts` contains `buildNetworkScopeSummary`
    - `apps/api/src/modules/network/network.repository.ts` contains `class NetworkRepository`
    - `apps/api/src/modules/network/network.repository.ts` contains `listFindings`
    - `apps/api/src/modules/network/network.repository.ts` contains `listInventory`
    - `apps/api/src/modules/network/network.repository.ts` contains `getMap`
    - `apps/api/src/modules/network/network.repository.ts` contains `getResourceDetail`
    - `apps/api/src/modules/network/network.repository.ts` contains `seeded_example`
    - `apps/api/src/modules/network/network.repository.test.ts` contains `confirmed`
    - `apps/api/src/modules/network/network.repository.test.ts` contains `inferred`
    - `apps/api/src/modules/network/network.findings.test.ts` contains `Review site mapping`
    - `apps/api/src/modules/network/network.findings.test.ts` contains `Confirm uplink relationship`
  </acceptance_criteria>
  <done>The backend can now serve one server-owned definition of network inventory, map relationships, and operator findings.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/modules/network/network.repository.test.ts apps/api/src/modules/network/network.findings.test.ts` succeeds
- [ ] Network inventory, map, and findings are represented by canonical backend types rather than connector payloads
- [ ] Confirmed versus inferred relationship semantics are available for later routes and UI
</verification>

<success_criteria>

- Network resources, relationships, and findings exist as canonical models
- Repository methods provide read-only inventory, map, finding, and detail data
- Finding logic derives summary and suggested next step on the server
- The rest of the phase can build on a trustworthy backend read model

</success_criteria>

<output>
After completion, create `.planning/phases/04-network-visibility-lite/04-01-SUMMARY.md`
</output>
