---
phase: 06-documentation-assistant
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - prisma/migrations/20260328_0602_documentation_search/migration.sql
  - apps/api/src/modules/docs/docs.search.ts
  - apps/api/src/modules/docs/docs.repository.ts
  - apps/api/src/modules/docs/docs.repository.test.ts
  - apps/api/src/routes/docs.ts
  - apps/api/src/routes/docs.test.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
must_haves:
  truths:
    - "The API owns documentation relevance, queue ordering, and seeded-example fallback instead of leaving search quality to React filters."
    - "Overview, search, and detail responses expose why a document surfaced, how stale it is, and which systems or tags are linked."
    - "Search works across content, metadata, and linked systems, not just exact document titles."
    - "The HTTP contract keeps document content read-only by exposing the exact write boundary `metadata_review_only` until the explicit review flow lands later."
  artifacts:
    - path: "prisma/migrations/20260328_0602_documentation_search/migration.sql"
      provides: "Custom SQL search index for weighted document search"
      contains: "document_search_vector_idx"
    - path: "apps/api/src/modules/docs/docs.repository.ts"
      provides: "Server-owned overview, search, and detail read models with seeded fallback"
      contains: "getDocumentDetail"
    - path: "apps/api/src/modules/docs/docs.repository.test.ts"
      provides: "Repository coverage for relevance ranking, queue derivation, seeded fallback, and detail history"
      contains: "sharepoint restore"
    - path: "apps/api/src/routes/docs.ts"
      provides: "Stable HTTP routes for documentation overview, search, and detail"
      contains: "/api/docs/search"
    - path: "apps/api/src/routes/docs.test.ts"
      provides: "Route-level coverage for query parsing, detail payloads, and 404 behavior"
      contains: "metadata_review_only"
  key_links:
    - from: "prisma/migrations/20260328_0602_documentation_search/migration.sql"
      to: "apps/api/src/modules/docs/docs.repository.ts"
      via: "weighted search vector query over document content"
      pattern: "websearch_to_tsquery"
    - from: "apps/api/src/modules/docs/docs.repository.ts"
      to: "apps/api/src/routes/docs.ts"
      via: "overview, search, and detail DTO mapping"
      pattern: "searchDocuments"
    - from: "apps/api/src/routes/docs.ts"
      to: "apps/api/src/server.ts"
      via: "Fastify registration through buildServer"
      pattern: "registerDocsRoutes"
---

<objective>
Expose the documentation overview, search, and detail read model through server-owned search and HTTP contracts.

Purpose: Turn the canonical document corpus into ranked operator search results, a queue-first overview, and explanation-ready detail responses before any UI is built.
Output: Search-index migration, docs repository/search helpers, Fastify docs routes, and tests that lock the backend contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation-assistant/06-RESEARCH.md
@.planning/phases/06-documentation-assistant/06-VALIDATION.md
@.planning/phases/06-documentation-assistant/06-01-PLAN.md
@AGENTS.md
@apps/api/src/server.ts
@apps/api/src/routes/backup.ts
@apps/api/src/modules/backup/backup.repository.ts
@apps/api/src/modules/lifecycle/lifecycle.repository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement repository-owned docs overview, search ranking, and detail read models</name>
  <files>prisma/migrations/20260328_0602_documentation_search/migration.sql, apps/api/src/modules/docs/docs.search.ts, apps/api/src/modules/docs/docs.repository.ts, apps/api/src/modules/docs/docs.repository.test.ts</files>
  <read_first>apps/api/src/modules/docs/docs.types.ts, apps/api/src/modules/docs/docs.fixtures.ts, .planning/phases/06-documentation-assistant/06-RESEARCH.md, .planning/phases/06-documentation-assistant/06-VALIDATION.md, apps/api/src/modules/backup/backup.repository.ts, prisma/schema.prisma</read_first>
  <behavior>
    - Test 1: search query `sharepoint restore` returns `doc-m365-break-glass` before weaker content matches and includes a `content_match` or `title_match` reason
    - Test 2: exact metadata or linked-system filters return documents with `metadata_match` or `system_match` reasons and server-built facets
    - Test 3: overview queue ranks overdue review, incomplete metadata, and changed-after-review documents ahead of healthy records
    - Test 4: empty or missing document tables fall back to the seeded fixture corpus with `dataMode: 'seeded_example'`
    - Test 5: detail payloads include `history`, `linkedSystems`, `metadataCatalog`, and `writeBoundary: 'metadata_review_only'`
  </behavior>
  <action>Create `prisma/migrations/20260328_0602_documentation_search/migration.sql` with a weighted PostgreSQL full-text index named `document_search_vector_idx` over `title`, `summary`, `contentText`, and `searchText`, and use `websearch_to_tsquery('english', ...)` plus `ts_rank_cd` in repository queries. Create `apps/api/src/modules/docs/docs.search.ts` with exact helpers `buildDocumentationSearchQuery`, `rankSeededDocumentationResult`, `buildMatchedExcerpt`, and `collectDocumentationFacets` so live SQL and seeded fallback share one result contract. Implement `apps/api/src/modules/docs/docs.repository.ts` with exact methods `getOverview()`, `searchDocuments(filters)`, and `getDocumentDetail(documentId)`. Search must accept exact filters `q`, `kind`, `category`, `site`, `owner`, `systemId`, `reviewState`, and `staleOnly`; return exact reason types `title_match`, `content_match`, `metadata_match`, `system_match`, `review_overdue`, and `recent_change`; and keep the write-boundary constant `metadata_review_only` in every response. Overview queue items must use the exact reason labels `Review overdue`, `Metadata incomplete`, and `Updated since last review`, plus server-owned `suggestedNextStep`. Detail responses must include exact fields `contentText`, `metadataTags`, `linkedSystems`, `history`, `metadataCatalog`, `reviewDueAt`, `lastReviewedAt`, and `sourceUpdatedAt`. Lock the behavior with `apps/api/src/modules/docs/docs.repository.test.ts` using the seeded search cases from `docs.fixtures.ts` and explicit checks for missing-table fallback, queue ordering, and detail history visibility.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/modules/docs/docs.repository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/migrations/20260328_0602_documentation_search/migration.sql` contains `document_search_vector_idx`
    - `prisma/migrations/20260328_0602_documentation_search/migration.sql` contains `websearch_to_tsquery`
    - `apps/api/src/modules/docs/docs.search.ts` contains `buildDocumentationSearchQuery`
    - `apps/api/src/modules/docs/docs.search.ts` contains `buildMatchedExcerpt`
    - `apps/api/src/modules/docs/docs.repository.ts` contains `getOverview`
    - `apps/api/src/modules/docs/docs.repository.ts` contains `searchDocuments`
    - `apps/api/src/modules/docs/docs.repository.ts` contains `getDocumentDetail`
    - `apps/api/src/modules/docs/docs.repository.ts` contains `metadata_review_only`
    - `apps/api/src/modules/docs/docs.repository.test.ts` contains `sharepoint restore`
    - `apps/api/src/modules/docs/docs.repository.test.ts` contains `Review overdue`
    - `apps/api/src/modules/docs/docs.repository.test.ts` contains `seeded_example`
  </acceptance_criteria>
  <done>The backend now owns documentation ranking, queue derivation, seeded fallback, and detail history semantics for Phase 06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose overview, search, and detail routes through the shared server pattern</name>
  <files>apps/api/src/routes/docs.ts, apps/api/src/routes/docs.test.ts, apps/api/src/server.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/routes/backup.ts, apps/api/src/routes/network.ts, apps/api/src/modules/docs/docs.repository.ts, apps/api/src/modules/docs/docs.types.ts</read_first>
  <behavior>
    - Test 1: `GET /api/docs/overview` returns cards, queue items, `generatedAt`, and `writeBoundary: 'metadata_review_only'`
    - Test 2: `GET /api/docs/search` parses `q`, `kind`, `category`, `site`, `owner`, `systemId`, `reviewState`, and `staleOnly` into the repository filters without client-only logic
    - Test 3: `GET /api/docs/:documentId` returns detail sections including `history`, `linkedSystems`, and `metadataCatalog`
    - Test 4: `GET /api/docs/:documentId` returns `404` with `Documentation record not found` for an unknown document
  </behavior>
  <action>Create `apps/api/src/routes/docs.ts` exporting `registerDocsRoutes` and `DocsRoutesDependencies` with exact repository methods `getOverview`, `searchDocuments`, and `getDocumentDetail`. Add exact routes `GET /api/docs/overview`, `GET /api/docs/search`, and `GET /api/docs/:documentId`. Map exact query params `q`, `kind`, `category`, `site`, `owner`, `systemId`, `reviewState`, and `staleOnly`, and keep all response DTOs explicit with exact fields `writeBoundary`, `matchedExcerpt`, `relevanceScore`, `reasons`, `history`, `metadataCatalog`, and `linkedSystems`. Create `apps/api/src/routes/docs.test.ts` using injected repository doubles plus `app.inject()` to lock overview, search, detail, and `404` behavior. Update `apps/api/src/server.ts` to add `docsRoutes?: Partial<DocsRoutesDependencies>` to `BuildServerOptions`, instantiate the docs repository through the shared dependency-injection pattern, and register `registerDocsRoutes(app, docsRouteOptions)` alongside the existing module routes.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/routes/docs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/docs.ts` contains `registerDocsRoutes`
    - `apps/api/src/routes/docs.ts` contains `/api/docs/overview`
    - `apps/api/src/routes/docs.ts` contains `/api/docs/search`
    - `apps/api/src/routes/docs.ts` contains `Documentation record not found`
    - `apps/api/src/routes/docs.ts` contains `metadata_review_only`
    - `apps/api/src/routes/docs.test.ts` contains `/api/docs/search`
    - `apps/api/src/routes/docs.test.ts` contains `metadataCatalog`
    - `apps/api/src/routes/docs.test.ts` contains `Documentation record not found`
    - `apps/api/src/server.ts` contains `DocsRoutesDependencies`
    - `apps/api/src/server.ts` contains `registerDocsRoutes`
  </acceptance_criteria>
  <done>The shared API server exposes stable docs overview, search, and detail contracts that the web layer can consume without inventing relevance or history semantics.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/modules/docs/docs.repository.test.ts apps/api/src/routes/docs.test.ts` succeeds
- [ ] Search works across content, metadata, and linked-system context from one server-owned contract
- [ ] Overview, search, and detail responses all keep the exact write boundary `metadata_review_only` explicit
</verification>

<success_criteria>

- The API exposes trustworthy documentation overview, search, and detail routes
- Search relevance and queue derivation are server-owned and test-locked
- Seeded example fallback keeps the docs module usable without a live documentation connector

</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-assistant/06-02-SUMMARY.md`
</output>
