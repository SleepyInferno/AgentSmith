---
phase: 02-asset-health-dashboard
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - apps/web/src/routes/assets/DeviceInventoryPage.tsx
  - apps/web/src/components/assets/NeedsAttentionQueue.tsx
  - apps/web/src/lib/assets.ts
autonomous: true
requirements:
  - ASST-02
  - ASST-04
gap_closure: true
must_haves:
  truths:
    - Operator can choose inventory sort field and sort direction from the shipped UI instead of relying on a hardcoded default.
    - Inventory sort selections persist in URL query params and drive the existing `/api/assets/devices` server-side sort contract.
    - Queue and inventory risk labels use the backend `watch` bucket consistently across types, filters, and visible UI text.
  artifacts:
    - apps/web/src/routes/assets/DeviceInventoryPage.tsx
    - apps/web/src/components/assets/NeedsAttentionQueue.tsx
    - apps/web/src/lib/assets.ts
  key_links:
    - from: apps/web/src/routes/assets/DeviceInventoryPage.tsx
      to: apps/web/src/lib/assets.ts
      via: readParams plus getDeviceInventory query params
      pattern: sortField|sortDirection
    - from: apps/web/src/lib/assets.ts
      to: apps/api/src/routes/assets.ts
      via: DeviceInventoryParams matches supported inventory query keys
      pattern: riskScore|lastCheckInAt|deviceName|operatingSystem|asc|desc
    - from: apps/web/src/components/assets/NeedsAttentionQueue.tsx
      to: apps/web/src/lib/assets.ts
      via: AssetQueueItem riskLevel rendering
      pattern: watch
---

<objective>
Close the remaining Phase 2 operator-accessibility gap in asset triage.

Purpose: Finish ASST-02 by exposing sort controls the operator can actually use and by correcting the web contract from `medium` to backend `watch` so queue and inventory semantics stay trustworthy.
Output: Inventory sort controls wired through URL params, corrected web risk typing and labels, and verification steps proving the UI can drive the existing API sort contract.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-asset-health-dashboard/02-RESEARCH.md
@.planning/phases/02-asset-health-dashboard/02-VERIFICATION.md
@.planning/phases/02-asset-health-dashboard/02-03-SUMMARY.md
@apps/api/src/routes/assets.ts
@apps/web/src/routes/assets/DeviceInventoryPage.tsx
@apps/web/src/components/assets/NeedsAttentionQueue.tsx
@apps/web/src/lib/assets.ts

<interfaces>
From apps/api/src/routes/assets.ts:
```ts
Querystring: {
  search?: string;
  ownerId?: string;
  department?: string;
  site?: string;
  riskLevel?: string;
  riskSignal?: string;
  encryptionStatus?: string;
  antivirusStatus?: string;
  patchStatus?: string;
  staleOnly?: string;
  sortField?: string;
  sortDirection?: string;
}
```

From apps/api/src/modules/assets/asset-health.types.ts:
```ts
export const assetInventorySortFields = [
  "riskScore",
  "lastCheckInAt",
  "deviceName",
  "operatingSystem",
] as const;
export type AssetInventorySortDirection = "asc" | "desc";
```

From apps/web/src/lib/assets.ts:
```ts
export type DeviceInventoryParams = {
  riskLevel?: string;
  riskSignal?: string;
  encryptionStatus?: string;
  antivirusStatus?: string;
  patchStatus?: string;
  staleOnly?: boolean;
  sortField?: "riskScore" | "lastCheckInAt" | "deviceName" | "operatingSystem";
  sortDirection?: "asc" | "desc";
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct web risk-level typing and queue labels to use backend `watch`</name>
  <files>apps/web/src/lib/assets.ts, apps/web/src/components/assets/NeedsAttentionQueue.tsx</files>
  <read_first>apps/web/src/lib/assets.ts, apps/web/src/components/assets/NeedsAttentionQueue.tsx, apps/api/src/routes/assets.ts, apps/api/src/modules/assets/asset-health.types.ts, .planning/phases/02-asset-health-dashboard/02-VERIFICATION.md</read_first>
  <action>Update `apps/web/src/lib/assets.ts` so `RiskLevel` is the exact union `\"low\" | \"watch\" | \"high\" | \"critical\" | string`; remove `medium` from the type. Keep `AssetRiskSignal.severity` unchanged because backend signal severity still uses `medium`, but ensure visible risk-level options and labels do not. Update `apps/web/src/components/assets/NeedsAttentionQueue.tsx` so `toneForRisk()` handles `watch` instead of `medium`, renders the chip text from the backend `riskLevel`, and keeps `critical`, `high`, `watch`, and `low` visually distinct. Do not add a frontend-only mapping back to `medium`.</action>
  <verify>
    <automated>rg -n "watch|medium" apps/web/src/lib/assets.ts apps/web/src/components/assets/NeedsAttentionQueue.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/assets.ts` contains `export type RiskLevel = "low" | "watch" | "high" | "critical" | string;`
    - `apps/web/src/lib/assets.ts` does not contain `export type RiskLevel = "low" | "medium"`
    - `apps/web/src/components/assets/NeedsAttentionQueue.tsx` contains `case "watch":`
    - `apps/web/src/components/assets/NeedsAttentionQueue.tsx` does not contain `case "medium":`
    - `apps/web/src/components/assets/NeedsAttentionQueue.tsx` still contains `{item.riskLevel}`
  </acceptance_criteria>
  <done>The web layer matches the backend `watch` risk bucket, so queue visuals and any risk-level UI stop masking the API contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add operator sort controls and URL wiring for inventory queries</name>
  <files>apps/web/src/routes/assets/DeviceInventoryPage.tsx</files>
  <read_first>apps/web/src/routes/assets/DeviceInventoryPage.tsx, apps/web/src/lib/assets.ts, apps/api/src/routes/assets.ts, .planning/REQUIREMENTS.md, .planning/phases/02-asset-health-dashboard/02-VERIFICATION.md</read_first>
  <action>Update `apps/web/src/routes/assets/DeviceInventoryPage.tsx` so `readParams()` reads `sortField` and `sortDirection` from `useSearchParams()` and falls back only when those params are absent to `riskScore` and `desc`. Add two user-facing `FilterSelect` controls labeled exactly `sortField` and `sortDirection`. The `sortField` options must be `riskScore`, `lastCheckInAt`, `deviceName`, and `operatingSystem`. The `sortDirection` options must be `desc` and `asc`. Keep the controls in the existing filter panel and wire them through `updateFilter()` so changing either control writes the query string keys `sortField` and `sortDirection`, which then flow into `getDeviceInventory(params)` without any client-side re-sorting. Also change the visible `riskLevel` filter options from `critical`, `high`, `medium`, `low` to `critical`, `high`, `watch`, `low`.</action>
  <verify>
    <automated>npm exec pnpm -- --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `const sortField = searchParams.get("sortField")`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `const sortDirection = searchParams.get("sortDirection")`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `label="sortField"`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `label="sortDirection"`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `options={["riskScore", "lastCheckInAt", "deviceName", "operatingSystem"]}`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `options={["desc", "asc"]}`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `options={["critical", "high", "watch", "low"]}`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` contains `nextParams.set(name, value);`
    - `apps/web/src/routes/assets/DeviceInventoryPage.tsx` does not contain `options={["critical", "high", "medium", "low"]}`
  </acceptance_criteria>
  <done>The operator can change inventory sorting from the UI, the selection is preserved in the URL, and the inventory request calls the existing API sort contract with the chosen query params.</done>
</task>

</tasks>

<verification>
- Run `npm exec pnpm -- --filter @agentsmith/web build`.
- Run `rg -n "sortField|sortDirection|watch" apps/web/src/routes/assets/DeviceInventoryPage.tsx apps/web/src/components/assets/NeedsAttentionQueue.tsx apps/web/src/lib/assets.ts`.
- Confirm `apps/web/src/routes/assets/DeviceInventoryPage.tsx` no longer contains `options={["critical", "high", "medium", "low"]}`.
- Confirm the resulting build still compiles against the existing `/api/assets/devices` contract without adding new API fields.
</verification>

<success_criteria>
ASST-02 is operator-accessible in the shipped UI: sort field and direction controls exist, write `sortField` and `sortDirection` into the URL, and the web risk-level contract uses `watch` everywhere risk buckets are operator-visible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-health-dashboard/02-04-SUMMARY.md`
</output>
