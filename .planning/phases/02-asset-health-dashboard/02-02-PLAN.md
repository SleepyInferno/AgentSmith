---
phase: 02-asset-health-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/api/src/modules/assets/asset.types.ts
  - apps/api/src/modules/assets/assets.service.ts
  - apps/api/src/modules/assets/assets.service.test.ts
  - apps/api/src/routes/assets.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
must_haves:
  truths:
    - The operator can request a unified device inventory with the exact health fields needed for morning triage.
    - The operator can request a prioritized needs-attention queue generated from backend scoring rather than client-side heuristics.
    - The operator can open a device detail payload that explains every signal and stale-data warning driving the risk status.
  artifacts:
    - path: apps/api/src/modules/assets/assets.service.ts
      provides: Inventory, queue, and detail response shaping
    - path: apps/api/src/routes/assets.ts
      provides: Read-only Phase 2 asset endpoints
    - path: apps/api/src/modules/assets/assets.service.test.ts
      provides: Endpoint-shaping and stale-data behavior coverage
  key_links:
    - from: apps/api/src/routes/assets.ts
      to: apps/api/src/modules/assets/assets.service.ts
      via: Route handlers for inventory, queue, and detail
      pattern: "listAssets|getAssetQueue|getAssetDetail"
    - from: apps/api/src/modules/assets/assets.service.ts
      to: apps/api/src/modules/assets/assets.repository.ts
      via: Canonical repository reads
      pattern: "assetsRepository"
    - from: apps/api/src/modules/assets/assets.service.ts
      to: apps/api/src/modules/assets/risk-score.ts
      via: Shared risk and freshness summary generation
      pattern: "compareAssetRisk|scoreAssetRisk"
---

<objective>
Expose the Phase 2 asset inventory, queue, and detail APIs on top of the backend scoring layer.

Purpose: Give the frontend one stable, read-only API surface for browsing endpoints, filtering risk, and explaining why a device needs attention.
Output: Asset service, API route registration, automated API-shaping tests, and stale-data aware responses.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/AI/AgentSmith/AGENTS.md
@F:/AI/AgentSmith/.planning/ROADMAP.md
@F:/AI/AgentSmith/.planning/REQUIREMENTS.md
@F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-VALIDATION.md
@F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-01-PLAN.md
@F:/AI/AgentSmith/apps/api/src/server.ts

<interfaces>
From 02-01 expected outputs:
```ts
export interface AssetInventoryItem {
  id: string;
  name: string;
  ownerName: string | null;
  department: string | null;
  site: string | null;
  operatingSystem: string | null;
  osVersion: string | null;
  encryptionState: string | null;
  antivirusState: string | null;
  patchState: string | null;
  lastCheckInAt: string | null;
  diskFreeGb: number | null;
  assetAgeDays: number | null;
  unsupportedOs: boolean;
  freshnessState: "healthy" | "warning" | "stale" | "error";
  dataCompleteness: string | null;
  riskLevel: "healthy" | "low" | "medium" | "high";
  riskScore: number;
  riskReasons: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the read-only asset service with inventory, queue, and detail contracts</name>
  <files>apps/api/src/modules/assets/assets.service.ts, apps/api/src/modules/assets/assets.service.test.ts, apps/api/src/modules/assets/asset.types.ts</files>
  <read_first>apps/api/src/modules/assets/asset.types.ts, apps/api/src/modules/assets/assets.repository.ts, apps/api/src/modules/assets/risk-score.ts, F:/AI/AgentSmith/.planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: `listAssets` returns paged inventory rows with filter metadata and no provider-native field names
    - Test 2: `getAssetQueue` returns only non-healthy assets sorted by descending `riskScore`, then oldest `lastCheckInAt`
    - Test 3: `getAssetDetail` returns signal explanations and a top-level stale-data warning when freshness is `stale` or `error`
  </behavior>
  <action>Implement `apps/api/src/modules/assets/assets.service.ts` with exported functions `listAssets`, `getAssetQueue`, and `getAssetDetail`. `listAssets` must accept query fields `search`, `riskLevel`, `site`, `department`, `staleOnly`, `sortBy`, and `sortDirection`, where `sortBy` supports exactly `riskScore`, `lastCheckInAt`, `diskFreeGb`, `assetAgeDays`, and `name`. `getAssetQueue` must cap results at 25 by default and return `queueRank`, `riskScore`, `riskLevel`, `riskReasons`, `lastCheckInAt`, and `freshnessState`. `getAssetDetail` must return a `signals` array with entries keyed `stale-check-in`, `missing-encryption`, `missing-antivirus`, `low-disk`, `unsupported-os`, `outdated-patches`, and `stale-source-data`, each with `status`, `severity`, and `summary`. Add `apps/api/src/modules/assets/assets.service.test.ts` to lock these shapes before implementation and keep the service strictly read-only per the phase scope.</action>
  <acceptance_criteria>
    - apps/api/src/modules/assets/assets.service.ts contains `export async function listAssets`
    - apps/api/src/modules/assets/assets.service.ts contains `export async function getAssetQueue`
    - apps/api/src/modules/assets/assets.service.ts contains `export async function getAssetDetail`
    - apps/api/src/modules/assets/assets.service.ts contains `sortBy`
    - apps/api/src/modules/assets/assets.service.test.ts contains `stale-source-data`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agentsmith/api test</automated>
  </verify>
  <done>The asset module exposes stable backend contracts for inventory, queue, and detail without leaking schema details or provider fields to the UI.</done>
</task>

<task type="auto">
  <name>Task 2: Register authenticated asset API routes with explicit stale-data response fields</name>
  <files>apps/api/src/routes/assets.ts, apps/api/src/server.ts, apps/api/src/modules/assets/assets.service.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/routes/health.ts, apps/api/src/modules/assets/assets.service.ts, F:/AI/AgentSmith/AGENTS.md</read_first>
  <action>Create `apps/api/src/routes/assets.ts` and register it from `apps/api/src/server.ts` with the exact read-only endpoints `GET /api/assets`, `GET /api/assets/queue`, and `GET /api/assets/:assetId`. `GET /api/assets` must parse the filters from Task 1 and return `{ items, total, filtersApplied, staleDataBanner }`. `GET /api/assets/queue` must return `{ items, generatedAt, staleDataBanner }`. `GET /api/assets/:assetId` must return `{ asset, signals, staleDataBanner }` and use `404` when `assetId` is unknown. All three routes must carry `freshnessState`, `dataCompleteness`, and `staleDataBanner` explicitly so stale or partial data is visible to the frontend without inference. Keep route handlers read-only and do not add remediation or update endpoints in this phase.</action>
  <acceptance_criteria>
    - apps/api/src/routes/assets.ts contains `/api/assets`
    - apps/api/src/routes/assets.ts contains `/api/assets/queue`
    - apps/api/src/routes/assets.ts contains `/api/assets/:assetId`
    - apps/api/src/routes/assets.ts contains `staleDataBanner`
    - apps/api/src/server.ts contains `registerAssetRoutes` or `assets.ts`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agentsmith/api build && pnpm --filter @agentsmith/api test</automated>
  </verify>
  <done>The backend exposes one coherent Phase 2 asset API surface that already includes stale-data semantics the UI can render directly.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @agentsmith/api test` passes with asset service and route coverage
- `pnpm --filter @agentsmith/api build` passes with the new route registration
- Response payloads expose stale-data warnings as first-class fields instead of implicit null handling
</verification>

<success_criteria>
Phase 2 has read-only inventory, queue, and detail APIs that satisfy ASST-01 through ASST-04 and keep stale or incomplete source data explicit.
</success_criteria>

<output>
After completion, create `F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-02-SUMMARY.md`
</output>
