---
phase: 14-document-ingest-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - apps/web/e2e/ingest.spec.ts
autonomous: false
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
  - INGEST-05

must_haves:
  truths:
    - "E2E tests verify ingest UI surfaces render and respond to interaction"
    - "Human has verified end-to-end ingest flow with real file drop"
  artifacts:
    - path: "apps/web/e2e/ingest.spec.ts"
      provides: "Playwright E2E tests for ingest settings and status UI"
      contains: "ingest"
  key_links:
    - from: "apps/web/e2e/ingest.spec.ts"
      to: "/settings"
      via: "page.goto"
      pattern: "/settings"
---

<objective>
Write Playwright E2E tests for the ingest UI surfaces and provide a human verification checkpoint for the full end-to-end flow.

Purpose: Validates that the entire Phase 14 pipeline works from UI through backend. The E2E tests provide regression safety; the human checkpoint confirms real-world behavior with filesystem and OpenAI integration.
Output: Playwright spec file, human verification of full flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-document-ingest-pipeline/14-CONTEXT.md
@.planning/phases/14-document-ingest-pipeline/14-01-SUMMARY.md
@.planning/phases/14-document-ingest-pipeline/14-02-SUMMARY.md
@.planning/phases/14-document-ingest-pipeline/14-03-SUMMARY.md

<interfaces>
From apps/web/e2e/ (existing E2E test pattern):
```typescript
// Existing specs use mocked /api routes via page.route()
// See apps/web/e2e/intune-sync.spec.ts for the Phase 13 pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E tests for ingest UI</name>
  <files>
    apps/web/e2e/ingest.spec.ts
  </files>
  <read_first>
    apps/web/e2e/intune-sync.spec.ts,
    apps/web/e2e/auth.spec.ts,
    apps/web/playwright.config.ts,
    apps/web/src/routes/settings/IntegrationsPage.tsx
  </read_first>
  <action>
    Create `apps/web/e2e/ingest.spec.ts` following the pattern established in `intune-sync.spec.ts`:

    **Mock API routes** via `page.route()`:
    - `GET /api/me` -> authenticated session
    - `GET /api/auth/bootstrap-status` -> `{ bootstrapRequired: false }`
    - `GET /api/integrations/intune` -> `{ configured: true, ... }` (existing sections need data)
    - `GET /api/integrations/openai` -> `{ configured: true, ... }`
    - `GET /api/settings` -> `{ "ingest.sourceFolder": "/tmp/source", "ingest.outputFolder": "/tmp/output" }`
    - `PUT /api/settings` -> `{ ok: true }`
    - `POST /api/ingest/run` -> `{ runId: "test-run-id" }`
    - `GET /api/ingest/status` -> response varies by test

    **Tests:**
    1. "Ingest section renders on settings page" -- navigate to /settings, assert "Ingest" heading visible, "Source folder" and "Output folder" inputs visible
    2. "Folder inputs pre-fill from settings API" -- assert source folder input has value "/tmp/source", output folder has "/tmp/output"
    3. "Save button saves folder settings" -- fill inputs, click save, assert PUT /api/settings was called (use `page.route` to capture)
    4. "Trigger ingest button starts a run" -- click trigger button, assert POST /api/ingest/run was called, assert success toast appears
    5. "Status table shows ingest file results" -- mock GET /api/ingest/status to return a run with files (one done, one failed), assert table rows show filenames and status pills
    6. "Trigger button disabled when folders empty" -- mock GET /api/settings to return {}, navigate to /settings, assert trigger button is disabled
  </action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/e2e/ingest.spec.ts contains "Ingest section renders"
    - apps/web/e2e/ingest.spec.ts contains "Source folder"
    - apps/web/e2e/ingest.spec.ts contains "/api/ingest/run"
    - apps/web/e2e/ingest.spec.ts contains "/api/ingest/status"
    - apps/web/e2e/ingest.spec.ts contains "/api/settings"
    - `npx pnpm --filter @agentsmith/web test:e2e` exits 0
  </acceptance_criteria>
  <done>Six Playwright E2E tests cover ingest UI rendering, folder pre-fill, save, trigger, status table, and disabled state. All E2E tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete document ingest pipeline:
    - AppSetting, IngestRun, IngestFile database models
    - Settings API (GET/PUT /api/settings) with folder path validation
    - Document parsers for md, txt, docx, pdf
    - OpenAI classification (kind, title, summary) with structured JSON output
    - 512-token chunking with DocumentEmbedding rows via pgvector
    - chokidar file watcher with hot-reload on path save
    - Manual ingest trigger via POST /api/ingest/run
    - Status polling via GET /api/ingest/status
    - IngestSection on IntegrationsPage with folder inputs, save, trigger, and status table
    - Playwright E2E tests and unit tests
  </what-built>
  <how-to-verify>
    1. Start the API and web app (`npx pnpm dev` or equivalent)
    2. Navigate to /settings
    3. Verify Ingest section appears below Intune and OpenAI sections
    4. Enter a source folder path and output folder path, click Save
    5. Verify save success toast appears
    6. Copy a .txt or .md file into the source folder
    7. If OpenAI is configured: observe status table update with file entry showing "done" status
    8. Check the output folder for a kind-subfolder (e.g., infrastructure_note/) containing the copied file
    9. Click "Trigger ingest" button, verify it shows running state
    10. Verify previously ingested file is not re-processed (hash dedup)
    11. Run `npx pnpm test` and confirm all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx pnpm --filter @agentsmith/web test:e2e` passes
- `npx pnpm test` passes (full suite green)
- Human confirms ingest flow works end-to-end
</verification>

<success_criteria>
1. All Playwright E2E tests pass for ingest UI surfaces.
2. Human confirms: folder config saves, trigger starts run, status table updates, files are classified and copied.
3. Full test suite (`npx pnpm test`) remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-document-ingest-pipeline/14-04-SUMMARY.md`
</output>
