---
phase: 01-foundations-and-secure-data-flow
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - apps/api/src/modules/connectors/connector.registry.ts
  - apps/api/src/modules/connectors/connectors.service.ts
  - apps/api/src/modules/connectors/freshness.ts
  - apps/api/src/modules/connectors/providers/entra.provider.ts
  - apps/api/src/modules/connectors/providers/intune.provider.ts
  - apps/api/src/routes/connectors.ts
  - apps/api/src/routes/audit.ts
  - apps/api/src/jobs/runConnectorSync.ts
  - apps/web/src/routes/connectors/ConnectorStatusPage.tsx
  - apps/web/src/routes/audit/AuditTrailPage.tsx
  - apps/web/src/router.tsx
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - The operator can open a connector status page and see health, last sync timing, freshness state, and sync outcomes for integrated sources.
    - The operator can open an audit trail and see structured user and workflow events with timestamps and result states.
    - Entra and Intune are represented as first-class connector sources, even if the first iteration uses safe mocked or thin-sync data.
  artifacts:
    - apps/api/src/modules/connectors/connectors.service.ts
    - apps/api/src/routes/connectors.ts
    - apps/api/src/routes/audit.ts
    - apps/web/src/routes/connectors/ConnectorStatusPage.tsx
    - apps/web/src/routes/audit/AuditTrailPage.tsx
  key_links:
    - Connector API responses expose freshness and sync metadata stored in the canonical data model.
    - The protected shell routes to connector and audit pages added in Phase 1.
    - Background sync runs write both sync-run records and corresponding audit events.
---

<objective>
Implement connector health visibility and the shared audit trail surfaces for Phase 1.

Purpose: Make source freshness and operator/background activity visible so later dashboards are built on trusted, inspectable data flow.
Output: Connector services and routes, Entra/Intune source registrations, sync-run plumbing, connector status UI, and audit timeline UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md
@.planning/phases/01-foundations-and-secure-data-flow/01-01-SUMMARY.md
@.planning/phases/01-foundations-and-secure-data-flow/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create connector registry, sync tracking, and freshness evaluation</name>
  <files>apps/api/src/modules/connectors/connector.registry.ts, apps/api/src/modules/connectors/connectors.service.ts, apps/api/src/modules/connectors/freshness.ts, apps/api/src/modules/connectors/providers/entra.provider.ts, apps/api/src/modules/connectors/providers/intune.provider.ts, apps/api/src/jobs/runConnectorSync.ts</files>
  <read_first>prisma/schema.prisma, apps/api/src/server.ts, .planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md, .planning/research/STACK.md</read_first>
  <action>Create a connector module that registers exactly two sources in Phase 1: `entra` and `intune`. In `connector.registry.ts`, define their ids, labels, and sync handlers. In `freshness.ts`, implement freshness buckets `healthy`, `warning`, `stale`, and `error` based on last successful sync age and last attempted result. In `runConnectorSync.ts`, execute a sync run record with `startedAt`, `completedAt`, `result`, `recordsSeen`, and `recordsNormalized`, then write an audit event using action values `connector.sync_started`, `connector.sync_succeeded`, or `connector.sync_failed`. The initial provider files may return mocked or thin live data, but they must normalize into the canonical model shape rather than exposing raw provider payloads directly.</action>
  <verify>pnpm --filter @agentsmith/api test</verify>
  <acceptance_criteria>
    - apps/api/src/modules/connectors/connector.registry.ts contains `entra` and `intune`
    - apps/api/src/modules/connectors/freshness.ts contains `healthy`, `warning`, `stale`, and `error`
    - apps/api/src/jobs/runConnectorSync.ts contains `connector.sync_started` and `connector.sync_succeeded`
    - apps/api/src/modules/connectors/connectors.service.ts returns sync timing and freshness fields
  </acceptance_criteria>
  <done>The backend has a real connector-health contract, not just placeholder UI text.</done>
</task>

<task type="auto">
  <name>Task 2: Expose connector and audit APIs from the protected backend</name>
  <files>apps/api/src/routes/connectors.ts, apps/api/src/routes/audit.ts, apps/api/src/server.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/routes/me.ts, apps/api/src/modules/connectors/connectors.service.ts, apps/api/src/modules/audit/audit.service.ts</read_first>
  <action>Add authenticated API routes `GET /api/connectors` and `GET /api/audit-events`. `GET /api/connectors` must return an array of connector cards with `id`, `label`, `health`, `freshnessState`, `lastSuccessfulSyncAt`, `lastAttemptedSyncAt`, and `lastResult`. `GET /api/audit-events` must return reverse-chronological events with `timestamp`, `action`, `actorId`, `targetType`, `targetId`, `result`, and `metadata`. Require a valid session for both routes and keep payloads purpose-built for the UI rather than leaking database internals.</action>
  <verify>pnpm --filter @agentsmith/api build</verify>
  <acceptance_criteria>
    - apps/api/src/routes/connectors.ts contains `/api/connectors`
    - apps/api/src/routes/audit.ts contains `/api/audit-events`
    - apps/api/src/routes/connectors.ts returns `freshnessState`, `lastSuccessfulSyncAt`, and `lastResult`
    - apps/api/src/routes/audit.ts returns `timestamp`, `action`, and `result`
  </acceptance_criteria>
  <done>The frontend can fetch protected connector-health and audit data through stable Phase 1 APIs.</done>
</task>

<task type="auto">
  <name>Task 3: Add connector status and audit trail pages to the secure shell</name>
  <files>apps/web/src/routes/connectors/ConnectorStatusPage.tsx, apps/web/src/routes/audit/AuditTrailPage.tsx, apps/web/src/router.tsx</files>
  <read_first>apps/web/src/routes/ProtectedLayout.tsx, apps/web/src/router.tsx, .planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md</read_first>
  <action>Add `ConnectorStatusPage.tsx` that fetches `/api/connectors` and renders one card per source showing source name, health, freshness state, last successful sync, and last attempted result. Add `AuditTrailPage.tsx` that fetches `/api/audit-events` and renders a readable reverse-chronological list with timestamp, action label, actor, target, and result badge. Register both pages in `apps/web/src/router.tsx` beneath the protected shell routes. Include explicit empty and loading states so connector freshness and audit visibility are understandable even before real sync volume exists.</action>
  <verify>pnpm --filter @agentsmith/web build</verify>
  <acceptance_criteria>
    - apps/web/src/routes/connectors/ConnectorStatusPage.tsx requests `/api/connectors`
    - apps/web/src/routes/connectors/ConnectorStatusPage.tsx contains `freshness` or `Freshness`
    - apps/web/src/routes/audit/AuditTrailPage.tsx requests `/api/audit-events`
    - apps/web/src/router.tsx registers connector and audit routes under the protected layout
  </acceptance_criteria>
  <done>Phase 1 delivers visible connector status and audit history inside the authenticated application shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @agentsmith/api build` succeeds
- [ ] `pnpm --filter @agentsmith/web build` succeeds
- [ ] Connector API smoke checks return freshness and sync metadata
- [ ] Audit API returns structured events in reverse chronological order
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Connector health is visible for Entra and Intune
- User and background workflow events are queryable through the audit trail

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-secure-data-flow/01-03-SUMMARY.md`
</output>
