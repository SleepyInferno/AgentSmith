---
phase: 02-asset-health-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
  - "02-02"
files_modified:
  - apps/web/package.json
  - apps/web/src/main.tsx
  - apps/web/src/router.tsx
  - apps/web/src/lib/queryClient.ts
  - apps/web/src/lib/assets.ts
  - apps/web/src/routes/dashboard/AssetDashboardPage.tsx
  - apps/web/src/routes/assets/DeviceInventoryPage.tsx
  - apps/web/src/routes/assets/DeviceDetailPage.tsx
  - apps/web/src/components/assets/NeedsAttentionQueue.tsx
  - apps/web/src/components/assets/DeviceInventoryTable.tsx
  - apps/web/src/components/assets/RiskSignalList.tsx
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
user_setup: []
must_haves:
  truths:
    - The first Phase 2 screen is queue-first and tells the solo operator what needs attention now.
    - Inventory, queue, and detail pages render the same backend-provided risk and freshness data without recomputing scores in React.
    - The UI distinguishes healthy empty states from stale or incomplete source-data states.
  artifacts:
    - apps/web/src/routes/dashboard/AssetDashboardPage.tsx
    - apps/web/src/routes/assets/DeviceInventoryPage.tsx
    - apps/web/src/routes/assets/DeviceDetailPage.tsx
    - apps/web/src/components/assets/NeedsAttentionQueue.tsx
    - apps/web/src/components/assets/DeviceInventoryTable.tsx
    - apps/web/src/components/assets/RiskSignalList.tsx
  key_links:
    - The dashboard queue links directly to routeable device detail pages.
    - Inventory filters call the server-side query contract instead of duplicating sorting or ranking in the browser.
    - Freshness warnings shown on the dashboard and detail pages come from API payloads, not hardcoded UI assumptions.
---

<objective>
Deliver the operator-facing asset dashboard, inventory, and device detail experience for Phase 2.

Purpose: Turn the asset APIs into a useful morning-start workflow that surfaces the riskiest endpoints first and explains why they need attention.
Output: Router wiring, query client setup, queue-first dashboard page, filterable device inventory, routeable device detail page, and explicit stale-data UX states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-asset-health-dashboard/02-RESEARCH.md
@.planning/phases/02-asset-health-dashboard/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard routing, query infrastructure, and typed asset fetchers</name>
  <files>apps/web/package.json, apps/web/src/main.tsx, apps/web/src/router.tsx, apps/web/src/lib/queryClient.ts, apps/web/src/lib/assets.ts</files>
  <read_first>apps/web/package.json, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/router.tsx, packages/shared/src/index.ts, .planning/phases/01-foundations-and-secure-data-flow/01-02-PLAN.md</read_first>
  <action>Add `react-router-dom`, `@tanstack/react-query`, and `@tanstack/react-table` to `apps/web/package.json`. Create `apps/web/src/lib/queryClient.ts` exporting a `QueryClient` instance and update `apps/web/src/main.tsx` to wrap the app with `QueryClientProvider`. Create `apps/web/src/router.tsx` with routes for `/` to `AssetDashboardPage`, `/devices` to `DeviceInventoryPage`, and `/devices/:deviceId` to `DeviceDetailPage`. Create `apps/web/src/lib/assets.ts` with fetchers `getNeedsAttentionQueue()`, `getDeviceInventory(params)`, and `getDeviceDetail(deviceId)` that call `/api/assets/queue`, `/api/assets/devices`, and `/api/assets/devices/:deviceId` respectively and include request credentials if the shared API wrapper exists by execution time.</action>
  <verify>pnpm --filter @agentsmith/web build</verify>
  <acceptance_criteria>
    - apps/web/package.json contains `react-router-dom`
    - apps/web/package.json contains `@tanstack/react-query`
    - apps/web/package.json contains `@tanstack/react-table`
    - apps/web/src/router.tsx contains `/devices` and `/devices/:deviceId`
    - apps/web/src/lib/assets.ts contains `/api/assets/queue`, `/api/assets/devices`, and `/api/assets/devices/:deviceId`
  </acceptance_criteria>
  <done>The web app has the routing and data-fetching infrastructure needed for the Phase 2 operator experience.</done>
</task>

<task type="auto">
  <name>Task 2: Build the queue-first dashboard and filterable inventory views</name>
  <files>apps/web/src/routes/dashboard/AssetDashboardPage.tsx, apps/web/src/routes/assets/DeviceInventoryPage.tsx, apps/web/src/components/assets/NeedsAttentionQueue.tsx, apps/web/src/components/assets/DeviceInventoryTable.tsx</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/assets.ts, apps/web/src/App.tsx, .planning/phases/02-asset-health-dashboard/02-RESEARCH.md</read_first>
  <action>Create `AssetDashboardPage.tsx` that renders a top section titled `Needs attention`, a freshness warning area, and a queue list using `NeedsAttentionQueue.tsx`. The queue component must show `deviceName`, `riskLevel`, `riskScore`, `summary`, and the first one or two signal labels, and each row must link to `/devices/:deviceId`. Create `DeviceInventoryPage.tsx` using `DeviceInventoryTable.tsx` to render columns for `deviceName`, `ownerDisplayName`, `department`, `site`, `operatingSystem`, `encryptionStatus`, `antivirusStatus`, `patchStatus`, `lastCheckInAt`, and `riskLevel`. Add filter controls for `riskLevel`, `riskSignal`, `encryptionStatus`, `antivirusStatus`, `patchStatus`, and `staleOnly`, and wire them to the server-side query params instead of filtering in memory.</action>
  <verify>pnpm --filter @agentsmith/web build</verify>
  <acceptance_criteria>
    - apps/web/src/routes/dashboard/AssetDashboardPage.tsx contains `Needs attention`
    - apps/web/src/components/assets/NeedsAttentionQueue.tsx contains `riskScore` and `summary`
    - apps/web/src/routes/assets/DeviceInventoryPage.tsx contains `riskLevel`, `riskSignal`, and `staleOnly`
    - apps/web/src/components/assets/DeviceInventoryTable.tsx contains `ownerDisplayName`, `department`, `site`, `operatingSystem`, `encryptionStatus`, `antivirusStatus`, `patchStatus`, and `lastCheckInAt`
    - apps/web/src/components/assets/NeedsAttentionQueue.tsx links to `/devices/`
  </acceptance_criteria>
  <done>The app now offers a daily queue and inventory workflow that helps the operator triage endpoint risk quickly.</done>
</task>

<task type="auto">
  <name>Task 3: Build the explainable device detail page and stale-data UX states</name>
  <files>apps/web/src/routes/assets/DeviceDetailPage.tsx, apps/web/src/components/assets/RiskSignalList.tsx, apps/web/src/routes/dashboard/AssetDashboardPage.tsx, apps/web/src/routes/assets/DeviceInventoryPage.tsx</files>
  <read_first>apps/web/src/lib/assets.ts, apps/web/src/routes/assets/DeviceInventoryPage.tsx, apps/web/src/routes/dashboard/AssetDashboardPage.tsx, .planning/phases/02-asset-health-dashboard/02-RESEARCH.md</read_first>
  <action>Create `DeviceDetailPage.tsx` that requests `/api/assets/devices/:deviceId` and renders the exact sections `Risk summary`, `Signals`, and `Data freshness`. Create `RiskSignalList.tsx` that displays each signal's `label`, `severity`, and `explanation`. Add explicit UI messages for three distinct cases across dashboard, inventory, and detail pages: `No risky devices right now`, `No devices match the current filters`, and `Asset data is stale or incomplete`. On the detail page, display normalized health fields including `encryptionStatus`, `antivirusStatus`, `patchStatus`, `lastCheckInAt`, and `sourceFreshnessState` so the operator can see both the score and the underlying condition.</action>
  <verify>pnpm --filter @agentsmith/web build</verify>
  <acceptance_criteria>
    - apps/web/src/routes/assets/DeviceDetailPage.tsx contains `Risk summary`, `Signals`, and `Data freshness`
    - apps/web/src/components/assets/RiskSignalList.tsx contains `label`, `severity`, and `explanation`
    - apps/web/src/routes/dashboard/AssetDashboardPage.tsx contains `Asset data is stale or incomplete`
    - apps/web/src/routes/assets/DeviceInventoryPage.tsx contains `No devices match the current filters`
    - apps/web/src/routes/assets/DeviceDetailPage.tsx contains `encryptionStatus`, `antivirusStatus`, `patchStatus`, `lastCheckInAt`, and `sourceFreshnessState`
  </acceptance_criteria>
  <done>The operator can open a device and understand exactly why it needs attention, even when data freshness is degraded.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @agentsmith/web build` succeeds
- [ ] Dashboard page renders queue, freshness warning, and inventory navigation
- [ ] Device detail page renders backend-provided signal explanations
- [ ] Empty-state copy distinguishes healthy, filtered-empty, and stale-data conditions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The first Phase 2 screen tells the operator what matters next
- Device detail pages explain risk using backend-provided signals rather than UI-only heuristics

</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-health-dashboard/02-03-SUMMARY.md`
</output>
