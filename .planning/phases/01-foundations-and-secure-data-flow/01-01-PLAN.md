---
phase: 01-foundations-and-secure-data-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .env.example
  - README.md
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/vite.config.ts
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/server.ts
  - apps/api/src/routes/health.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/env.ts
  - prisma/schema.prisma
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
user_setup:
  - service: postgresql
    why: "Phase 1 needs a real relational datastore for canonical entities, sync runs, and audit records."
    env_vars:
      - name: DATABASE_URL
        source: "Local Postgres instance or hosted PostgreSQL connection string"
must_haves:
  truths:
    - The repository installs as a pnpm workspace with separate web, api, and shared packages.
    - A Prisma schema defines canonical records for users, devices, systems, groups, documents, connectors, sync runs, and audit events.
    - The API exposes a health endpoint and the web app can boot against the shared workspace configuration.
  artifacts:
    - package.json
    - pnpm-workspace.yaml
    - apps/web/src/main.tsx
    - apps/api/src/server.ts
    - packages/shared/src/env.ts
    - prisma/schema.prisma
  key_links:
    - Root workspace scripts invoke both apps through pnpm workspace commands.
    - Shared environment parsing is imported by the API and reusable from later modules.
    - Prisma models include the same canonical entities named in Phase 1 context.
---

<objective>
Create the executable repository foundation for Phase 1.

Purpose: Establish the workspace, runtime, and database backbone that every later module will build on.
Output: A pnpm workspace with web, api, and shared packages; baseline env/config; and a Prisma schema for the canonical operational model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the workspace and shared TypeScript toolchain</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json, apps/web/package.json, apps/web/tsconfig.json, apps/web/vite.config.ts, apps/api/package.json, apps/api/tsconfig.json, packages/shared/package.json, packages/shared/tsconfig.json</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md, README.md</read_first>
  <action>Create a pnpm workspace rooted at `package.json` with three packages: `apps/web`, `apps/api`, and `packages/shared`. Define root scripts `dev`, `build`, `lint`, `typecheck`, and `db:generate`. Set package names to `@agentsmith/web`, `@agentsmith/api`, and `@agentsmith/shared`. Add TypeScript workspace settings in `tsconfig.base.json` and make each package extend it. Configure `apps/web` for Vite + React + TypeScript and `apps/api` for Fastify + TypeScript with ESM-compatible build scripts. Keep the structure lightweight so later phases add modules without reworking the workspace layout.</action>
  <verify>pnpm install</verify>
  <acceptance_criteria>
    - package.json contains `"workspaces"` or equivalent pnpm workspace scripts for `dev`, `build`, `lint`, `typecheck`, and `db:generate`
    - pnpm-workspace.yaml contains `apps/*` and `packages/*`
    - apps/web/package.json contains `"name": "@agentsmith/web"`
    - apps/api/package.json contains `"name": "@agentsmith/api"`
    - packages/shared/package.json contains `"name": "@agentsmith/shared"`
  </acceptance_criteria>
  <done>Workspace manifests exist and `pnpm install` resolves the three-package layout without manual edits.</done>
</task>

<task type="auto">
  <name>Task 2: Add baseline app entrypoints and shared environment parsing</name>
  <files>.env.example, README.md, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/api/src/server.ts, apps/api/src/routes/health.ts, packages/shared/src/env.ts</files>
  <read_first>.planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md, .planning/research/STACK.md, README.md</read_first>
  <action>Create a minimal React application entrypoint in `apps/web/src/main.tsx` and `apps/web/src/App.tsx` that renders an AgentSmith placeholder shell with sections for Dashboard, Connectors, and Audit. Create a Fastify server in `apps/api/src/server.ts` that registers `GET /health` from `apps/api/src/routes/health.ts` and reads configuration from `packages/shared/src/env.ts`. Populate `.env.example` with concrete variables `DATABASE_URL=`, `PORT=3001`, `WEB_ORIGIN=http://localhost:5173`, `ENTRA_TENANT_ID=`, `ENTRA_CLIENT_ID=`, `ENTRA_CLIENT_SECRET=`, and `ENTRA_REDIRECT_URI=http://localhost:3001/auth/callback`. Update README.md with workspace bootstrap, install, and local run steps using `pnpm install` and `pnpm dev`.</action>
  <verify>pnpm --filter @agentsmith/api build && pnpm --filter @agentsmith/web build</verify>
  <acceptance_criteria>
    - .env.example contains `DATABASE_URL=`, `ENTRA_TENANT_ID=`, `ENTRA_CLIENT_ID=`, `ENTRA_CLIENT_SECRET=`, and `ENTRA_REDIRECT_URI=`
    - apps/api/src/routes/health.ts contains `GET /health` or an exported health route handler
    - apps/web/src/App.tsx contains the text `Dashboard`, `Connectors`, and `Audit`
    - README.md contains `pnpm install` and `pnpm dev`
  </acceptance_criteria>
  <done>The repo boots with a visible shell placeholder and a Fastify health endpoint wired through shared environment parsing.</done>
</task>

<task type="auto">
  <name>Task 3: Define the canonical Prisma schema for Phase 1</name>
  <files>prisma/schema.prisma</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Create `prisma/schema.prisma` with models named `User`, `Device`, `System`, `Group`, `Document`, `ConnectorSource`, `SyncRun`, and `AuditEvent`. Each model must preserve provider provenance using fields such as `sourceSystem`, `sourceId`, or explicit relations. Add status fields needed for freshness tracking: `lastSuccessfulSyncAt`, `lastAttemptedSyncAt`, `freshnessState`, and `syncStatus` on connector-related records where appropriate. Include `timestamp`, `actorId`, `action`, `targetType`, `targetId`, `result`, and `metadata Json` on `AuditEvent`. Use PostgreSQL as the datasource and Prisma client generation for the ORM layer.</action>
  <verify>pnpm db:generate</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `model User`, `model Device`, `model System`, `model Group`, and `model Document`
    - prisma/schema.prisma contains `model ConnectorSource`, `model SyncRun`, and `model AuditEvent`
    - prisma/schema.prisma contains `provider = "postgresql"`
    - prisma/schema.prisma contains `metadata Json` in `AuditEvent`
  </acceptance_criteria>
  <done>The Prisma schema represents the full canonical backbone required by Phase 1 and is ready for migrations and API services.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` succeeds
- [ ] `pnpm --filter @agentsmith/api build` succeeds
- [ ] `pnpm --filter @agentsmith/web build` succeeds
- [ ] `pnpm db:generate` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a concrete executable foundation rather than planning-only files
- Canonical models required by Phase 1 exist in Prisma schema

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-secure-data-flow/01-01-SUMMARY.md`
</output>
