---
phase: 05-backup-confidence-dashboard
plan: 04
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/lib/backup.ts
  - apps/web/src/routes/backup/BackupDetailPage.tsx
  - apps/web/src/components/backup/BackupEvidenceTimeline.tsx
  - apps/web/src/components/backup/BackupConfidenceBreakdown.tsx
  - apps/web/src/components/backup/BackupSourceHealthCard.tsx
autonomous: true
requirements:
  - BACK-01
  - BACK-03
must_haves:
  truths:
    - "The detail experience explains why a system is healthy, high-risk, unknown, or excluded using policy windows and source evidence instead of opaque status pills."
    - "Queue and inventory handoffs lead directly into a detail route where the operator can inspect provider evidence and restore proof without losing context."
    - "The detail page stays explanation-first and avoids live backup or restore actions."
    - "Unknown telemetry, excluded policy, and operator-attested proof are all visible as distinct trust states."
  artifacts:
    - path: "apps/web/src/routes/backup/BackupDetailPage.tsx"
      provides: "Explanation-first backup system detail route"
      contains: "Confidence breakdown"
    - path: "apps/web/src/components/backup/BackupEvidenceTimeline.tsx"
      provides: "Chronological backup and restore-proof evidence rendering"
      contains: "Restore proof"
    - path: "apps/web/src/components/backup/BackupConfidenceBreakdown.tsx"
      provides: "Readable explanation of policy window, coverage, and confidence posture"
      contains: "Policy window"
    - path: "apps/web/src/components/backup/BackupSourceHealthCard.tsx"
      provides: "Source-health and provenance disclosure for provider telemetry"
      contains: "Source health"
  key_links:
    - from: "apps/web/src/lib/backup.ts"
      to: "apps/web/src/routes/backup/BackupDetailPage.tsx"
      via: "detail query contract"
      pattern: "getBackupSystemDetail"
    - from: "apps/web/src/routes/backup/BackupInventoryPage.tsx"
      to: "apps/web/src/routes/backup/BackupDetailPage.tsx"
      via: "inventory-to-detail handoff"
      pattern: "/backup/systems/"
    - from: "apps/web/src/components/backup/BackupEvidenceTimeline.tsx"
      to: "apps/web/src/components/backup/BackupSourceHealthCard.tsx"
      via: "trust explanation stack"
      pattern: "Source health"
---

<objective>
Deliver the explanation-first backup detail workflow.

Purpose: Give the operator a detail route that explains the exact evidence, policy window, restore-proof posture, and telemetry freshness behind each backup-confidence state without implying live remediation actions.
Output: Backup detail route, detail fetcher wiring, and reusable components for confidence breakdown, evidence timeline, and source-health disclosure.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backup-confidence-dashboard/05-02-PLAN.md
@.planning/phases/05-backup-confidence-dashboard/05-03-PLAN.md
@apps/web/src/router.tsx
@apps/web/src/lib/backup.ts
@apps/web/src/routes/network/NetworkDetailPage.tsx
@apps/web/src/routes/backup/BackupOverviewPage.tsx
@apps/web/src/routes/backup/BackupInventoryPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the backup detail route and typed detail fetch contract</name>
  <files>apps/web/src/router.tsx, apps/web/src/lib/backup.ts</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/backup.ts, apps/web/src/routes/network/NetworkDetailPage.tsx, apps/web/src/routes/backup/BackupInventoryPage.tsx, .planning/phases/05-backup-confidence-dashboard/05-03-PLAN.md</read_first>
  <action>Extend `apps/web/src/lib/backup.ts` so `getBackupSystemDetail(systemId)` returns exact detail fields `systemId`, `systemName`, `category`, `siteName`, `providerKey`, `coverageState`, `confidenceState`, `lastSuccessfulBackupAt`, `lastRestoreTestAt`, `policyWindow`, `sourceHealth`, `timeline`, `summary`, `suggestedNextStep`, and `isReadOnly`. Update `apps/web/src/router.tsx` to register `path: "backup/systems/:systemId"` for `BackupDetailPage`, and keep route links from overview and inventory pointed to that exact path so the queue-first workflow can drill into evidence instead of dead-ending at the list view.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/backup.ts` contains `getBackupSystemDetail`
    - `apps/web/src/lib/backup.ts` contains `policyWindow`
    - `apps/web/src/lib/backup.ts` contains `sourceHealth`
    - `apps/web/src/lib/backup.ts` contains `timeline`
    - `apps/web/src/router.tsx` contains `backup/systems/:systemId`
    - `apps/web/src/router.tsx` contains `BackupDetailPage`
  </acceptance_criteria>
  <done>The backup module now has a dedicated detail route and a typed detail contract for explanation-first evidence review.</done>
</task>

<task type="auto">
  <name>Task 2: Build the explanation-first backup detail page and trust-context components</name>
  <files>apps/web/src/routes/backup/BackupDetailPage.tsx, apps/web/src/components/backup/BackupEvidenceTimeline.tsx, apps/web/src/components/backup/BackupConfidenceBreakdown.tsx, apps/web/src/components/backup/BackupSourceHealthCard.tsx</files>
  <read_first>apps/web/src/lib/backup.ts, apps/web/src/routes/network/NetworkDetailPage.tsx, apps/web/src/routes/backup/BackupOverviewPage.tsx, apps/web/src/components/backup/BackupFindingsQueue.tsx, .planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md</read_first>
  <action>Create `BackupDetailPage.tsx` with exact sections `Confidence breakdown`, `Coverage evidence`, `Restore proof`, `Source health`, and `Policy window`. Build `BackupConfidenceBreakdown.tsx` to explain coverage, freshness threshold, restore-test recency, and why the current `confidenceState` was assigned. Build `BackupEvidenceTimeline.tsx` to render the latest successful backup, last failed backup, latest restore proof, and any operator-attested evidence in chronological order. Build `BackupSourceHealthCard.tsx` to show provider freshness, connector freshness, evidence source, and whether the current view is read-only. Render the exact operator-facing state copy `Expected backup coverage is missing for this system`, `Backup evidence is current but restore proof is overdue`, `Provider telemetry is unavailable, so confidence is unknown`, and `This system is intentionally excluded from backup policy` when those states apply. Keep top-level actions limited to navigation such as `Back to overview` and `Open protected-system inventory`; do not add buttons that imply live backup jobs, restore tests, acknowledgements, or exception approvals.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Confidence breakdown`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Coverage evidence`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Restore proof`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Source health`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Policy window`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Expected backup coverage is missing for this system`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Backup evidence is current but restore proof is overdue`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `Provider telemetry is unavailable, so confidence is unknown`
    - `apps/web/src/routes/backup/BackupDetailPage.tsx` contains `This system is intentionally excluded from backup policy`
    - `apps/web/src/components/backup/BackupEvidenceTimeline.tsx` contains `operator_attested`
    - `apps/web/src/components/backup/BackupSourceHealthCard.tsx` contains `read-only`
  </acceptance_criteria>
  <done>The operator can now inspect why each system is healthy, risky, unknown, or excluded through an explanation-first backup detail route.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Backup detail explains confidence using policy windows and source evidence
- [ ] Queue and inventory routes can hand off directly into backup detail
- [ ] No live-action controls imply backup jobs, restore tests, or exception approvals
</verification>

<success_criteria>

- The backup module has a usable detail workflow for evidence review
- Confidence, coverage, restore proof, and source health are all readable from one route
- Trust-boundary copy remains explicit and consistent with the app's high-trust design rules

</success_criteria>

<output>
After completion, create `.planning/phases/05-backup-confidence-dashboard/05-04-SUMMARY.md`
</output>
