---
phase: 01-foundations-and-secure-data-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .env.example
  - apps/api/src/plugins/auth.ts
  - apps/api/src/routes/auth.ts
  - apps/api/src/routes/me.ts
  - apps/api/src/modules/audit/audit.service.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/hooks/useSession.ts
  - apps/web/src/routes/LoginPage.tsx
  - apps/web/src/routes/ProtectedLayout.tsx
  - apps/web/src/router.tsx
  - apps/web/src/main.tsx
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
user_setup:
  - service: microsoft-entra-id
    why: "Operator sign-in requires an Entra application registration and redirect URI configuration."
    env_vars:
      - name: ENTRA_TENANT_ID
        source: "Microsoft Entra admin center -> Overview -> Tenant ID"
      - name: ENTRA_CLIENT_ID
        source: "App registration -> Overview -> Application (client) ID"
      - name: ENTRA_CLIENT_SECRET
        source: "App registration -> Certificates & secrets -> Client secret value"
      - name: ENTRA_REDIRECT_URI
        source: "App registration -> Authentication -> Web redirect URI"
must_haves:
  truths:
    - Operators can authenticate with Entra ID and land inside a protected application shell.
    - Authentication state is checked through the API rather than local-only browser state.
    - Sign-in and sign-out events are written to the audit trail with actor and result metadata.
  artifacts:
    - apps/api/src/plugins/auth.ts
    - apps/api/src/routes/auth.ts
    - apps/api/src/routes/me.ts
    - apps/web/src/routes/LoginPage.tsx
    - apps/web/src/routes/ProtectedLayout.tsx
  key_links:
    - The web login page redirects through API auth endpoints backed by Entra configuration.
    - The protected layout gates navigation based on the `/api/me` session response.
    - Audit logging is invoked from the auth route handlers for successful and failed auth events.
---

<objective>
Implement Entra ID authentication and the first secure application shell.

Purpose: Satisfy Phase 1's secure-access requirement and ensure the rest of the product builds inside an authenticated, auditable surface.
Output: API auth routes, secure session handling, a protected React shell, and auth-driven audit events.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md
@.planning/phases/01-foundations-and-secure-data-flow/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Entra OIDC and secure session handling in the API</name>
  <files>.env.example, apps/api/src/plugins/auth.ts, apps/api/src/routes/auth.ts, apps/api/src/routes/me.ts, apps/api/src/modules/audit/audit.service.ts</files>
  <read_first>.env.example, apps/api/src/server.ts, prisma/schema.prisma, .planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md</read_first>
  <action>Use `openid-client` with the Microsoft identity platform issuer to implement Authorization Code + PKCE in `apps/api/src/plugins/auth.ts`. Register routes in `apps/api/src/routes/auth.ts` for `GET /auth/login`, `GET /auth/callback`, and `POST /auth/logout`. Store the authenticated operator in a server-managed session cookie marked `httpOnly`, `secure` in production, `sameSite=lax`, and path `/`. Add `GET /api/me` in `apps/api/src/routes/me.ts` returning `{ authenticated: true, user: { id, email, displayName } }` for valid sessions and `401` otherwise. Extend `.env.example` with `SESSION_SECRET=`. Call the audit service for successful sign-in, failed callback validation, and logout events using action values `auth.login`, `auth.login_failed`, and `auth.logout`.</action>
  <verify>pnpm --filter @agentsmith/api build</verify>
  <acceptance_criteria>
    - apps/api/src/routes/auth.ts contains `/auth/login`, `/auth/callback`, and `/auth/logout`
    - apps/api/src/routes/me.ts contains `/api/me`
    - .env.example contains `SESSION_SECRET=`
    - apps/api/src/modules/audit/audit.service.ts contains `auth.login` and `auth.logout`
  </acceptance_criteria>
  <done>The API can perform Entra sign-in and expose authenticated operator identity through a protected endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add a protected React shell and login flow</name>
  <files>apps/web/src/lib/api.ts, apps/web/src/hooks/useSession.ts, apps/web/src/routes/LoginPage.tsx, apps/web/src/routes/ProtectedLayout.tsx, apps/web/src/router.tsx, apps/web/src/main.tsx</files>
  <read_first>apps/web/src/main.tsx, apps/web/src/App.tsx, .planning/phases/01-foundations-and-secure-data-flow/01-CONTEXT.md</read_first>
  <action>Create a shared fetch wrapper in `apps/web/src/lib/api.ts` that includes credentials for API requests. Add `useSession.ts` to query `/api/me` and return loading, authenticated, and unauthenticated states. Replace the temporary app entry with a router in `apps/web/src/router.tsx` containing a public `LoginPage` route and a `ProtectedLayout` route tree. `LoginPage.tsx` must show a clear sign-in CTA that points to `/auth/login`, while `ProtectedLayout.tsx` must render navigation links for Dashboard, Connectors, and Audit plus the current operator display name from `useSession`. Redirect unauthenticated users to the login page and show a loading state while the `/api/me` request is in flight.</action>
  <verify>pnpm --filter @agentsmith/web build</verify>
  <acceptance_criteria>
    - apps/web/src/routes/LoginPage.tsx contains `Sign in with Microsoft`
    - apps/web/src/routes/ProtectedLayout.tsx contains `Dashboard`, `Connectors`, and `Audit`
    - apps/web/src/hooks/useSession.ts requests `/api/me`
    - apps/web/src/lib/api.ts includes request credentials
  </acceptance_criteria>
  <done>The web app enforces authenticated access and renders the secured shell once a session exists.</done>
</task>

<task type="auto">
  <name>Task 3: Wire authentication events into the audit trail contract</name>
  <files>apps/api/src/modules/audit/audit.service.ts, apps/api/src/routes/auth.ts, apps/api/src/routes/me.ts</files>
  <read_first>prisma/schema.prisma, apps/api/src/modules/audit/audit.service.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create or extend `apps/api/src/modules/audit/audit.service.ts` so audit writes accept the exact fields `timestamp`, `actorId`, `action`, `targetType`, `targetId`, `result`, and `metadata`. In the auth routes, persist `targetType='session'`, `targetId` as the operator or session identifier, and `result` values `success`, `failure`, or `signed_out`. Ensure failed callback validation or token exchange writes an audit event even when the session is not established. Keep `/api/me` read-only and do not log every session check.</action>
  <verify>pnpm --filter @agentsmith/api test</verify>
  <acceptance_criteria>
    - apps/api/src/modules/audit/audit.service.ts contains `timestamp`, `actorId`, `action`, `targetType`, `targetId`, `result`, and `metadata`
    - apps/api/src/routes/auth.ts contains `success` and `failure` audit result values
    - apps/api/src/routes/me.ts does not create audit entries on every request
  </acceptance_criteria>
  <done>Authentication is not only functional but also traceable through structured audit records.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @agentsmith/api build` succeeds
- [ ] `pnpm --filter @agentsmith/web build` succeeds
- [ ] Auth route tests or smoke checks pass
- [ ] Successful login and logout create audit records
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Entra ID is the only operator sign-in path
- The application shell is inaccessible without a valid session

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-secure-data-flow/01-02-SUMMARY.md`
</output>
