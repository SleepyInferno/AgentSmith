---
phase: 03-lifecycle-automation
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
  - "02-03"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/lib/lifecycle.ts
  - apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx
  - apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx
  - apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "The operator can open a lifecycle page, see onboarding and offboarding templates, and launch a run from the UI."
    - "Launching a lifecycle run from the queue opens the specific run detail route so guided execution continues immediately."
    - "The lifecycle landing page shows active runs with grouped progress, unresolved work visibility, and a visible path into run detail."
    - "The queue-first lifecycle flow reaches the step editor and final summary screens without asking the operator to bridge pages manually."
    - "The UI explains that Phase 3 records work; it does not trigger live administrative actions."
  artifacts:
    - path: "apps/web/src/lib/lifecycle.ts"
      provides: "Lifecycle fetchers and mutation helpers"
      exports: ["getLifecycleTemplates", "getLifecycleRuns", "startLifecycleRun"]
    - path: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      provides: "Lifecycle landing page"
      contains: "Lifecycle workflows"
    - path: "apps/web/src/router.tsx"
      provides: "Lifecycle queue and detail route registration in the existing app shell"
      contains: "path: \"lifecycle/runs/:runId\""
    - path: "apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx"
      provides: "Template launch cards"
      contains: "employee-onboarding"
    - path: "apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx"
      provides: "Active-run progress cards with detail CTAs"
      contains: "Open run details"
  key_links:
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      via: "route registration"
      pattern: "path: \"lifecycle\""
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx"
      via: "detail route registration"
      pattern: "path: \"lifecycle/runs/:runId\""
    - from: "apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx"
      to: "apps/web/src/lib/lifecycle.ts"
      via: "React Query load, launch mutation, and post-launch navigation"
      pattern: "navigate(`/lifecycle/runs/"
    - from: "apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx"
      to: "apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx"
      via: "visible CTA into the guided run-detail workflow"
      pattern: "to={`/lifecycle/runs/"
---

<objective>
Deliver the operator-facing lifecycle landing page that launches runs and moves directly into guided execution.

Purpose: Give the solo operator a queue-first lifecycle workspace where reusable templates, active runs, unresolved work, and the next guided action are visible within minutes of opening the app.
Output: Lifecycle fetch/mutation helpers, router wiring for queue and detail routes, template launch cards, and active-run overview UI with direct run-detail entry points.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lifecycle-automation/03-CONTEXT.md
@.planning/phases/03-lifecycle-automation/03-02-PLAN.md
@.planning/phases/03-lifecycle-automation/03-VERIFICATION.md
@apps/web/src/router.tsx
@apps/web/src/lib/queryClient.ts
@apps/web/src/lib/assets.ts

<interfaces>
From apps/web/src/lib/assets.ts:
```ts
async function apiRequest<T>(input: string): Promise<T> { ... }
export function getDeviceInventory(params: DeviceInventoryParams = {}) { ... }
export function getDeviceDetail(deviceId: string) { ... }
```

From apps/web/src/router.tsx:
```tsx
export const router = createBrowserRouter([
  {
    path: "/",
    element: <AppShell />,
    children: [...]
  },
]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle web contracts and route entry points</name>
  <files>apps/web/src/router.tsx, apps/web/src/lib/lifecycle.ts</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/assets.ts, apps/web/src/lib/queryClient.ts, apps/api/src/routes/lifecycle.ts, apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx, .planning/phases/03-lifecycle-automation/03-CONTEXT.md, .planning/phases/03-lifecycle-automation/03-VERIFICATION.md</read_first>
  <action>Create `apps/web/src/lib/lifecycle.ts` using the same `credentials: "include"` fetch pattern as `assets.ts`. Export lifecycle DTO types and exact helpers `getLifecycleTemplates()`, `getLifecycleRuns()`, `getLifecycleRun(runId)`, `getLifecycleRunSummary(runId)`, `startLifecycleRun(input)`, `updateLifecycleStep(input)`, and `closeLifecycleRun(runId)`. Update `apps/web/src/router.tsx` to register both `path: "lifecycle"` for `LifecycleQueuePage` and `path: "lifecycle/runs/:runId"` for `LifecycleRunDetailPage` inside the existing app shell so the queue-first flow can reach the guided execution screen called out in the verification gap. Keep lifecycle beside the asset queue in primary navigation, and add tracking-only copy per D-04 and D-06 using the exact sentence `Records work only - no live admin actions are triggered here.`</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/lifecycle.ts` contains `getLifecycleTemplates`
    - `apps/web/src/lib/lifecycle.ts` contains `startLifecycleRun`
    - `apps/web/src/lib/lifecycle.ts` contains `updateLifecycleStep`
    - `apps/web/src/lib/lifecycle.ts` contains `closeLifecycleRun`
    - `apps/web/src/router.tsx` contains `path: "lifecycle"`
    - `apps/web/src/router.tsx` contains `path: "lifecycle/runs/:runId"`
    - `apps/web/src/router.tsx` contains `Records work only`
  </acceptance_criteria>
  <done>The web app knows how to call the lifecycle API and route operators from the queue into the guided run-detail workflow without introducing a second data-loading pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Build the lifecycle landing page with template launch and active-run cards</name>
  <files>apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx, apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx, apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx</files>
  <read_first>apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx, apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx, apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx, apps/web/src/lib/lifecycle.ts, apps/web/src/router.tsx, apps/web/src/routes/lifecycle/LifecycleRunDetailPage.tsx, apps/web/src/routes/dashboard/AssetDashboardPage.tsx, .planning/phases/03-lifecycle-automation/03-RESEARCH.md, .planning/phases/03-lifecycle-automation/03-VERIFICATION.md</read_first>
  <action>Update `LifecycleQueuePage.tsx` with the exact heading `Lifecycle workflows`, a short tracking-only explainer, and two top sections: `Start a run` and `Active runs`. Keep the `LifecycleTemplateCards.tsx` launch flow for `employee-onboarding` and `employee-offboarding`, but after `startLifecycleRun` succeeds the queue page must call `navigate(\`/lifecycle/runs/${run.runId}\`)` so the operator lands in the guided step editor immediately and the queue-first workflow fully covers LIFE-02, LIFE-03, and LIFE-04. Replace the active-runs intro copy with exact guidance that operators can review grouped progress, scan the last update time, and open the run detail to continue evidence capture or close-out. Build `ActiveLifecycleRuns.tsx` to show `subjectDisplayName`, `kind`, grouped progress counts, latest updated time from the API `updatedAt` field, unresolved follow-up counts, and a visible CTA rendered as either `Link` text `Open run details` or an equivalent button that routes to `/lifecycle/runs/:runId`. Keep the cards review-first and tracking-only per D-06, and continue invalidating `["lifecycle-templates"]`, `["lifecycle-runs"]`, `["lifecycle-run", run.runId]`, and `["lifecycle-run-summary", run.runId]` after a successful launch.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `Lifecycle workflows`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `Start a run`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `Active runs`
    - `apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx` contains `navigate(\`/lifecycle/runs/`
    - `apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx` contains `employee-onboarding`
    - `apps/web/src/components/lifecycle/LifecycleTemplateCards.tsx` contains `employee-offboarding`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `unresolved`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `updatedAt`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `Open run details`
    - `apps/web/src/components/lifecycle/ActiveLifecycleRuns.tsx` contains `/lifecycle/runs/`
  </acceptance_criteria>
  <done>The operator can launch onboarding or offboarding from reusable templates, land in the guided run-detail flow immediately, and reopen active runs from visible queue CTAs.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] Lifecycle navigation is available from the existing app shell
- [ ] Launching a run invalidates the affected queries and navigates to `/lifecycle/runs/:runId`
- [ ] Active-run cards render a visible CTA into `/lifecycle/runs/:runId`
- [ ] The landing page stays queue-first and clearly states the tracking-only boundary
</verification>

<success_criteria>

- The lifecycle page is a usable operator slice with template launch, active-run visibility, and direct run-detail entry
- Onboarding and offboarding are presented as reusable grouped workflows instead of ad hoc lists
- The operator can continue into step editing and final summary screens from the queue-first workflow without manual URL hops

</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-automation/03-03-SUMMARY.md`
</output>
