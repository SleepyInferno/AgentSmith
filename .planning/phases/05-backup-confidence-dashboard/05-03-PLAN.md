---
phase: 05-backup-confidence-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/lib/backup.ts
  - apps/web/src/routes/backup/BackupOverviewPage.tsx
  - apps/web/src/routes/backup/BackupInventoryPage.tsx
  - apps/web/src/components/backup/BackupFindingsQueue.tsx
  - apps/web/src/components/backup/BackupInventoryTable.tsx
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - BACK-03
must_haves:
  truths:
    - "The first backup screen is queue-first and helps the solo operator decide what needs review within minutes of opening the app."
    - "Overview and inventory screens render backend-owned coverage and confidence semantics instead of recomputing them in React."
    - "The UI discloses seeded-example mode and the read-only trust boundary instead of implying live backup operations."
    - "Inventory filters remain server-driven and bookmarkable."
  artifacts:
    - path: "apps/web/src/router.tsx"
      provides: "Top-level navigation and route wiring for the backup-confidence module"
      contains: "/backup"
    - path: "apps/web/src/lib/backup.ts"
      provides: "Typed fetchers for backup overview, findings, inventory, and detail APIs"
      contains: "/api/backup/overview"
    - path: "apps/web/src/routes/backup/BackupOverviewPage.tsx"
      provides: "Queue-first landing page for backup review"
      contains: "Needs review"
    - path: "apps/web/src/routes/backup/BackupInventoryPage.tsx"
      provides: "Filterable protected-system inventory route"
      contains: "confidenceState"
    - path: "apps/web/src/components/backup/BackupFindingsQueue.tsx"
      provides: "Operator-facing backup finding list with provider and next-step context"
      contains: "suggestedNextStep"
    - path: "apps/web/src/components/backup/BackupInventoryTable.tsx"
      provides: "Dense protected-system scanning table"
      contains: "lastRestoreTestAt"
  key_links:
    - from: "apps/web/src/lib/backup.ts"
      to: "apps/web/src/routes/backup/BackupOverviewPage.tsx"
      via: "backup overview and findings queries"
      pattern: "getBackupOverview"
    - from: "apps/web/src/lib/backup.ts"
      to: "apps/web/src/routes/backup/BackupInventoryPage.tsx"
      via: "server-driven inventory query"
      pattern: "getBackupInventory"
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/routes/backup/BackupOverviewPage.tsx"
      via: "top-level route registration"
      pattern: "/backup"
---

<objective>
Deliver the queue-first backup overview and filterable protected-system inventory.

Purpose: Turn the new backup API into an operator workflow that highlights missing coverage, stale restore proof, and unknown telemetry first, then lets the operator scan the protected-system inventory with clear trust context.
Output: Router wiring, typed backup fetchers, a queue-first overview route, and a filterable inventory UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md
@.planning/phases/05-backup-confidence-dashboard/05-02-PLAN.md
@apps/web/src/router.tsx
@apps/web/src/lib/network.ts
@apps/web/src/routes/network/NetworkOverviewPage.tsx
@apps/web/src/routes/network/NetworkInventoryPage.tsx
@apps/web/src/components/network/NetworkFindingsQueue.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backup navigation, route entry points, and typed web contracts</name>
  <files>apps/web/src/router.tsx, apps/web/src/lib/backup.ts</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/network.ts, apps/web/src/lib/queryClient.ts, apps/web/src/routes/network/NetworkOverviewPage.tsx, .planning/phases/05-backup-confidence-dashboard/05-02-PLAN.md</read_first>
  <action>Create `apps/web/src/lib/backup.ts` with exact fetchers `getBackupOverview()`, `getBackupFindings()`, `getBackupInventory(params)`, and `getBackupSystemDetail(systemId)` calling `/api/backup/overview`, `/api/backup/findings`, `/api/backup/systems`, and `/api/backup/systems/:systemId`. Define local DTOs that include exact fields `dataMode`, `systemId`, `systemName`, `category`, `siteName`, `providerKey`, `coverageState`, `confidenceState`, `lastSuccessfulBackupAt`, `lastRestoreTestAt`, `summary`, `suggestedNextStep`, `sourceHealth`, and `isReadOnly`. Update `apps/web/src/router.tsx` so the existing ghost `Backup Confidence` nav item becomes a real route at `/backup`, add `/backup/inventory`, and preserve the established app-shell visual language. Add the exact sentence `Records evidence only - no backup jobs or restore actions are triggered here.` to the backup module shell copy or route intro so the UI keeps the write boundary explicit.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/backup.ts` contains `/api/backup/overview`
    - `apps/web/src/lib/backup.ts` contains `/api/backup/findings`
    - `apps/web/src/lib/backup.ts` contains `/api/backup/systems`
    - `apps/web/src/lib/backup.ts` contains `getBackupOverview`
    - `apps/web/src/lib/backup.ts` contains `getBackupInventory`
    - `apps/web/src/lib/backup.ts` contains `getBackupSystemDetail`
    - `apps/web/src/router.tsx` contains `Backup Confidence`
    - `apps/web/src/router.tsx` contains `/backup`
    - `apps/web/src/router.tsx` contains `/backup/inventory`
    - `apps/web/src/router.tsx` contains `Records evidence only - no backup jobs or restore actions are triggered here.`
  </acceptance_criteria>
  <done>The shared shell now exposes Backup Confidence as a first-class route and the web layer has typed contracts for all Phase 5 APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the queue-first overview and protected-system inventory pages</name>
  <files>apps/web/src/routes/backup/BackupOverviewPage.tsx, apps/web/src/routes/backup/BackupInventoryPage.tsx, apps/web/src/components/backup/BackupFindingsQueue.tsx, apps/web/src/components/backup/BackupInventoryTable.tsx</files>
  <read_first>apps/web/src/lib/backup.ts, apps/web/src/routes/network/NetworkOverviewPage.tsx, apps/web/src/routes/network/NetworkInventoryPage.tsx, apps/web/src/components/network/NetworkFindingsQueue.tsx, .planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md</read_first>
  <action>Create `BackupOverviewPage.tsx` with exact sections `Needs review`, `Coverage baseline`, `Restore proof`, and `Inventory navigation`. When any backup query returns `dataMode === 'seeded_example'`, render the exact disclosure copy `Example backup data is shown until a live backup source is connected`. Use `BackupFindingsQueue.tsx` to render finding rows with exact fields `systemName`, `coverageState`, `confidenceState`, `providerKey`, `lastSuccessfulBackupAt`, `lastRestoreTestAt`, `summary`, and `suggestedNextStep`, plus link targets of `/backup/systems/:systemId` for later detail routing. Create `BackupInventoryPage.tsx` using `BackupInventoryTable.tsx` to render columns `systemName`, `category`, `siteName`, `coverageState`, `backupProvider`, `lastSuccessfulBackupAt`, `lastRestoreTestAt`, `confidenceState`, and `summary`. Add filter controls for `confidenceState`, `coverageState`, `providerKey`, `siteName`, and `search`, and keep them server-driven by wiring query params to `getBackupInventory` instead of filtering in memory. Render the exact empty-state copy `No backup findings need review right now`, `No protected systems match the current filters`, `Backup telemetry is stale or incomplete`, and `Excluded systems are shown separately from missing coverage` where appropriate.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `Needs review`
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `Coverage baseline`
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `Restore proof`
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `Inventory navigation`
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `Example backup data is shown until a live backup source is connected`
    - `apps/web/src/components/backup/BackupFindingsQueue.tsx` contains `suggestedNextStep`
    - `apps/web/src/routes/backup/BackupInventoryPage.tsx` contains `confidenceState`
    - `apps/web/src/routes/backup/BackupInventoryPage.tsx` contains `coverageState`
    - `apps/web/src/routes/backup/BackupInventoryPage.tsx` contains `providerKey`
    - `apps/web/src/components/backup/BackupInventoryTable.tsx` contains `lastRestoreTestAt`
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `No backup findings need review right now`
    - `apps/web/src/routes/backup/BackupInventoryPage.tsx` contains `No protected systems match the current filters`
    - `apps/web/src/routes/backup/BackupOverviewPage.tsx` contains `Backup telemetry is stale or incomplete`
    - `apps/web/src/routes/backup/BackupInventoryPage.tsx` contains `Excluded systems are shown separately from missing coverage`
  </acceptance_criteria>
  <done>The operator can now start in a queue-first backup view and move into a filterable protected-system inventory without losing trust context.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] The backup overview page is queue-first and highlights what needs review now
- [ ] Inventory filters remain server-driven and bookmarkable
- [ ] The UI discloses seeded-example and stale-data states instead of implying live protection certainty
</verification>

<success_criteria>

- Backup Confidence is reachable as a first-class module from the app shell
- The first backup screen helps the operator decide what matters next
- The inventory exposes server-owned coverage and restore-proof fields without client-side confidence math

</success_criteria>

<output>
After completion, create `.planning/phases/05-backup-confidence-dashboard/05-03-SUMMARY.md`
</output>
