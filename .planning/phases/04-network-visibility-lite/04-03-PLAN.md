---
phase: 04-network-visibility-lite
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/lib/network.ts
  - apps/web/src/routes/network/NetworkOverviewPage.tsx
  - apps/web/src/routes/network/NetworkInventoryPage.tsx
  - apps/web/src/components/network/NetworkFindingsQueue.tsx
  - apps/web/src/components/network/NetworkInventoryTable.tsx
autonomous: true
requirements:
  - NET-01
  - NET-03
must_haves:
  truths:
    - "The first network screen is queue-first and tells the operator what needs review before they open the full map."
    - "Inventory and overview screens render backend-provided freshness, scope, and next-step fields without recomputing ranking in React."
    - "The shared app shell expands to include network visibility without breaking the established navigation pattern from earlier phases."
    - "If the backend is serving seeded example data, the UI says so explicitly instead of letting sample topology look like live telemetry."
  artifacts:
    - path: "apps/web/src/router.tsx"
      provides: "Top-level navigation and route wiring for the network module"
      contains: "/network"
    - path: "apps/web/src/lib/network.ts"
      provides: "Typed fetchers for findings, inventory, map, and detail APIs"
      contains: "/api/network/findings"
    - path: "apps/web/src/routes/network/NetworkOverviewPage.tsx"
      provides: "Queue-first landing page for network review"
      contains: "Needs review"
    - path: "apps/web/src/routes/network/NetworkInventoryPage.tsx"
      provides: "Filterable network inventory route"
      contains: "freshnessState"
    - path: "apps/web/src/components/network/NetworkFindingsQueue.tsx"
      provides: "Operator-facing finding list with scope and next-step context"
      contains: "suggestedNextStep"
    - path: "apps/web/src/components/network/NetworkInventoryTable.tsx"
      provides: "Dense network inventory scanning table"
      contains: "resourceKind"
  key_links:
    - from: "apps/web/src/lib/network.ts"
      to: "apps/web/src/routes/network/NetworkOverviewPage.tsx"
      via: "network findings query"
      pattern: "getNetworkFindings"
    - from: "apps/web/src/lib/network.ts"
      to: "apps/web/src/routes/network/NetworkInventoryPage.tsx"
      via: "inventory query"
      pattern: "getNetworkInventory"
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/routes/network/NetworkOverviewPage.tsx"
      via: "top-level route registration"
      pattern: "/network"
---

<objective>
Deliver the queue-first network landing page and filterable inventory experience.

Purpose: Turn the new network API into an operator workflow that surfaces what needs review now and lets the operator expand into inventory when needed.
Output: Router wiring, typed network fetchers, queue-first overview page, and filterable network inventory UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-network-visibility-lite/04-CONTEXT.md
@.planning/phases/04-network-visibility-lite/04-RESEARCH.md
@.planning/phases/04-network-visibility-lite/04-02-PLAN.md
@apps/web/src/router.tsx
@apps/web/src/routes/dashboard/AssetDashboardPage.tsx
@apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx
@apps/web/src/routes/assets/DeviceInventoryPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add network navigation, routes, and typed web fetchers</name>
  <files>apps/web/src/router.tsx, apps/web/src/lib/network.ts</files>
  <read_first>apps/web/src/router.tsx, apps/web/src/lib/assets.ts, apps/web/src/lib/queryClient.ts, apps/web/src/routes/lifecycle/LifecycleQueuePage.tsx, .planning/phases/04-network-visibility-lite/04-RESEARCH.md</read_first>
  <action>Create `apps/web/src/lib/network.ts` with exact fetchers `getNetworkFindings()`, `getNetworkInventory(params)`, `getNetworkMap()`, and `getNetworkResourceDetail(resourceId)` calling `/api/network/findings`, `/api/network/resources`, `/api/network/map`, and `/api/network/resources/:resourceId`. Define local web DTO types that include `dataMode`, `resourceId`, `resourceName`, `resourceKind`, `siteName`, `operationalStatus`, `freshnessState`, `lastSeenAt`, `summary`, `suggestedNextStep`, and `confidence`. Update `apps/web/src/router.tsx` to add navigation item label `Network visibility`, routes `/network` and `/network/inventory`, and app-shell copy that broadens the current Phase 3 messaging into a product-wide operator shell while preserving the existing visual language.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/network.ts` contains `/api/network/findings`
    - `apps/web/src/lib/network.ts` contains `/api/network/resources`
    - `apps/web/src/lib/network.ts` contains `/api/network/map`
    - `apps/web/src/lib/network.ts` contains `/api/network/resources/`
    - `apps/web/src/lib/network.ts` contains `dataMode`
    - `apps/web/src/lib/network.ts` contains `getNetworkFindings`
    - `apps/web/src/lib/network.ts` contains `getNetworkInventory`
    - `apps/web/src/router.tsx` contains `Network visibility`
    - `apps/web/src/router.tsx` contains `/network`
    - `apps/web/src/router.tsx` contains `/network/inventory`
  </acceptance_criteria>
  <done>The shared shell now knows about the network module and the web layer has typed fetchers for all Phase 4 APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the queue-first overview and filterable inventory pages</name>
  <files>apps/web/src/routes/network/NetworkOverviewPage.tsx, apps/web/src/routes/network/NetworkInventoryPage.tsx, apps/web/src/components/network/NetworkFindingsQueue.tsx, apps/web/src/components/network/NetworkInventoryTable.tsx</files>
  <read_first>apps/web/src/lib/network.ts, apps/web/src/routes/dashboard/AssetDashboardPage.tsx, apps/web/src/routes/assets/DeviceInventoryPage.tsx, apps/web/src/components/assets/NeedsAttentionQueue.tsx, .planning/phases/04-network-visibility-lite/04-RESEARCH.md</read_first>
  <action>Create `NetworkOverviewPage.tsx` with sections titled `Needs review`, `Topology confidence`, and `Inventory navigation`. When any network query returns `dataMode === 'seeded_example'`, render the exact disclosure copy `Example network data is shown until a live source is connected`. Use `NetworkFindingsQueue.tsx` to render finding rows with exact fields `resourceName`, `resourceKind`, `siteName`, `summary`, `suggestedNextStep`, and `freshnessState`, plus a link target of `/network/resources/:resourceId` for later detail routing. Create `NetworkInventoryPage.tsx` using `NetworkInventoryTable.tsx` to render columns `resourceName`, `resourceKind`, `siteName`, `operationalStatus`, `freshnessState`, `lastSeenAt`, and `summary`. Add filter controls for `kind`, `site`, `operationalStatus`, and `freshnessState`, and keep them server-driven by wiring query params to `getNetworkInventory` instead of filtering in memory. Render exact empty-state copy `No network findings need review right now`, `No network resources match the current filters`, and `Network data is stale or incomplete` where appropriate.</action>
  <verify>
    <automated>npx pnpm --filter @agentsmith/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/network/NetworkOverviewPage.tsx` contains `Needs review`
    - `apps/web/src/routes/network/NetworkOverviewPage.tsx` contains `Topology confidence`
    - `apps/web/src/routes/network/NetworkOverviewPage.tsx` contains `Inventory navigation`
    - `apps/web/src/routes/network/NetworkOverviewPage.tsx` contains `Example network data is shown until a live source is connected`
    - `apps/web/src/components/network/NetworkFindingsQueue.tsx` contains `suggestedNextStep`
    - `apps/web/src/routes/network/NetworkInventoryPage.tsx` contains `kind`, `site`, `operationalStatus`, and `freshnessState`
    - `apps/web/src/components/network/NetworkInventoryTable.tsx` contains `resourceKind`
    - `apps/web/src/components/network/NetworkInventoryTable.tsx` contains `lastSeenAt`
    - `apps/web/src/routes/network/NetworkOverviewPage.tsx` contains `No network findings need review right now`
    - `apps/web/src/routes/network/NetworkInventoryPage.tsx` contains `No network resources match the current filters`
    - `apps/web/src/routes/network/NetworkOverviewPage.tsx` contains `Network data is stale or incomplete`
  </acceptance_criteria>
  <done>The operator can now start in a queue-first network view and move into a filterable inventory without losing trust context.</done>
</task>

</tasks>

<verification>
- [ ] `npx pnpm --filter @agentsmith/web build` succeeds
- [ ] The network overview page is queue-first and highlights what needs review now
- [ ] Inventory filters remain server-driven and bookmarkable
- [ ] Empty states distinguish healthy, filtered-empty, and stale-data conditions
</verification>

<success_criteria>

- The app shell includes Network Visibility as a first-class module
- The first network screen helps the operator decide what matters next
- Inventory exposes typed network resources with server-owned freshness and scope fields

</success_criteria>

<output>
After completion, create `.planning/phases/04-network-visibility-lite/04-03-SUMMARY.md`
</output>
