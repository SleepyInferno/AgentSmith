---
phase: 05-backup-confidence-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - apps/api/src/modules/backup/backup.findings.ts
  - apps/api/src/modules/backup/backup.repository.ts
  - apps/api/src/modules/backup/backup.repository.test.ts
  - apps/api/src/modules/backup/backup.findings.test.ts
  - apps/api/src/routes/backup.ts
  - apps/api/src/routes/backup.test.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - BACK-03
must_haves:
  truths:
    - "The backend derives one server-owned confidence view per protected system instead of asking the UI to combine backup and restore timestamps itself."
    - "Unknown provider telemetry and missing backup coverage are separate states with different queue behavior and next steps."
    - "API responses expose overview, findings, inventory, and detail flows with honest `dataMode` and source-health disclosure."
    - "Phase 1 connector debt is surfaced as source-health context rather than hidden behind a falsely healthy dashboard."
  artifacts:
    - path: "apps/api/src/modules/backup/backup.findings.ts"
      provides: "Server-owned confidence ranking and suggested-next-step helpers"
      contains: "buildBackupFindingQueue"
    - path: "apps/api/src/modules/backup/backup.repository.ts"
      provides: "Backup overview, queue, inventory, and detail repository methods"
      exports: ["BackupRepository"]
    - path: "apps/api/src/routes/backup.ts"
      provides: "Fastify routes for backup overview, findings, inventory, and detail endpoints"
      contains: "/api/backup/overview"
    - path: "apps/api/src/routes/backup.test.ts"
      provides: "Route coverage for filter parsing, seeded fallback, and detail 404 behavior"
      contains: "/api/backup/systems"
  key_links:
    - from: "apps/api/src/modules/backup/backup.findings.ts"
      to: "apps/api/src/modules/backup/backup.repository.ts"
      via: "server-owned queue ranking and next-step messaging"
      pattern: "buildBackupFindingQueue"
    - from: "apps/api/src/modules/backup/backup.repository.ts"
      to: "apps/api/src/routes/backup.ts"
      via: "overview and inventory route dependencies"
      pattern: "getOverview"
    - from: "apps/api/src/routes/backup.ts"
      to: "apps/api/src/routes/backup.test.ts"
      via: "HTTP contract coverage"
      pattern: "/api/backup/overview"
---

<objective>
Expose the canonical backup overview, queue, inventory, and detail APIs with honest confidence states and source-health context.

Purpose: Turn the Phase 5 backend model into stable read-only contracts that reveal missing coverage, stale restore proof, unknown telemetry, and explicit data mode without leaking provider-specific logic into the UI.
Output: Confidence helpers, repository methods, route wiring, and tests for overview, findings, inventory, and detail flows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backup-confidence-dashboard/05-REVIEWS.md
@.planning/phases/05-backup-confidence-dashboard/05-01-PLAN.md
@prisma/schema.prisma
@apps/api/src/server.ts
@apps/api/src/routes/assets.ts
@apps/api/src/routes/network.ts
@apps/api/src/modules/assets/asset-health.repository.ts
@apps/api/src/modules/network/network.repository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement server-owned backup confidence, queue ranking, and repository read models</name>
  <files>apps/api/src/modules/backup/backup.findings.ts, apps/api/src/modules/backup/backup.repository.ts, apps/api/src/modules/backup/backup.repository.test.ts, apps/api/src/modules/backup/backup.findings.test.ts</files>
  <read_first>apps/api/src/modules/backup/backup.types.ts, apps/api/src/modules/backup/backup.fixtures.ts, apps/api/src/modules/assets/asset-health.repository.ts, apps/api/src/modules/network/network.repository.ts, .planning/phases/05-backup-confidence-dashboard/05-01-PLAN.md</read_first>
  <behavior>
    - Test 1: required systems with no provider evidence produce `coverageState: 'missing'`, `confidenceState: 'high_risk'`, and the summary `Coverage missing for an expected protected system`
    - Test 2: recent backup evidence plus stale restore proof produces `confidenceState: 'high_risk'` with the suggested next step `Schedule a restore test`
    - Test 3: provider telemetry outage produces `confidenceState: 'unknown'` and the summary `Backup telemetry is unavailable from the configured provider`
    - Test 4: excluded systems stay out of the risk queue and keep the exact suggested next step `System is intentionally excluded from backup policy`
    - Test 5: empty or missing backup tables fall back to `dataMode: 'seeded_example'` without pretending the data is live
  </behavior>
  <action>Create `apps/api/src/modules/backup/backup.findings.ts` exporting exact helpers `buildBackupFindingQueue(rows)`, `buildBackupOverviewCards(rows)`, and `buildBackupSuggestedNextStep(row)`. Create `apps/api/src/modules/backup/backup.repository.ts` exporting class `BackupRepository` with methods `getOverview()`, `listFindings(limit)`, `listInventory(filters)`, and `getSystemDetail(systemId)`. Read from Prisma models `System`, `BackupCoveragePolicy`, `BackupEvidence`, and `BackupRestoreTest`; when Phase 5 tables are empty or missing, return the deterministic fixture dataset from `backup.fixtures.ts` and tag every payload with `dataMode: 'seeded_example'`, otherwise tag live reads with `dataMode: 'live'`. Derive exact confidence logic: `healthy` when policy is `required`, coverage is `protected`, latest successful backup is within `backupFreshnessHours`, and latest successful restore test is within `restoreTestMaxAgeDays`; `watch` when backup is current but within the policy grace window or coverage is partial without a stale backup; `high_risk` when coverage is `missing` or `partial`, backup freshness is beyond policy plus grace, restore proof is stale or missing, or the latest restore outcome is `failure`; `unknown` when policy is `required` but connector freshness is `stale` or `error` or provider telemetry is absent. Return queue items with exact suggested-next-step strings `Confirm the system is enrolled in backup policy`, `Review the last successful backup job`, `Schedule a restore test`, `Investigate provider telemetry freshness`, and `System is intentionally excluded from backup policy`. Create `backup.repository.test.ts` and `backup.findings.test.ts` covering healthy, missing, stale backup, stale restore proof, excluded, unknown, and seeded-example scenarios.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/modules/backup/backup.repository.test.ts apps/api/src/modules/backup/backup.findings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/backup/backup.findings.ts` contains `buildBackupFindingQueue`
    - `apps/api/src/modules/backup/backup.findings.ts` contains `buildBackupOverviewCards`
    - `apps/api/src/modules/backup/backup.repository.ts` contains `class BackupRepository`
    - `apps/api/src/modules/backup/backup.repository.ts` contains `getOverview`
    - `apps/api/src/modules/backup/backup.repository.ts` contains `listFindings`
    - `apps/api/src/modules/backup/backup.repository.ts` contains `listInventory`
    - `apps/api/src/modules/backup/backup.repository.ts` contains `getSystemDetail`
    - `apps/api/src/modules/backup/backup.repository.ts` contains `seeded_example`
    - `apps/api/src/modules/backup/backup.repository.test.ts` contains `Coverage missing for an expected protected system`
    - `apps/api/src/modules/backup/backup.findings.test.ts` contains `Schedule a restore test`
    - `apps/api/src/modules/backup/backup.findings.test.ts` contains `Investigate provider telemetry freshness`
  </acceptance_criteria>
  <done>The backend can now derive one trustworthy backup-confidence view per protected system and rank what the operator should review next.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose backup overview, findings, inventory, and detail routes through the shared server pattern</name>
  <files>apps/api/src/routes/backup.ts, apps/api/src/routes/backup.test.ts, apps/api/src/server.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/routes/assets.ts, apps/api/src/routes/network.ts, apps/api/src/modules/backup/backup.repository.ts, apps/api/src/modules/backup/backup.types.ts</read_first>
  <action>Create `apps/api/src/routes/backup.ts` with exact endpoints `/api/backup/overview`, `/api/backup/findings`, `/api/backup/systems`, and `/api/backup/systems/:systemId`. Parse query filters `search`, `confidenceState`, `coverageState`, `providerKey`, `siteName`, `category`, and `staleOnly`, and map them into repository filters without performing client-only filtering. Map responses so overview cards, finding rows, inventory rows, and detail payloads include exact fields `dataMode`, `systemId`, `systemName`, `category`, `siteName`, `providerKey`, `coverageState`, `confidenceState`, `lastSuccessfulBackupAt`, `lastRestoreTestAt`, `summary`, `suggestedNextStep`, `sourceHealth`, and `isReadOnly`. Create `apps/api/src/routes/backup.test.ts` covering filter parsing, seeded-example fallback, unknown-telemetry disclosure, and 404 behavior for missing detail IDs. Update `apps/api/src/server.ts` to import `BackupRepository`, add a `backupRoutes` dependency option, instantiate the repository in `buildServer`, and register `registerBackupRoutes` through the same injection pattern used by assets, lifecycle, and network routes.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/routes/backup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/backup.ts` contains `/api/backup/overview`
    - `apps/api/src/routes/backup.ts` contains `/api/backup/findings`
    - `apps/api/src/routes/backup.ts` contains `/api/backup/systems`
    - `apps/api/src/routes/backup.ts` contains `/api/backup/systems/:systemId`
    - `apps/api/src/routes/backup.ts` contains `confidenceState`
    - `apps/api/src/routes/backup.ts` contains `sourceHealth`
    - `apps/api/src/routes/backup.ts` contains `isReadOnly`
    - `apps/api/src/routes/backup.test.ts` contains `seeded_example`
    - `apps/api/src/routes/backup.test.ts` contains `Backup telemetry is unavailable from the configured provider`
    - `apps/api/src/server.ts` contains `BackupRepository`
    - `apps/api/src/server.ts` contains `registerBackupRoutes`
  </acceptance_criteria>
  <done>The shared server now exposes stable backup-confidence APIs with honest source-health and read-only trust-boundary context.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/modules/backup/backup.repository.test.ts apps/api/src/modules/backup/backup.findings.test.ts apps/api/src/routes/backup.test.ts` succeeds
- [ ] API responses distinguish missing coverage from unknown telemetry and excluded policy states
- [ ] The backend surfaces overview, queue, inventory, and detail flows without asking the web layer to calculate confidence
</verification>

<success_criteria>

- Phase 5 has stable API contracts for overview, findings, inventory, and detail workflows
- Confidence and next-step logic are server-owned and test-covered
- Source-health honesty is preserved even when live backup telemetry is unavailable

</success_criteria>

<output>
After completion, create `.planning/phases/05-backup-confidence-dashboard/05-02-SUMMARY.md`
</output>
