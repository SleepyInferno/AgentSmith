---
phase: 03-lifecycle-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - apps/api/src/modules/lifecycle/lifecycle.types.ts
  - apps/api/src/modules/lifecycle/lifecycle.templates.ts
  - apps/api/src/modules/lifecycle/lifecycle.service.ts
  - apps/api/src/modules/lifecycle/lifecycle.service.test.ts
  - apps/api/src/modules/lifecycle/lifecycle-summary.test.ts
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "Onboarding and offboarding each exist as reusable grouped templates instead of a flat checklist."
    - "Onboarding templates explicitly include group and checklist steps so LIFE-01 is fully covered."
    - "Starting a lifecycle run copies template groups and steps into an immutable run snapshot."
    - "Skipped and blocked steps cannot be recorded without an explicit reason."
    - "A finished run can produce a deterministic unresolved follow-up summary from persisted run-step state."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Lifecycle run persistence with grouped steps and structured evidence fields"
      contains: "model LifecycleRun"
    - path: "apps/api/src/modules/lifecycle/lifecycle.templates.ts"
      provides: "Reusable onboarding and offboarding template definitions with required group and checklist coverage"
      contains: "employee-onboarding"
    - path: "apps/api/src/modules/lifecycle/lifecycle.service.ts"
      provides: "Snapshot launch, step validation, and summary derivation helpers"
      exports: ["buildLifecycleRunSnapshot", "validateLifecycleStepUpdate", "buildLifecycleRunSummary"]
    - path: "apps/api/src/modules/lifecycle/lifecycle.service.test.ts"
      provides: "Launch and status validation coverage"
      contains: "required reason"
    - path: "apps/api/src/modules/lifecycle/lifecycle-summary.test.ts"
      provides: "Unresolved follow-up summary coverage"
      contains: "unresolved follow-up"
  key_links:
    - from: "apps/api/src/modules/lifecycle/lifecycle.templates.ts"
      to: "apps/api/src/modules/lifecycle/lifecycle.service.ts"
      via: "template snapshot builder"
      pattern: "templateVersion"
    - from: "apps/api/src/modules/lifecycle/lifecycle.service.ts"
      to: "apps/api/src/modules/lifecycle/lifecycle.service.test.ts"
      via: "launch and status validation tests"
      pattern: "validateLifecycleStepUpdate"
    - from: "apps/api/src/modules/lifecycle/lifecycle.service.ts"
      to: "apps/api/src/modules/lifecycle/lifecycle-summary.test.ts"
      via: "summary aggregation tests"
      pattern: "buildLifecycleRunSummary"
---

<objective>
Create the shared lifecycle engine contracts before routes or UI depend on them.

Purpose: Lock the grouped template model, tracking-only step semantics, immutable run snapshots, and server-owned summary rules for the rest of Phase 3.
Output: Prisma lifecycle schema additions, local lifecycle DTO/types, seeded onboarding and offboarding templates, and tested service helpers for launch, step validation, and summary derivation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-automation/03-CONTEXT.md
@.planning/phases/03-lifecycle-automation/03-RESEARCH.md
@prisma/schema.prisma

<interfaces>
From apps/api/src/server.ts:
```ts
export type BuildServerOptions = {
  env?: ServerEnv;
  prisma?: PrismaClient;
  assetRoutes?: Partial<AssetRoutesDependencies>;
};

export function buildServer(options: BuildServerOptions = {}) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle run persistence, statuses, and template contracts</name>
  <files>prisma/schema.prisma, apps/api/src/modules/lifecycle/lifecycle.types.ts, apps/api/src/modules/lifecycle/lifecycle.templates.ts</files>
  <read_first>prisma/schema.prisma, .planning/phases/03-lifecycle-automation/03-CONTEXT.md, .planning/phases/03-lifecycle-automation/03-RESEARCH.md, apps/api/src/modules/assets/asset-health.types.ts</read_first>
  <action>Extend `prisma/schema.prisma` with lifecycle-only enums and models per D-01, D-02, D-03, D-04, D-05, D-07, D-08, and D-09. Add `LifecycleRunStatus` with `active` and `completed`, `LifecycleStepStatus` with `pending`, `automated`, `manual`, `skipped`, and `blocked`, and models `LifecycleRun`, `LifecycleRunGroup`, and `LifecycleRunStep`. `LifecycleRun` must store `kind`, `templateKey`, `templateVersion`, `subjectDisplayName`, `subjectEmail`, `requestedBy`, `status`, `startedAt`, `closedAt`, and `updatedAt` so active-run views have a stable latest-update source of truth. `LifecycleRunStep` must store immutable snapshot fields `key`, `title`, `instructions`, `groupKey`, `position`, `status`, `statusReason`, `note`, `ticketId`, `assetId`, `mailboxRef`, `handoffRef`, and `completedAt`. Create `apps/api/src/modules/lifecycle/lifecycle.types.ts` exporting grouped template, run-detail, step-update, list-row, and summary shapes, including an `updatedAt` field on active-run responses. Create `apps/api/src/modules/lifecycle/lifecycle.templates.ts` with exactly two reusable templates keyed `employee-onboarding` and `employee-offboarding`. The onboarding template must explicitly include identity, licensing, group, device, and checklist steps to satisfy LIFE-01, and the offboarding template must explicitly include access removal, device recovery, handoff, and follow-up coverage for LIFE-02.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const schema=fs.readFileSync('prisma/schema.prisma','utf8');const templates=fs.readFileSync('apps/api/src/modules/lifecycle/lifecycle.templates.ts','utf8');if(!schema.includes('model LifecycleRun')||!schema.includes('model LifecycleRunGroup')||!schema.includes('model LifecycleRunStep')||!schema.includes('enum LifecycleStepStatus'))process.exit(1);for (const token of ['employee-onboarding','employee-offboarding','identity','licensing','group','device','checklist','handoff','follow-up']) { if (!templates.includes(token)) process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model LifecycleRun`
    - `prisma/schema.prisma` contains `model LifecycleRunGroup`
    - `prisma/schema.prisma` contains `model LifecycleRunStep`
    - `prisma/schema.prisma` contains `enum LifecycleStepStatus`
    - `prisma/schema.prisma` contains `updatedAt`
    - `apps/api/src/modules/lifecycle/lifecycle.templates.ts` contains `employee-onboarding`
    - `apps/api/src/modules/lifecycle/lifecycle.templates.ts` contains `employee-offboarding`
    - `apps/api/src/modules/lifecycle/lifecycle.templates.ts` contains `identity`
    - `apps/api/src/modules/lifecycle/lifecycle.templates.ts` contains `group`
    - `apps/api/src/modules/lifecycle/lifecycle.templates.ts` contains `checklist`
    - `apps/api/src/modules/lifecycle/lifecycle.templates.ts` contains `follow-up`
  </acceptance_criteria>
  <done>The lifecycle engine has stable persistence fields and reusable grouped templates that downstream routes and UI can target directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement snapshot launch, reason validation, and unresolved summary helpers</name>
  <files>apps/api/src/modules/lifecycle/lifecycle.service.ts, apps/api/src/modules/lifecycle/lifecycle.service.test.ts, apps/api/src/modules/lifecycle/lifecycle-summary.test.ts</files>
  <read_first>apps/api/src/modules/lifecycle/lifecycle.types.ts, apps/api/src/modules/lifecycle/lifecycle.templates.ts, .planning/phases/03-lifecycle-automation/03-VALIDATION.md, apps/api/src/modules/assets/asset-health.service.test.ts</read_first>
  <behavior>
    - Test 1: launching `employee-onboarding` returns grouped run data with ordered `identity`, `licensing`, `group`, `device`, and `checklist` phases copied into run-step snapshots per D-01, D-03, and LIFE-01
    - Test 2: launching `employee-offboarding` returns grouped run data with `access`, `device`, `handoff`, and `follow-up` phases, preserving immutable step keys and positions
    - Test 3: `validateLifecycleStepUpdate` rejects `skipped` and `blocked` updates when `statusReason` is empty per D-08
    - Test 4: `buildLifecycleRunSummary` reports completed, blocked, skipped, and unresolved follow-up counts from run-step state per D-10, D-11, and D-12
  </behavior>
  <action>Create `apps/api/src/modules/lifecycle/lifecycle.service.test.ts` and `apps/api/src/modules/lifecycle/lifecycle-summary.test.ts` first, then implement `apps/api/src/modules/lifecycle/lifecycle.service.ts`. Export `buildLifecycleRunSnapshot(templateKey, subject)` to copy template groups and steps into ordered run-ready records with `templateVersion`, default `pending` statuses, and immutable snapshot instructions per D-03 and D-04. The onboarding snapshot behavior must explicitly preserve identity, licensing, group, device, and checklist coverage so LIFE-01 is fully represented in code and tests. Export `validateLifecycleStepUpdate(input)` to allow only `automated`, `manual`, `skipped`, and `blocked` as completion statuses while enforcing `statusReason` for `skipped` and `blocked` per D-07 and D-08. Export `buildLifecycleRunSummary(run)` to compute `completedCount`, `manualCount`, `skippedCount`, `blockedCount`, `unresolvedFollowUps`, and grouped summaries, treating follow-up work, blocked steps, and skipped steps as explicit unresolved items per D-10, D-11, and D-12.</action>
  <verify>
    <automated>node --import tsx --test apps/api/src/modules/lifecycle/lifecycle.service.test.ts apps/api/src/modules/lifecycle/lifecycle-summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/lifecycle/lifecycle.service.ts` contains `buildLifecycleRunSnapshot`
    - `apps/api/src/modules/lifecycle/lifecycle.service.ts` contains `validateLifecycleStepUpdate`
    - `apps/api/src/modules/lifecycle/lifecycle.service.ts` contains `buildLifecycleRunSummary`
    - `apps/api/src/modules/lifecycle/lifecycle.service.test.ts` contains `employee-onboarding`
    - `apps/api/src/modules/lifecycle/lifecycle.service.test.ts` contains `group`
    - `apps/api/src/modules/lifecycle/lifecycle.service.test.ts` contains `checklist`
    - `apps/api/src/modules/lifecycle/lifecycle.service.test.ts` contains `required reason`
    - `apps/api/src/modules/lifecycle/lifecycle-summary.test.ts` contains `unresolved follow-up`
  </acceptance_criteria>
  <done>The lifecycle engine can generate immutable grouped runs, enforce exception reasons, and derive final summary truth without any UI logic.</done>
</task>

</tasks>

<verification>
- [ ] `node --import tsx --test apps/api/src/modules/lifecycle/lifecycle.service.test.ts apps/api/src/modules/lifecycle/lifecycle-summary.test.ts` succeeds
- [ ] Lifecycle schema names and service exports match the route and UI contracts planned for later waves
- [ ] Run summaries derive unresolved work from persisted run-step state instead of UI-only calculations
</verification>

<success_criteria>

- Onboarding and offboarding templates exist as reusable grouped definitions
- Lifecycle runs can be launched as immutable snapshots without implying live admin execution
- Skipped and blocked steps require an explicit reason
- Final summary logic already knows how to surface unresolved follow-up work

</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-automation/03-01-SUMMARY.md`
</output>
