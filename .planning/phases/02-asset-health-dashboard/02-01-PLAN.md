---
phase: 02-asset-health-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - prisma/schema.prisma
  - apps/api/src/modules/assets/asset.types.ts
  - apps/api/src/modules/assets/risk-score.ts
  - apps/api/src/modules/assets/risk-score.test.ts
  - apps/api/src/modules/assets/assets.repository.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-04
must_haves:
  truths:
    - The backend has a normalized asset shape with the Phase 2 health fields required for inventory, filtering, and ranking.
    - Asset risk scoring produces a deterministic priority order instead of ad hoc UI sorting.
    - Every scored asset carries an explicit freshness state so stale or incomplete source data is never implied to be healthy.
  artifacts:
    - path: apps/api/src/modules/assets/asset.types.ts
      provides: Canonical asset inventory, detail, and queue contracts
    - path: apps/api/src/modules/assets/risk-score.ts
      provides: Weighted asset risk calculation and stale-data escalation
    - path: apps/api/src/modules/assets/assets.repository.ts
      provides: Canonical Prisma reads for Phase 2 asset surfaces
    - path: prisma/schema.prisma
      provides: Device health, ownership, site, and storage fields required by Phase 2
  key_links:
    - from: prisma/schema.prisma
      to: apps/api/src/modules/assets/assets.repository.ts
      via: Prisma device field selection
      pattern: "department|site|diskFreeGb|encryptionState|antivirusState|patchState|assetAgeDays|unsupportedOs"
    - from: apps/api/src/modules/assets/assets.repository.ts
      to: apps/api/src/modules/assets/risk-score.ts
      via: Repository rows mapped into scoring input
      pattern: "scoreAssetRisk"
    - from: apps/api/src/modules/assets/risk-score.ts
      to: later queue and inventory APIs
      via: Shared riskSummary and freshness metadata
      pattern: "riskReasons|freshnessState|dataCompleteness"
---

<objective>
Define the backend asset-health domain and deterministic scoring contract for Phase 2.

Purpose: Give the dashboard a trustworthy device model, a reusable risk engine, and explicit stale-data semantics before any asset API or UI is built.
Output: Prisma field additions, asset domain types, repository contract, risk-scoring logic, and executable tests.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/AI/AgentSmith/AGENTS.md
@F:/AI/AgentSmith/.planning/PROJECT.md
@F:/AI/AgentSmith/.planning/ROADMAP.md
@F:/AI/AgentSmith/.planning/REQUIREMENTS.md
@F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-VALIDATION.md
@F:/AI/AgentSmith/.planning/research/SUMMARY.md
@F:/AI/AgentSmith/.planning/research/ARCHITECTURE.md
@F:/AI/AgentSmith/.planning/research/STACK.md
@F:/AI/AgentSmith/.planning/research/PITFALLS.md
@F:/AI/AgentSmith/prisma/schema.prisma
@F:/AI/AgentSmith/apps/api/src/server.ts

<interfaces>
From prisma/schema.prisma:
```prisma
model Device {
  id              String   @id @default(cuid())
  sourceSystem    String
  sourceId        String
  name            String
  serialNumber    String?
  operatingSystem String?
  complianceState String?
  ownerId         String?
  lastSeenAt      DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

From apps/api/src/server.ts:
```ts
export function buildServer() {
  const env = parseServerEnv();
  const app = Fastify({ logger: true });
  app.register(registerHealthRoute);
  return { app, env };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the asset-health contract and executable scoring test harness</name>
  <files>apps/api/package.json, apps/api/src/modules/assets/asset.types.ts, apps/api/src/modules/assets/risk-score.test.ts</files>
  <read_first>apps/api/package.json, F:/AI/AgentSmith/.planning/REQUIREMENTS.md, F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-VALIDATION.md, F:/AI/AgentSmith/AGENTS.md</read_first>
  <action>Per ASST-01, ASST-02, and ASST-04, create `apps/api/src/modules/assets/asset.types.ts` with exported contracts named `AssetInventoryItem`, `AssetDetail`, `AssetQueueItem`, `AssetFreshnessState`, `AssetRiskLevel`, and `AssetRiskReason`. The inventory/detail shape must include the exact fields `id`, `name`, `ownerName`, `department`, `site`, `operatingSystem`, `osVersion`, `encryptionState`, `antivirusState`, `patchState`, `lastCheckInAt`, `diskFreeGb`, `assetAgeDays`, `unsupportedOs`, `freshnessState`, `dataCompleteness`, `riskLevel`, `riskScore`, and `riskReasons`. Update `apps/api/package.json` so `pnpm --filter @agentsmith/api test` executes TypeScript source tests directly for `src/**/*.test.ts`. Add `apps/api/src/modules/assets/risk-score.test.ts` with table-driven tests covering stale check-in, missing encryption, missing AV, low disk, unsupported OS, and stale connector metadata so the test file defines the exact expected ordering contract before implementation.</action>
  <acceptance_criteria>
    - apps/api/package.json contains `"test": "tsx --test src/**/*.test.ts"`
    - apps/api/src/modules/assets/asset.types.ts contains `export type AssetFreshnessState = "healthy" | "warning" | "stale" | "error"`
    - apps/api/src/modules/assets/asset.types.ts contains `export interface AssetInventoryItem`
    - apps/api/src/modules/assets/risk-score.test.ts contains `scoreAssetRisk`
    - apps/api/src/modules/assets/risk-score.test.ts contains `missing-encryption` and `stale-source-data`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agentsmith/api test</automated>
  </verify>
  <done>The API package has a real Phase 2 test path and a source-of-truth asset contract that downstream plans can import without rediscovering fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the canonical device model and implement deterministic asset risk scoring</name>
  <files>prisma/schema.prisma, apps/api/src/modules/assets/risk-score.ts, apps/api/src/modules/assets/assets.repository.ts, apps/api/src/modules/assets/risk-score.test.ts</files>
  <read_first>prisma/schema.prisma, apps/api/src/modules/assets/asset.types.ts, apps/api/src/modules/assets/risk-score.test.ts, F:/AI/AgentSmith/.planning/research/PITFALLS.md</read_first>
  <behavior>
    - Test 1: assets with `unsupportedOs=true` or `lastCheckInAt` older than 14 days rank above otherwise healthy assets
    - Test 2: assets with `encryptionState="missing"` or `antivirusState="missing"` produce `riskLevel="high"` and a matching reason code
    - Test 3: assets with stale connector freshness force `freshnessState="stale"` and include a stale-data reason even if endpoint signals look healthy
  </behavior>
  <action>Update `prisma/schema.prisma` so `Device` holds the normalized Phase 2 read model without adding any write-action tables: `ownerName String?`, `department String?`, `site String?`, `osVersion String?`, `encryptionState String?`, `antivirusState String?`, `patchState String?`, `diskFreeGb Float?`, `assetAgeDays Int?`, `unsupportedOs Boolean @default(false)`, `lastCheckInAt DateTime?`, `connectorSourceId String?`, and `dataCompleteness String?`. Implement `apps/api/src/modules/assets/risk-score.ts` exporting `scoreAssetRisk(asset)` and `compareAssetRisk(a, b)` with concrete weights: stale check-in over 14 days = 40 points, missing encryption = 35, missing AV = 30, patch status `outdated` = 20, disk below 15 GB = 15, unsupported OS = 30, incomplete data = 10, stale connector freshness = 50. Return `riskLevel` bands `high` for 70+, `medium` for 35-69, `low` for 1-34, and `healthy` for 0, plus ordered `riskReasons` codes. Implement `apps/api/src/modules/assets/assets.repository.ts` to read normalized device rows from Prisma and attach connector freshness metadata through the existing `ConnectorSource` relation pattern, keeping all provider-specific logic out of this module per AGENTS.md.</action>
  <acceptance_criteria>
    - prisma/schema.prisma contains `ownerName String?`, `department String?`, `site String?`, `diskFreeGb Float?`, and `unsupportedOs Boolean @default(false)`
    - prisma/schema.prisma contains `lastCheckInAt DateTime?` and `connectorSourceId String?`
    - apps/api/src/modules/assets/risk-score.ts contains `scoreAssetRisk`
    - apps/api/src/modules/assets/risk-score.ts contains `70 ? "high"` or `riskScore >= 70`
    - apps/api/src/modules/assets/assets.repository.ts contains `ConnectorSource`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agentsmith/api test && pnpm --filter @agentsmith/api build</automated>
  </verify>
  <done>The backend can compute a stable per-device risk score and freshness-aware summary from canonical fields alone.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @agentsmith/api test` passes with Phase 2 asset tests
- `pnpm --filter @agentsmith/api build` passes after schema and module additions
- Risk scoring exposes stale-data reasons explicitly rather than hiding them in null checks
</verification>

<success_criteria>
Phase 2 has a concrete backend asset model, deterministic ranking logic, and explicit stale-data semantics that later plans can reuse without redefining business rules.
</success_criteria>

<output>
After completion, create `F:/AI/AgentSmith/.planning/phases/02-asset-health-dashboard/02-01-SUMMARY.md`
</output>
